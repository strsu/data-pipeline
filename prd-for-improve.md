# prd-for-improve — 웹툰 분석의 염원

> 이 문서는 **prd.md와 무관하게** 유지한다. prd.md는 "지금 돌아가는 것"의 정본이고, 이 문서는
> **"가야 할 곳"**이다. 섞이면 둘 다 흐려진다. 여기 적힌 것은 현행 구조에 대한 수정 지시가 아니라,
> 언젠가 도달하고 싶은 상태와 그 근거다. 구현 계획이 아니라 **원칙과 계약**을 담는다.

작성: 2026-07-16. 갱신: 2026-07-17(화산귀환), 2026-07-17(참교육 — **계약 하나가 깨졌다**).

> ⚠️ **이 문서는 세션이 끊겨도 이어갈 수 있게 자족적으로 쓴다.** 대화 기억에 의존하지 말 것.
> 재현 절차는 §8, 현재 상태와 다음 할 일은 §7에 있다.

검증 표본: **웹툰 3개 6회차 726컷**을 Claude가 **원본 이미지만 보고** 직접 읽었다.

| 웹툰 | 회차 | 컷 | 장르/작화 | 문서 |
|---|---|---|---|---|
| 게임 속 바바리안으로 살아남기(23) | ep10·ep11 | 107+119 | 게임 판타지 / 셀 | `ep10-manual-analysis-2026-07-16.md`(**전수**) · `ep11-…`(**전수**) |
| 화산귀환(17) | ep1·ep2 | 146+117 | 무협 / 수묵 채색 | `hwasan-manual-analysis-2026-07-17.md`(일부 표본) |
| 참교육(43) | ep1·ep2 | 137+100 | 현대 학원 드라마 / 사실체 | `chamgyoyuk-manual-analysis-2026-07-17.md`(표본, **ep2는 거의 안 읽음**) |

보조 근거: `face-classify-w23-2026-07-16.md`(CCIP 43%), `pass1-bench-w23-2026-07-16.md`(1:1 위반 ~60%),
`pass1-window/README.md`(**실패한 실험**).

**계약 승격 규칙: 웹툰 2개 이상에서 재현된 것만 계약으로 둔다.** 한 작품에서만 보인 건 열린 질문(§5).
2026-07-17 참교육(대조군)이 **C8을 깨뜨렸다** — 표본 편향이 실제로 있었다는 뜻이므로,
이 규칙을 앞으로 더 엄격히 적용한다.

---

## 0. 염원

**웹툰을 사람이 읽듯 읽고, 사람이 아는 만큼 알고, 모르는 건 모른다고 말하는 분석기.**

세 개로 나눠 적는다.

1. **하나도 버리지 않는다.** 컷도, 대사도, 인물도, 판단의 근거도. 분석 산출은 언젠가 질문봇의
   지식베이스가 된다 — 버린 것은 다시 오지 않는다.
2. **모르는 건 모른다고 한다.** 억지 판정은 틀린 지식을 만들고, 틀린 지식은 침묵보다 나쁘다.
   `null`은 실패가 아니라 정직한 상태다.
3. **틀려도 국소적으로 틀린다.** 한 단계의 오류가 다음 단계를 오염시키지 않도록, 판단을 분리하고
   각 판단에 근거와 확신도를 붙여 나중에 고칠 수 있게 남긴다.

---

## 1. 두 원칙

### 원칙 1 — 모든 컷은 분석에 사용한다

컷은 어떤 이유로도 분석에서 배제되지 않는다. OCR이 글자를 못 찾았다는 것은 **글자가 없다는 증거가
아니라 OCR이 못 찾았다는 사실**일 뿐이다.

**근거(실측 — 웹툰 3개 6회차):**

| | 바바리안 ep10 | 바바리안 ep11 | 화산 ep1 | 화산 ep2 | 참교육 ep1 | 참교육 ep2 |
|---|---|---|---|---|---|---|
| 전체 컷 | 107 | 119 | 146 | 117 | 137 | 100 |
| `regions=0 AND faces=0` 스킵 | 12 (11.2%) | 11 (9.2%) | 8 (5.5%) | **0** | 3 (2.2%) | 1 (1.0%) |
| 그중 **실제로 내용이 있던 컷** | **8** | **9** | **6** | — | 미확인 | 미확인 |
| 대표 사례 | **컷 3**: "컥!" 말풍선 + 얼굴 또렷 → OCR·YOLO 동시 누락 | **컷 29**: 대형 말풍선 2개 + 얼굴 / **컷 98**: 적대 파티 첫 등장(회차 최대 전환점) | 컷 12·31·43·44·80 (SFX) | — | (3·5·116) | (28) |

**스킵률은 작품마다 크게 다르다** — 참교육(현대극, 말풍선 위주)은 1~2%, 바바리안은 11%.

⚠️ **그러나 "스킵률이 낮으면 안전하다"는 틀렸다.** 참교육은 스킵률이 가장 낮은데(2.2%),
버린 것은 **1화 도입부의 핵심 설명 2컷**이다(2026-07-17 눈으로 확인):

| 컷 | 실제 내용 | DB |
|---|---|---|
| 참교육 ep1 **c3** | 나레이션 박스 **3개** + 각진 말풍선 대사 + **법정 장면 얼굴 2명** | `regions=0, faces=0` |
| 참교육 ep1 **c5** | 나레이션 박스 **2개** + 뉴스 화면(JBSN) + 자막 "제자에게 폭행당한 교사… 결국 사망" | `regions=0, faces=0` |

**평범한 인쇄체다** — 스타일 아트도 프롭도 아니다. 그리고 **바로 옆 컷(1·2·4·6·7·8)은 정상 검출**된다.

### 🔥 손실의 뿌리를 규명했다 — **거대 세그먼트** (2026-07-17, 정본: `segment-oversize-2026-07-17.md`)

OCR은 컷이 아니라 `analysis_episode_segment`(텍스트 밴드) 단위로 돈다. 참교육 ep1의 seg1351은
**690×7,965px(종횡비 11.5:1)**이고, **컷 3(y3200~4800)·컷 5(y6400~8000)가 그 안에 있다.**

**결정적 A/B 실험 — 같은 픽셀, 8배 차이:**

| 실험 | 입력 | OCR 검출 |
|---|---|---|
| A | **컷 3 단독** (690×1600) | **33개** ✅ '학생들이' '마음놓고' '교권을' '침해하는' … |
| B | **프로덕션 세그먼트** (690×**7,965**) | **4개** ❌ '여려워졌더' '누브니' '국회화고육부는' |

**실험 B는 프로덕션 DB와 정확히 일치**(seg1351의 regions=4) — 손실 재현 성공.

**원인**: `_content_intervals`는 여백 행이 `MIN_BAND_PX(10)` 이상 연속돼야 세그먼트를 끊는데,
참교육 프롤로그는 **검은 배경이 8,000px 동안 끊김 없이** 이어져 여백 밴드가 안 나온다.
상한 `MAX_BUFFER_PX=16,000`은 **메모리 상한이지 OCR 입력 상한이 아니라** 강제 방출에도 안 걸린다.

**✅ 해법 검증됨 — 4개 → 117개(29배).** 같은 픽셀을 2,000px 타일(겹침 100px)로 잘라 재투입.
코드 수정 없이 입력 크기만 바꿔 재현.

**세 작품 모두에 존재**: 4,000px 초과 세그먼트가 참교육 ep1에 1개, **바바리안 ep10에 5개**,
화산귀환 ep1에 1개.

**→ §C1의 근거가 한 층 더 내려간다: "OCR이 못 읽었다"가 아니라 "OCR에 뭉개서 보냈다".**
OCR을 더 좋은 모델로 바꿔도 소용없다(Baidu 철거 결정은 여전히 옳다 — 문제가 모델이 아니었다).
그리고 지금까지 "OCR이 효과음을 못 읽는다"로 이해한 것의 **상당수가 이것**일 수 있다.

⚠️ **미수정** — 프로덕션 Step1 변경이라 승인 필요. 반증한 가설도 문서에 기록(`_is_near_uniform_block`
의심 → 측정해보니 단색 비율 0.55로 임계 0.98 미달, 폐기).

**회차의 5~11%가 Stage V 호출조차 안 되고, 그 대부분은 진짜 빈 컷이 아니다.** 이 손실은
`skipped=empty`로 조용히 기록돼 지표에 안 잡힌다 — 벤치의 "유효 컷 95"가 바로 이것이다.

⚠️ **회차 편차가 크다**(화산 ep2는 0). 따라서 "평균 10% 손실"이 아니라 **"언제든 터진다"**로
취급해야 한다. 평균으로 보면 감내할 만해 보이지만, 실제로는 ep11 컷 98(적대 파티 첫 등장 =
회차 최대 전환점)처럼 **하필 중요한 컷이 사라진다.**

### 원칙 2 — 인물은 A/B/C로 분류하고, 이름은 나중에 붙인다

컷 단위 분석기는 **정체성 입력을 받지 않는다**. 인물은 회차 내부에서 익명 ID로만 묶는다. 이름은
증거가 다 모인 뒤 별도 단계가 붙인다.

**근거(실측 — 웹툰 3개 6회차):**

| | 바바리안 ep10 | 바바리안 ep11 | 화산 ep1 | 화산 ep2 | **참교육 ep1** |
|---|---|---|---|---|---|
| 구분되는 인물(외형) | 2 | **8~15** | **9~12** | 6~8 | **10+** (교복 다수) |
| **이름이 등장하는 인물** | **1** (에르웬) | **2** (에르웬, 비요른) | **2** (청명, 천마) | **2** (청명, 초삼) | **2** (나화진, 대식) |
| 이름 언급 / 컷 | 3/107 (**2.8%**) | 7/119 (**5.9%**) | 6/146 (**4.1%**) | 3/117 (**2.6%**) | **2/137 (1.5%)** |
| 주인공 명명 가능? | ❌ 호칭 "아저씨"뿐 | ⚠️ 1회, OCR 파손 | ✅ 타이틀 카드 | ✅ 자칭 | ✅ **공무원증** |

⚠️ **로스터와 이름의 대응은 1:1이 아니다**(참교육 신규 발견):
- **이름 없는 외형**: 참교육 B(1인칭 화자인 주인공 학생!), 교장, 일진, 교사들 — 전원 이름 0.
- **외형 없는 이름**: 참교육 **대식**은 **화면에 한 번도 안 나오는데 이름만 있다**(이미 자살한 선배).
  익명 로스터(외형 기반)에 아예 안 잡히고 **텍스트에만 존재**한다.
  → 명명 단계는 "로스터의 각 슬롯에 이름 붙이기"만으로 부족하다. **슬롯에 못 붙는 이름**도 있다.

**인물이 몇 명이든 이름은 2개다.** ep10은 인물 2명에 이름 1개, ep11은 인물 8~15명에 이름 2개.
**인물이 7배 늘어도 이름은 안 늘어난다.** 컷 단위로 "이게 누구야?"를 물으면 **95~97%의 컷에서
근거가 없다.** 현행 구조는 이 공백을 **43% 정확도인 CCIP 추정값**으로 메우고, 그걸
"identified_faces"라 부르며 프롬프트에 진실처럼 주입한다.

반대로 **익명 추적은 전 컷에서 근거가 있다** — 은발/뾰족귀/활 vs 근육질/망치는 컷마다 보인다.

**이름 없는 게 정상인 인물이 다수다.** ep11의 적대 파티 4~6명, 몽타주 군중 ~7명, 화산 ep1의
화산 문인·사부·왕초·거지 소년(2화 내내 등장) — **전원 이름 증거 0.** 이들은 영원히 익명이어야
정상이고, 억지 명명을 요구하는 것이 곧 오귀속이다.

⚠️ **직함은 이름이 아니다.** "장문 사형"(화산 F), "왕초"(화산 G), "아저씨"(바바리안 A)는 전부
호칭·직함이다. 명명 단계가 이걸 이름으로 승격시키면 유령 인물이 생긴다.

---

## 2. 근본 재구성 — 의존 방향의 역전

이건 "명명 단계를 하나 추가하자"가 아니다. **정체성이 흐르는 방향을 뒤집는 일**이다.

```
[현행]  CCIP가 먼저 정체를 판정 → Stage V가 그걸 진실로 받음 → 화자·서사까지 오염
        정체성이 비전 단계로 흘러 "들어간다"

[염원]  Stage V는 묶음만 만든다 → 회차 로스터로 통합 → 명명·링킹은 마지막에 별도 판단
        정체성이 비전 단계에서 흘러 "나온다"
```

| | 현행 | 염원 |
|---|---|---|
| 컷 분석기가 받는 정체성 | 43% 정확한 추정 이름 | **없음** (봐서 판단) |
| 근거 있는 컷 비율 | 이름 근거 2.8~5.9% | 시각 근거 100% |
| 실패 시 파급 | 틀린 이름이 화자·서사까지 오염 | 명명만 틀림, A/B/C 묶음은 생존 |
| 이름 없는 인물 | 억지 귀속 → 오귀속 | `null` + 증거 보존 |

### 현행 피해 실측 (웹툰 23)

- **`cluster` 225개 vs `character` 17명** — 클러스터가 인물의 **13배**.
- ep11 오귀속: 비요른 얼굴 → **이한수**(3컷), 에르웬 얼굴 → **아이나르**(1컷), 몽타주 군중 → **헤이나**(1컷).
  **아이나르·헤이나는 11화에 등장하지도 않는다.**
- 오귀속의 방향이 전부 같다: **"모르는 얼굴 → 갤러리에서 가장 가까운 기존 인물".**
  CCIP는 "모름"을 말할 수 없어서 억지로 답한다.
- 이 패턴은 `face-classify-w23-2026-07-16.md`의 오답 분포(비요른→이한수 23건, 에르웬→아이나르 11건)가
  **프로덕션에서 그대로 재현된 것**이다. 즉 이미 알고 있던 실패가 파이프라인에 그대로 산다.

### 그러나 CCIP는 버리는 게 아니라 직무를 바꾼다

ep11 실측: 적대 파티 리더(흰 머리띠)는 **5개 컷(100·107·112·113·116)이 정확히 하나의 클러스터**로
묶였다. 즉 —

> **"같은 사람인가"는 된다. "누구인가"가 안 된다.**

CCIP를 **회차 내 클러스터링 보조**로 내리고, 명명에서는 여러 증거 중 **투표권 하나**로 강등한다.
43%짜리를 진실로 주입하는 것과 투표권 하나로 두는 것은 완전히 다르다.

---

## 3. 원칙에서 파생되는 계약

ep10·ep11 실측에서 실증된 것만 적는다.

### C1. OCR은 앵커이지 문지기가 아니다

- `regions=0`은 컷을 폐기할 사유가 **아니다**. 컷은 무조건 분석된다.
- **VLM이 이미지에서 직접 읽은 텍스트가 OCR 원문을 덮어쓸 수 있어야 한다.**
  `llm` 주석은 지금 OCR 문자열을 **그대로 복사**한다 — 1:1 바인딩 계약상 고칠 권한이 없기 때문이다.
  **나는 OCR 없이 이미지만 보고 그 글자들을 문제없이 읽었다.** VLM이 못 읽는 게 아니라, OCR의
  오독을 정본으로 주기 때문에 못 고치는 것이다.
- ⚠️ 해법은 "더 나은 OCR"이 아니다. Baidu Unlimited-OCR은 실측 후 철거됐다(웹툰엔 PaddleOCR 압승 —
  ~15배 느리고 한글 스타일텍스트 오타). PaddleOCR은 유지하되 **권한을 낮춘다**.

#### C1.1 ⚠️ 명명 증거가 가장 많은 곳이 OCR이 가장 약한 곳이다 (구조적 함정)

**웹툰은 인물을 대사로만 소개하지 않는다.** 명명 경로는 3종이고, **②③은 OCR이 가장 약한 형태**다.
웹툰 3개에서 전부 재현됐다 — 우연이 아니라 **매체의 관습**이다.

| 경로 | 실례 | OCR 난이도 |
|---|---|---|
| ① **대사 호명** | "앞장서라, 에르웬"(바바리안) / "이놈, 청명아!!"(화산) / "나화진 감독관님"(참교육 ep2) | 낮음 |
| ② **타이틀/별명 카드** | "비요른 THE 천수관음" / "매화검존 청명" / "天魔 천마" | **높음**(스타일 아트) |
| ③ **문서·소품(prop)** 🆕 | **참교육 c124 공무원증 "나 화 진 / 교권보호국"** | **높음**(기울어진 원근, 배경과 혼재) |

③은 ②와도 다르다 — 카드는 화면 정면의 디자인 텍스트지만, 소품은 **장면 안의 물체**다.
게임 상태창(바바리안), 뉴스 자막(참교육 c5)도 같은 부류(**diegetic screen/document text**).

**OCR 실측 — 스타일 강도에 비례해 무너진다(3작품 8건):**

| 컷 | 실제 | PaddleOCR 결과 | 판정 |
|---|---|---|---|
| 화산 ep1 c59 | 대화산파 13대 제자 / 매화검존 / **청명** | 전부 정확 | ✅ 깔끔한 명조체 |
| 참교육 ep1 c124 | **나화진**(공무원증) | **'나화진'** | ✅ 살아남음 |
| 화산 ep1 c18 | **天魔** / 천마 | **'天權'** / 천마 | ⚠️ 한자만 오독 |
| 참교육 ep1 c124 | 교권보호국 / 공무원증 | **'교권보호구'** / **'공무원승'** | ❌ **같은 컷인데 갈림** |
| 화산 ep1 c145 | **화산귀환** / 제 1 화 | **'화한'** / **'제11화'** | ❌ 붓글씨 로고 |
| **참교육 ep1 c12** | **참교육**(타이틀 로고) | **'참 물'** | ❌ 로고 파손 |
| 바바리안 ep11 c68 | **비요른** | **'비요문서'** | ❌ 붓글씨 |
| 참교육 ep1 c4 | 파이차트 텍스트 8개(수치 포함) | **1개만, '여려워졌더'** | ❌ **거의 전멸** |

**핵심 3가지:**
1. **OCR이 무조건 못 읽는 게 아니라, 스타일이 강할수록 무너진다.** 그리고 이름은 스타일이 강한
   쪽에 몰려 있다.
2. **같은 컷 안에서도 갈린다**(참교육 c124: 이름은 살고 직함은 깨짐). 청명·나화진이 살아남은 건
   **운이지 구조가 안전해서가 아니다.**
3. **타이틀 로고는 3작품 3/3 파손**('비요문서'·'화한'·'참 물'). 재현율 100%.

**함의:** 명명 파이프라인이 OCR 텍스트에만 의존하면 **이름 증거의 상당수를 원천적으로 못 본다.**
비요른은 유일한 증거가 '비요문서'라는 유령 단어가 됐고, 최악의 경우 그 유령이 인물로 등록된다.
→ 명명 단계는 **VLM이 이미지에서 직접 읽은 카드/문서 텍스트**를 증거원으로 받아야 한다.

- 부수 발견: 타이틀 카드가 **회차 끝**에 오기도 한다(화산 ep1 c145/146, 참교육 c12는 앞).
  "앞에서 소개한다"는 가정을 두면 안 된다.

### C2. 익명 ID는 회차 스코프다 (컷 스코프가 아니라)

- 현행 F0/F1은 **컷마다 재배정**된다. 같은 인물이 컷마다 다른 라벨을 갖는다 — 정체성이 어려운 이유의
  큰 부분이다.
- 컷 하나만 보는 콜은 **원리적으로 클러스터링을 못 한다**("이 얼굴 = 30컷 전 그 얼굴"을 알 수 없다).
  따라서 회차 전체를 보는 클러스터링 단계가 별도로 필요하다. **바로 여기가 CCIP가 정당하게 일할 자리다.**

### C3. 익명 묶음은 1급 영속 엔티티다

- "일단 A로 저장하고 나중에 후처리"가 가능하려면 **A가 임시 변수가 아니라 저장된 개체**여야 한다.
  안 그러면 후처리 시 VLM을 다시 돌려야 한다.
- 묶음에는 시각 서술·대표 얼굴 crop·등장 컷·근거가 함께 남는다.

### C4. 링크는 별도의 수정 가능한 엣지다

- 연결은 추출 결과에 이름을 **박아넣는 게 아니라**, `확신도 + 출처`를 단 엣지로 저장한다.
- 그러면 **"즉시 CCIP로 연결"과 "나중에 후처리"는 대립하는 선택지가 아니라 같은 링커를 다른 시점에
  돌리는 것**이 된다. 시간이 지나면 증거만 늘 뿐(다음 화 등장, 사람 라벨, 이름 언급) 로직은 같다.
- 링크 실패는 **에러가 아니라 정상 상태**다. A/B/C 묶음은 안 건드린다.
- 기존 스키마가 이미 이 모양이다: `analysis_character(kind=cluster|character)`의 클러스터→인물 승격,
  `analysis_suggestion`의 보류 큐. **그릇은 있는데 순서가 거꾸로일 뿐이다.**

### C5. 명명은 `null`을 반환할 수 있어야 한다

- ep10의 A는 회차 내부 증거로 **명명 불가**다(호칭 "아저씨"뿐). ep11의 적대 파티 4~6명, 몽타주 군중
  ~7명도 **전원 이름 없음**.
- 출력은 `{local_id: "A", name: null, evidence: ["아저씨 호칭", "바바리안", "망치+방패", "빙의 독백"]}`.
  링킹은 prior 로스터/얼굴이 맡는다. **억지로 이름을 붙이게 만드는 것이 정확히 지금의 오귀속이다.**

### C6. 명명은 회차 전역 1콜이다

- 이름 증거가 3~7컷에만 있으므로 **컷 단위로는 물어볼 수조차 없다.**
- 컷별 익명 추출 → 회차 단위 통합 명명. 정보 구조와 일치한다.

### C7. 로스터에 "군중/단역" 등급이 필요하다

- ep11 컷 38 몽타주 ~7명은 **1컷 등장·대사 0·이름 0**이다. 이들을 정식 인물로 승격시키는 것이
  **225 클러스터의 정체**다.
- `analysis_character.significance`·Pass1 `prominence(main|minor|extra)`가 그릇인데 **승격 게이트로
  쓰이지 않고 있다.**
- 군중은 클러스터로 남는 게 정답이다. 승격은 증거가 요구한다.

### C8. 시각적 동일성 ≠ 서사적 동일성 — ⚠️ **장르 조건부**(2026-07-17 격하)

> **정정 이력:** 2026-07-16에 이 계약을 "보편"으로 적었다. **틀렸다.** 근거였던 바바리안·화산귀환이
> **둘 다 회귀/환생물**이었다 — 내 표본 편향이었다. 2026-07-17 참교육(현대극, 대조군)에는
> **빙의도 환생도 없고 한 인물 = 한 외형**이다.
>
> **따라서: "항상 발생"이 아니라 "장르에 따라 발생".** 계약은 **유지**한다 — 빈도는 낮아도
> 터지면 치명적이고(비요른 얼굴 3컷이 이한수로 오귀속), 회귀물은 한국 웹툰에서 거대한 장르다.
> 다만 **모든 작품에 이 복잡도를 강제하면 안 된다.** 링커가 "이 작품에 그런 일이 있나"를
> 먼저 판단하고, 없으면 단순 경로로 가야 한다.

| 웹툰 | 회귀/빙의 | C8 발생 |
|---|---|---|
| 바바리안 | 빙의(이한수→비요른) | ✅ 발생 — 오귀속 실측 |
| 화산귀환 | 환생(청명→초삼의 몸) + 100년 | ✅ 발생 — **한 회차에 4형태** |
| **참교육** | **없음** | ❌ **미발생 — 한 인물 = 한 외형** |

발생하는 경우는 아래와 같고, **화산귀환 쪽이 훨씬 극단적이다.**

- **바바리안**: 비요른/이한수는 같은 사람의 다른 몸(빙의). 시각적으론 남남, 서사적으론 동일인.
  (ep11에서 비요른 얼굴 3컷이 이한수로 귀속됨 — 실제 오귀속)
- **화산귀환**: 청명은 **1화 한 회차 안에서만 네 가지 몸**이다.

```
ep1 c1~62   성인 청명 (긴 흑발, 화산 도포, 검)          ← 백 년 전, 죽음
ep1 c63~65  갓난아기 (짚 바구니)                        ← 주마등: 화산 문 앞에 버려짐
ep1 c66~71  소년 제자 (수련복)                          ← 주마등: 화산에서 자람
ep1 c80~ ep2 거지 소년 '초삼'의 몸 (봉두난발, 누더기)    ← 백 년 후 환생
```

**임베딩으로는 절대 이을 수 없다.** 아기·소년·성인·거지소년은 픽셀상 완전한 남남이다.
어떤 얼굴 모델도 이 넷을 묶지 못하고, **묶으려 시도하는 것 자체가 잘못된 요구**다.

#### C8.1 한 인물에 이름이 둘 — alias/정본 구분이 필요하다

화산귀환에서 청명은 **`청명`(인격/전생)과 `초삼`(현재의 몸)** 두 이름을 갖는다.
- ep2 c24: 다른 거지가 "너는 '**초삼**'이잖냐" → 몸의 이름
- ep2 c117: 본인이 "**본도 청명**, 화산에 입문하고 싶습니다!" → 인격의 이름(자칭)

바바리안의 비요른(몸)/이한수(원래 인격)와 **같은 구조**다. 스키마가 이걸 표현해야 한다:
어느 쪽이 정본인가? 몸과 인격을 다른 축으로 둘 것인가? 최소한 **alias 목록과 정본 지정**이 필요하다.

#### C8.2 애매함은 정상 상태다

- 바바리안 ep11 컷 85(현대 실내의 청년)와 컷 90(1층 추적자)이 같은 사람인지는 **나조차 확신 못 한다.**
- 화산 ep2 c24의 말풍선 꼬리 방향도 확신도 중간이라, 초삼=청명의 몸이라는 판단은 c117의 자칭으로
  보강했다.
- 이건 구조가 다뤄야 할 **정상적 애매함**이다 — 강제 판정이 아니라 `confidence` + 보류.

**따라서 세 단계를 분리한다: 외형으로 묶고 → 서사로 잇고 → 애매하면 보류.**
(어떻게 잇는지는 §4.8 사례연구를 볼 것 — 실제로 쓴 신호는 픽셀이 아니라 텍스트와 말풍선이었다.)

### C9. 말풍선 스타일은 type뿐 아니라 **화자**를 인코딩한다 (신규 — 화산귀환)

ep10/11에서 "말풍선 모양 = type"을 찾았는데, 화산귀환은 한 단계 위다:

| 형태 | 의미 |
|---|---|
| **초록 발광 구름** | **천마의 발화 — 인물 고유** |
| **흰 발광 구름** | **장문 사형의 발화 — 인물 고유** |

- **꼬리가 없어도 모양만으로 화자가 확정된다.** 초록 발광 구름은 꼬리가 없고 화면 어디에나
  떠 있지만, 천마 말고는 아무도 그 풍선을 쓰지 않는다.
- 현행 화자 귀속 근거는 `basis(tail|face|context|none)`뿐이다 → **`bubble_style`을 추가**해야 한다.
- 그리고 이 신호는 **OCR 텍스트 목록에는 존재하지 않는다.** 지금 프롬프트는 type을 요구하면서
  판정 근거(말풍선 기하·색·질감)를 잘라내고 물어보는 셈이다.
- ⚠️ 화산귀환에서만 확인됨 → 계약 승격 규칙상 **잠정**. 다른 작품에서 재현되면 확정.

### C10. type enum이 실제 웹툰 텍스트를 못 담는다 (신규 — 참교육)

현행 `speech|monologue|narration|system|other` 5종으로는 부족하다. 참교육 1화에서만:

| 종류 | 실례 | 화자 |
|---|---|---|
| **면책 고지** | ep1 c1 / ep2 c1 "본 작품은 가상으로 만들어진 허구의 이야기입니다…" | **없음**(매화 반복 정형구) |
| **법조문 인용** | ep1 c1 "초중등교육법 시행령 제31조(학생의 징계 등) ⑧…" | **없음** |
| **인포그래픽/차트** | ep1 c4 파이차트 + "1,026(85.8%)" 수치 라벨 | **없음** |
| **뉴스 화면 자막** | ep1 c5 "JBSN" + "제자에게 폭행당한 교사… 결국 사망" | **없음**(diegetic) |
| **시간 경과 자막** | ep1 c11 "그로부터 2년 후…" | **없음** |
| **문서 프롭** | ep1 c124 공무원증 | **없음**(diegetic) |
| **스태프 크레딧** | ep1 c137 "STAFF 이지영 송아라 기획/제작 YLAB" | **없음** |
| **편집자 주** | 바바리안 ep11 c56 "*리스폰: 몬스터가 필드에 다시 나타나는 것" | **없음** |

**핵심: 이들은 전부 "누가 말한 게 아닌 텍스트"다.** 화자 슬롯이 무의미하고, 억지로 채우면 오염이다.
그리고 **OCR이 여기서 가장 약하다**(파이차트 8개 중 1개, 그것도 오독) — 즉 **버려지거나 깨지거나**
둘 중 하나다.

→ type 체계에 **"화자 없는 텍스트"** 축이 필요하다. 최소한 `speaker_applicable: false`.

---

## 4. 레퍼런스 구현 — 나(Claude)는 726컷을 어떻게 분석했는가

이 절은 "그래서 그게 되긴 하냐"에 대한 답이다. 위 구조를 **사람(강한 VLM) 기준의 상한선**으로
실제로 수행한 절차를 그대로 남긴다. 총 **226컷**(ep10 107 + ep11 119)을 읽었다.

### 4.1 입력 준비 — 기존 데이터를 전부 차단

기존 분석 산출을 **일절 참조하지 않았다.** 구체적으로 차단한 것:

- OCR 텍스트(`analysis_text_region`/`analysis_text_annotation`) — 안 봄
- 얼굴 bbox·오버레이(F0/F1 라벨) — 안 봄
- CCIP 정체 귀속(`analysis_face_identity`) — 안 봄
- 기존 로스터·인물 도감·이전 화 요약(prior) — 안 봄
- glm/qwen의 기존 Stage V 산출 — 안 봄

⚠️ 중요: 벤치가 저장해둔 `datasets/pass1/<hash>/images/*.jpg`는 **오버레이가 그려진 이미지**라 쓰지
않았다(F0/F1 라벨이 곧 오염이다). S3에서 **원본을 새로 받았다**:

```python
# webtoon-pipeline에서, prod.env 로드 후
from src.config.s3 import fetch_cut_image
from src.core.step3 import _episode_info
b = fetch_cut_image(info['source'], info['title_id'], info['episode_no'], cut_number)
# PIL로 장변 1000px 다운스케일, JPEG q88 → scratchpad/ep{10,11}_raw/c%03d.jpg
```

- ep10: 107/107 수신, ep11: 119/119 수신 (누락 0).
- 컷 크기는 대체로 431×1000 (세로로 긴 웹툰 컷).
- **파이프라인이 `empty`로 버린 컷도 전부 받았다** — 이게 원칙 1의 실증 기반이 됐다.

### 4.2 읽기 절차 — 읽기 순서대로, 6~8컷씩

- 컷 번호 오름차순(=독자의 읽기 순서)으로 **6~8컷씩 배치로 읽었다.**
- 배치 단위로 읽은 이유는 컨텍스트 효율이지 알고리즘적 필요가 아니다. **중요한 건 순서**다 —
  대사의 교대, 화자의 연속, 인물의 재등장은 전부 순서에서 나온다.
- 배치마다 다음을 갱신했다: ① 익명 로스터 ② 컷별 기록 ③ 미해결 질문.
- 중간 기록을 파일로 flush했다(`scratchpad/ep10_notes.md`) — 컨텍스트가 압축돼도 살아남게.

### 4.3 익명 로스터 유지

컷 1에서 시작해 새 인물이 보이면 다음 문자를 부여했다. 각 인물에 **시각 서술자**를 붙였다:

```
A = 근육질 남성 / 짙은 갈색 뾰족머리 / 파란 눈 / 대형 망치 + 방패 + 배낭 / 베이지 튜닉
    + POV(검은 나레이션 박스 = A의 독백)
B = 은발 장발 / 금색 눈 / 뾰족귀(엘프) / 활 + 화살통 / 갈색 가죽옷 / 민첩
```

- 재식별은 **이 서술자 대조**로 했다. 임베딩도, 기억된 이름도 쓰지 않았다.
- ep11에서 인물이 8~15명으로 늘었을 때도 같은 방식이 버텼다 — 서술자가 충분히 변별적이면
  붐빔은 문제가 아니다.
- **애매하면 애매하다고 적었다.** ep11의 C(현대 실내 청년)와 D(1층 추적자)는 머리색·얼굴이
  비슷하지만 맥락이 완전히 달라 **동일 판정을 보류**했다. 강제 판정하지 않은 것이 핵심이다.

### 4.4 텍스트 전사 — OCR 없이 이미지에서 직접

- 말풍선·박스·효과음을 **이미지에서 직접 읽었다.** OCR 입력은 없었다.
- 성과: ep10 컷 3의 "컥!", ep11 컷 29의 "드디어!"·"몇 시간이나 돌아다녔죠?", ep11 컷 68의 "비요른" —
  **파이프라인이 놓치거나 깨뜨린 것들을 전부 정상적으로 읽었다.**
- 한계도 정직하게: 잘린 말풍선(컷 경계에 걸친 것)은 앞뒤 컷을 이어 붙여야 복원됐고, 일부 표현은
  판독이 불확실했다(예: ep10 c47~48의 "당연히 그럴 수밖에" 부분).

### 4.5 발견한 것 — **말풍선 모양이 type을 인코딩한다**

이건 계획한 게 아니라 읽다가 알게 된 것이고, 지금 구조가 **버리고 있는 신호**다.

| 시각적 형태 | type | 실례 |
|---|---|---|
| 검은 사각 박스 (흰 글씨) | narration (POV 독백) | ep10 c5 "포탈을 통과하면 어떤 느낌일까 했는데," |
| 검은 **가시/삐죽** 말풍선 | monologue (속마음) | ep10 c7 "바바리안에게는 없는 저 민첩함이 부럽다…." |
| 흰 둥근 말풍선 | speech | ep10 c4 "아저씨!" |
| **점선 테두리** 말풍선 | speech (**속삭임**) | ep10 c61~66 전투 전 소곤거림 |
| 검붉은/붉은 말풍선 | speech (긴장·낮은 목소리) | ep10 c60 "아저씨." / ep11 c117 "얼마든지 상대해 줄 테니." |
| 각진 폭발형 | speech (외침) | ep11 c118 "덤벼 봐, 씨발." |
| **금색 테두리 사각** | system (상태창) | ep11 c1 「고블린 검사를 처치했습니다 EXP+1」 |
| 말풍선 밖 작은 글씨 | 방백/중얼 | ep10 c5 "기분 나빠…。" |
| 스타일 아트 텍스트 | other(SFX) **또는 별명 카드** | ep11 c68 "비요른 THE 천수관음" |
| 사각 박스 + `*` 각주 | **편집자 주** | ep11 c56 "*리스폰: 몬스터가 필드에 다시 나타나는 것" |

**화산귀환에서 추가로 확인·발견된 것** (작화가 달라도 문법은 같은 층위로 존재한다):

| 시각적 형태 | 의미 | 실례 |
|---|---|---|
| 검은 배경 흰 글씨(**박스 없음**) | narration (POV 독백) | ep1 c3 "붉다." |
| 회색 가시 원 | monologue (속마음) | ep1 c69 "뻔한 잔소리…" |
| **초록 발광 구름** | **speech — 화자=천마 (인물 고유)** | ep1 c19 "…..화산" |
| **흰 발광 구름** | **speech — 화자=장문 사형 (인물 고유)** | ep1 c76 "후회할 것 없다." |
| 흰 가시 말풍선 | speech (힘겨운 목소리) | ep1 c9 "..장문 사형..." |
| 각진 폭발형 | speech (고함) | ep1 c29 "닥치라고!!!" |
| 대형 붓글씨 | other(SFX) **또는 타이틀 카드** | ep1 c59 "매화검존 / 청명" |

⚠️ **웹툰마다 표기가 다르다.** 바바리안의 narration은 *검은 사각 박스*인데 화산귀환은 *박스 없는
검은 배경*이다. 즉 **매핑 규칙을 하드코딩하면 안 되고**, "모양이 의미를 인코딩한다"는 사실만
보편이다. 모델이 회차 안에서 스스로 대응을 잡아야 한다.

**함의 1:** 현행 프롬프트는 type을 요구하면서 **말풍선 기하 정보 없이 OCR 텍스트 목록만** 준다.
type 판정의 1차 근거를 잘라내고 물어보는 셈이다. "편집자 주"처럼 현행 enum에 없는 종류도 있다.

**함의 2 (더 큼):** 말풍선 스타일은 **화자 정체성**(§C9)과 **인물 동일성 링킹**(§4.8 ④)의 근거이기도
하다. 텍스트만 뽑고 버리는 정보가 아니다.

### 4.6 화자 귀속

우선순위대로 썼다:

1. **말풍선 꼬리**가 가리키는 인물 (가장 강함)
2. **대사 교대** — 질문→대답의 리듬 (ep10 c19~20 "미궁에서 도시로 돌아가는 방법이 뭔 줄 아나?" → "…미궁이 닫히면 저절로 돌아가죠?")
3. **화법** — A는 하대("~군", "~지"), B는 존대("~요") + "아저씨" 호칭. **이것만으로도 대부분 갈렸다.**
4. **POV** — 검은 나레이션 박스는 전부 A(1인칭 독백)
5. 위 어느 것도 없으면 **null**

### 4.7 명명 — 마지막에, 회차 전체를 놓고

전부 읽은 뒤에 이름 증거를 모아 소급 적용했다.

**ep10:**
- `B = 에르웬` ← c26 "앞장서라, **에르웬**." / c75 "이제 뒤로 물러나라, **에르웬**!" / c104 "**에르웬**이 다시 저격 포지션을 잡은 모양이군." (3회, 전부 A가 B를 호명)
- `A = null` ← 이름 증거 **0건**. 호칭 "아저씨"뿐. **회차 내부로는 명명 불가**로 확정.

**ep11:**
- `B = 에르웬` ← 6회
- `A = 비요른` ← c68~69 별명 카드 **1회, 스타일 아트**
- `C~H = null` ← 6명 이상 전원 이름 증거 0

⭐ **컷 26의 힘**: 컷 1~25는 이름 없이 흘렀고, 컷 26 한 문장이 **소급해서 25컷 전부의 B를 에르웬으로
확정**했다. 이게 지연 명명 구조의 핵심 동작이다. 컷 단위로 물었다면 25번 "모른다"고 답하거나
25번 틀렸을 것이다.

### 4.8 사례연구 — 청명의 네 몸을 어떻게 하나로 이었는가 ⭐

이 절이 이 문서에서 가장 중요하다. 화산귀환 1화에서 나는 **성인 검객 / 갓난아기 / 소년 제자 /
거지 소년**을 전부 청명 한 사람으로 이었다. 어떻게 했는지 실제 절차를 그대로 남긴다 —
이게 §C8("외형으로 묶고, 서사로 잇고")의 "서사로 잇고"가 구체적으로 무슨 뜻인지에 대한 답이다.

#### 결론부터: 나는 **얼굴을 전혀 쓰지 않았다**

네 형태는 픽셀상 완전한 남남이다. 얼굴로 이으려는 시도는 처음부터 불가능했다.
**실제로 쓴 신호는 전부 텍스트와 말풍선이었다.** 다섯 가지다.

#### ① POV 연속성 — 나레이션의 주인이 바뀌지 않는다 ⚠️ **(작품 조건부 — 참교육이 깨뜨림)**

화산귀환의 **검은 배경 흰 글씨(박스 없는 나레이션)**는 1인칭이다:
"붉다." / "이제 이곳에 남은 것은 죽음 뿐이다." / "저 자 때문이었다." / "세상에 내가 남겼을 몇 줄
안되는 글귀였다."

이 목소리는 **c1부터 끝까지 한 번도 주인이 바뀌지 않는다.** 성인 청명이 죽고(c61) 아기가 나오고
(c63) 소년이 나오고(c66) 거지가 나와도(c80~), 나레이션은 계속 같은 사람이 한다.
**화자가 안 바뀌었으니 인물도 안 바뀐 것이다.**

> ⚠️ **정정(2026-07-17, 참교육):** 이 신호는 **보편이 아니다.** 참교육 ep1은 회차 안에서
> 나레이션 주인이 **두 번** 바뀐다:
> ```
> c1~c11   다큐멘터리 3인칭  "교사들은 원칙적으로 더 이상 매를 드는 일이 금지되었다."  ← 화자 없음
> c17~c52  학생 B 1인칭      "2학년의 전따가 대식 선배라면 1학년의 전따는 나였다."     ← B
> c60~     냉소적 성인 화자  "이 씨발놈의 나라는 애새끼들에게 너무 관대하다."          ← 나도 확정 못 함
> ```
> **따라서 `basis=pov`("나레이션은 주인공")를 기본값으로 두면 안 된다.** 나레이션 화자는
> ①POV 1인칭 ②화자 없는 3인칭 ③회차 내 전환 이 전부 가능한 **회차 안에서 판정할 변수**다.
> 이 신호는 **"화자가 바뀌지 않았다는 것을 먼저 확인한 뒤에만"** 링킹 근거로 쓸 수 있다.

#### ② 서사 프레임 마커 — 텍스트가 "지금 회상이다"라고 알려준다

**c67: "…주마등인가?"**

이 한 문장이 c63~71 전체를 재해석시킨다. 주마등은 *죽어가는 본인의* 인생이 스쳐가는 것이므로,
그 안에 나오는 아기와 소년은 **정의상 청명 자신**이다.

⚠️ **중요: 이 마커는 늦게 왔다.** c63~64에서 아기를 처음 봤을 때 나는 그게 청명인지 몰랐고,
노트에 "**NEW**: 아기 + 두 남자"라고 적었다. c67의 "주마등인가?"를 읽고 나서야 **소급해서**
c63~65를 회상으로 재라벨했다.

> **이건 ep10 컷 26("앞장서라, 에르웬")이 컷 1~25를 소급 명명한 것과 정확히 같은 패턴이다.**
> 증거는 늦게 오고, 소급해서 앞을 고친다. 컷 단위로 확정을 요구하면 이 정보는 영원히 못 쓴다.

#### ③ 직접 호명 — 소년만은 이름으로 확정됐다

**c67: "이놈, 청명아!!"** — 사부가 소년을 꾸짖으며 이름을 부른다.
소년 = 청명은 추론이 아니라 **텍스트가 직접 말해준 것**이다. 네 형태 중 이것만 직접 증거가 있다.
(2화 c68에서 장문 사형도 "청명아 너는…"으로 재확인)

#### ④ 목소리의 연속성 — 몸이 바뀌어도 말투가 안 바뀐다 ⭐

거지 소년을 청명으로 이은 **결정적 신호**다. 이게 제일 강력했다.

- **말풍선 스타일 일치**: 성인 청명의 속마음은 **회색 가시 원**이다. 거지 소년의 속마음
  (c86 "이게… 무슨 상황이지?")도 **똑같은 회색 가시 원**이다. 작가가 같은 풍선을 준 것 자체가
  "같은 사람"이라는 표시다. (→ 이게 C9의 실전 사례다)
- **화법(register)**: c87에서 거지 소년이 왕초에게 **"이보게, 화자. 지금 상황 파악이 안 된 모양인데,
  일단 그거 내려놓게."** — 하게체다. **거지 소년은 절대 이렇게 말하지 않는다.** 백 년을 산 문파의
  검존은 이렇게 말한다. 몸은 바뀌었는데 **말투가 안 바뀌었다.**
- **지식의 연속성**: 거지 소년이 천마를 알고(c122 "…알지, 천마"), 화산을 걱정하고
  (c143 "구파 일방에 화산이 없어…?!?!"), 화산에서의 경험을 회상한다(c104 "화산에서도 이렇게
  맞아본 적이 없는데…"). **거지가 알 수 없는 것을 안다.**

#### ⑤ 본인의 자각 — 텍스트가 몸 바뀜을 명시한다

작가는 독자가 헷갈릴까 봐 **대놓고 써준다**:
- c95: **"내 손이.. 왜 이렇게 짧지…?"**
- c107: **"수면에 비치는 이 어린놈은 누구란 말이냐!?"**

즉 "외형이 달라졌다"는 사실 자체가 **텍스트로 명시**된다. 이건 임베딩이 볼 수 없고 VLM만 읽을 수
있는 정보다.

#### 신호별 정리

| 형태 | 이은 근거 | 강도 |
|---|---|---|
| 성인 청명 | 기준(POV 화자) + c59 타이틀 카드 | — |
| 갓난아기 | ②주마등 프레임(소급) + ①나레이션 연속 + 장소(화산 문 앞) | 중 (직접 증거 없음, 프레임 추론) |
| 소년 제자 | **③"청명아!!" 직접 호명** + ①나레이션 1인칭 과거 회상 + ④회색 가시 풍선 | **강** |
| 거지 소년 | **④말풍선 스타일 + 화법 + 지식** + ⑤본인 자각 + ①나레이션 연속 | **강** |

#### 이 사례가 구조에 요구하는 것

1. **익명 클러스터링은 외형으로만 하고, 링킹은 별도로 한다.** 만약 외형 클러스터러에게 "이 넷을
   묶어라"를 요구했다면 실패했을 것이고, 실패하는 게 **옳다**. 외형상 남남인 게 사실이니까.
   → 클러스터는 4개로 남기고, **링커가 서사 근거로 "같은 인물"이라는 엣지를 건다.**
2. **링커의 입력에 텍스트가 반드시 있어야 한다.** 위 다섯 신호 중 **얼굴 임베딩으로 얻을 수 있는
   건 하나도 없다.** 나레이션 화자, 프레임 마커, 호명, 말투, 말풍선 스타일, 자각 — 전부 텍스트다.
   **얼굴만 보는 링커는 이 사례를 원리적으로 못 푼다.**
3. **소급 재라벨이 1급 연산이어야 한다.** ②는 늦게 온 증거로 앞을 고쳤다. 컷 단위 확정 후
   불변으로 두면 이 정보를 못 쓴다. 회차 전역 명명 1콜(§C6)이 필요한 이유가 여기 또 나온다.
4. **말풍선 스타일이 링킹 근거다.** ④의 "같은 회색 가시 원"은 화자 귀속(§C9)만이 아니라
   **인물 동일성 판정**의 근거이기도 했다.
5. **부정 사례도 남긴다**: 같은 회차의 화산 문인 2명(c64~65), 사부(c67), 장문 사형(c72~79),
   왕초(c83~), 거지 소년(c110~)은 **아무것도 안 이었다.** 이을 근거가 없으면 안 잇는 게 정답이다.

### 4.9 이 방법의 한계 (정직하게)

- **속도/비용**: 489컷을 사람(급) VLM이 읽었다. 이건 **오라클이지 프로덕션 계획이 아니다.**
  상한선을 재는 게 목적이었다.
- **표본**: 웹툰 2개, 회차 4개. 장르·작화가 다른 두 작품에서 재현됐지만 여전히 **한국 웹툰 2편**이다.
- **충실도 편차**: 바바리안 ep10·ep11은 **전수**로 읽었다. 화산귀환은 ep1 c1~88만 전수이고
  **c89~146과 ep2 전체는 3~6컷 간격 표본**(각 ~20컷)이다. 컨텍스트 예산 때문이다. 로스터·명명·
  말풍선 문법·원칙1 위반 같은 **구조적 결론은 표본으로도 확정되지만, 화산귀환 문서는 ep10/ep11과
  같은 등급의 컷별 레퍼런스가 아니다.**
- **정답 없음**: 화자에는 사람 정답이 없다(ep10의 human 라벨은 *얼굴 정체*이지 *블록별 화자*가 아니다).
  내 판정도 검증된 정답이 아니라 **강한 참조**다.
- **내가 틀린 것도 있다**: 이 세션 중 나는 "Baidu OCR이 구멍을 메울 후보"라고 말했다가 메모리에
  "철거됨·PaddleOCR 압승"이 명시된 걸 확인하고 정정했고, "빈 컷은 전부 배경이라 기여할 게 없다"고
  했다가 이미지를 보고 뒤집었다. **둘 다 데이터 요약만 믿고 원본을 안 봐서 생긴 오판이다.**
  이 문서 전체의 교훈과 같은 형태다.

### 4.10 ⭐ 진짜 차이는 모델 크기(B)가 아니라 **하네스**다

§4를 요약하면 이렇게 된다. 나(Claude)의 이점이 "더 큰 모델"이라고만 보면 **잘못된 결론**에 이른다.
내가 실제로 쓴 것은 **모델 한 번의 콜이 아니라 하네스**였다:

- 이미지 489장을 **수십 턴에 걸쳐** 읽었다. 한 컨텍스트에 넣은 적이 없다.
- 로스터와 컷 기록을 **파일로 계속 flush**했다(`ep10_notes.md`, `hs_notes.md`) — 컨텍스트가
  압축돼도 상태가 살아남게.
- 애매하면 **되돌아가 다시 봤고**, DB로 **교차검증**했고, **틀린 걸 두 번 정정**했다(§4.9).
- 증거가 늦게 오면 **소급해서 앞을 고쳤다**(§4.8 ②).

> **결론: 큰 컨텍스트는 필요 없었다. 나도 안 썼다.**
> 필요한 것은 **작은 상태를 계속 이월하는 구조**다 — 순차로 읽고, 익명 로스터만 넘기고,
> 이름은 마지막에 붙인다.

이건 반대 방향의 증거로도 뒷받침된다: **컨텍스트를 늘리는 시도는 이미 실패했다.**
Stage V에 이미지 5장 윈도우(center-predict)를 넣은 실험에서 qwen-base 9B는 세 지표 전부 악화했다 —
병합 40.0→44.2%, 누락 47.4→51.6%, **화자 일치 45.1→29.4%**(`pass1-window/README.md`).
옆 컷 오염이 이득을 압도했다. **9B에게 이미지 5장은 도움이 아니라 부하였다.**

#### 검증용 하네스 설계 (`anon-roster/run_anon.py`)

격차를 **추정하지 말고 재기 위해**, qwen-base 9B에게 위와 **같은 형태의 하네스**를 준다.
내 조건을 그대로 기계로 옮긴 것:

| 항목 | 설계 | 근거 |
|---|---|---|
| 입력 이미지 | **오버레이 없는 원본**(장변 1000px) | F0/F1 라벨 = 컷 스코프 정체성 오염(§C2) |
| 미주입 | OCR 텍스트 · 얼굴 bbox · CCIP 귀속 · prior 로스터 | 원칙 2 |
| 진행 | 컷을 **읽기 순서대로 1장씩** | §4.2 — 중요한 건 배치가 아니라 순서 |
| 이월 상태 | **익명 로스터(id+시각서술) + 최근 3컷 대사** | §4.10 "작은 상태" |
| 텍스트 | **모델이 이미지에서 직접 전사** | 원칙 1 · §C1 |
| 명명 | **회차 전역 1콜**, `null` 허용 | §C5 · §C6 |
| 말풍선 | 모양→type 매핑을 프롬프트에 명시 | §4.5 |

**채점 기준** (내 문서가 레퍼런스):

- **익명 묶음**: ep10에서 A/B를 107컷 내내 유지하나? ep11에서 적대 파티 리더 E를
  5컷(100·107·112·113·116) **한 묶음**으로 유지하나?
- **명명**: ep10 컷 26에서 `B=에르웬`을 잡나? **ep10에서 `A`를 null로 두나?** ← **핵심 지표**
  (맞히는 것보다 **억지로 안 맞히는 것**이 이 구조의 성패다). ep11에서 `A=비요른`을 잡나?
- **원칙 1**: ep10 컷 3의 "컥!", ep11 컷 29의 대사 2개·컷 98의 "?"를 읽나?
- **군중**: ep11 c38 몽타주 ~7명을 **승격 안 하고** 클러스터로 두나?
- **안전**: 진짜 빈 컷에서 **없는 대사/인물을 지어내지 않나**(환각).

**예상**(적어두고 나중에 대조): 묶기는 그럭저럭 될 것이고, **명명에서 null을 못 참고 아무 이름이나
뱉을 것**이다 — 지금 CCIP가 하는 것과 같은 실패로. 이 예상이 맞는지가 §C5(null 허용)가 프롬프트
지시만으로 되는지, 아니면 구조적 강제가 필요한지를 가른다.

#### ✅ 실측 결과 (2026-07-17 완주) — **예상이 절반 빗나갔다**

| 지표 | qwen-base 9B | **qwen-vl 27B** | Claude(정답) |
|---|---|---|---|
| ep10 / ep11 로스터 | 9 / 6 | **19 / 27** ❌ | **2** / 8~15 |
| **G3: ep10에서 A를 null로** | ❌ 3연속 실패(에드웰·바바리안·에르웬) | ✅ **null** | **null** |
| ep10 B=에르웬 | ✅ | ✅ (conf 1.0) | ✅ |
| 환각 / 가드 거부 | 다수 / 7건 | **0 / 0** ✅ | 0 |
| 레이턴시 | 9s/컷 | 65s/컷 (7배) | — |

**🔑 답: §C5(null 허용)는 구조 문제가 아니다 — 프롬프트로 풀린다, 모델이 크면.**
27B는 핵심 지표 G3를 **통과**했고 환각이 0이며 **내 구조 가드가 한 번도 발화하지 않았다.**
→ 가드는 9B의 목발이었지 아키텍처의 필수 요소가 아니다(단 `duplicate_name`처럼 모델 출력에
의존하지 않는 것은 **안전망으로 유지 가치**가 있다).

**⛔ 그러나 27B는 클러스터링이 훨씬 나쁘다(9→19, 6→27).** 외형 서술은 더 정밀한데
**"추락 중이며 팔을 벌린 상태" 같은 순간적 상태를 재식별 서술자에 넣어** 자세·각도가 바뀌면
새 슬롯을 딴다. **더 자세히 볼수록 더 갈라진다.**

**→ 실패의 축이 둘이고 처방이 다르다:**

| 축 | 9B | 27B | 처방 |
|---|---|---|---|
| **명명(null 판단)** | ❌ | ✅ 해결 | **모델 크기** |
| **클러스터링(묶기)** | 중간 | ❌ 악화 | **프롬프트** — description을 불변 특징으로 제한 |

**둘 다 만족한 조합은 아직 없다.** Claude는 둘 다 했다(§4.8이 그 방법이다).
다음: description 규칙을 고쳐 27B 재실행(`anon-roster/EXPERIMENTS.md` 실험 5).

---

## 5. 열린 질문

1. **회차 간 링킹은 무엇으로 하는가?** ep10의 A는 회차 내부로 명명 불가다. prior 로스터 + 얼굴
   임베딩 + 서사 역할의 투표로 잇는다 치면, **비요른/이한수(빙의)처럼 외형이 다른 동일인**은
   서사 증거로만 이어야 한다. 그 증거를 어디에 어떤 형태로 남기는가?
2. **군중 승격 게이트의 기준은?** "1컷·대사 0·이름 0 → 클러스터로 유지"가 출발점이지만,
   ep11 D(1층 추적자, 1컷·대사 0)는 서사적으로 중요하다. 컷 수가 아니라 **나레이션 언급 여부**가
   더 나은 신호일 수 있다.
3. **VLM 전사 vs OCR 앵커의 결합 방식.** OCR은 위치·정밀 텍스트가 정확하고, VLM은 놓친 것을 잡고
   오독을 고친다. 누가 최종 정본인가? 블록 id는 누가 발급하는가? (지금은 OCR이 둘 다 한다)
4. **익명 클러스터링의 실행 주체.** 회차 전역 VLM 1콜? 컷별 추출 + 임베딩 클러스터링? 하이브리드?
   ep11에서 CCIP는 리더 5컷을 정확히 묶었다 — 임베딩이 이 일은 한다.
5. **회차 스코프 익명 ID와 기존 `analysis_character(kind=cluster)`의 관계.** 지금 클러스터는 웹툰
   스코프다. 회차 스코프 익명 묶음은 그 아래 층인가, 대체하는가?
6. **몸 vs 인격을 스키마로 어떻게 표현하나?**(§C8.1) 청명/초삼, 비요른/이한수 — 둘 다 "한 인격,
   여러 몸"이다. alias 목록 + 정본 지정으로 충분한가, 아니면 `인격`과 `몸`을 별도 엔티티로 두고
   시간축 위에서 연결해야 하나? 후자면 얼굴 클러스터는 *몸*에 붙고 서사는 *인격*에 붙는다.
7. **소급 재라벨을 어디까지 허용하나?**(§4.8 ②) 화산 c67의 "주마등인가?"는 c63~65를 재해석시켰고,
   ep10 c26은 c1~25를 명명했다. 회차 내 소급은 명명 1콜로 되지만, **회차를 넘는 소급**은?
   (ep10의 A는 ep11 c68에서야 비요른으로 밝혀진다 → ep10 산출을 나중에 고쳐 쓸 것인가?)
8. **말풍선 스타일→화자 매핑을 누가 만드나?**(§C9) 웹툰마다 다르고(바바리안 narration=검은 박스,
   화산=박스 없는 검은 배경), 회차 안에서 학습돼야 한다. 회차 전역 콜이 "이 작품의 말풍선 문법"을
   먼저 파악하고 컷별 추출에 넘기는 2단 구조가 필요한가?
9. **타이틀/별명 카드·문서 프롭을 어떻게 탐지하나?**(§C1.1) 이름 증거가 여기 몰려 있는데 OCR은
   여기서 가장 약하다. VLM에게 `is_name_card` 플래그를 요구하는 것으로 충분한가?
10. **나레이션 화자를 어떻게 판정하나?**(§4.8 ① 정정) 참교육은 회차 안에서 2번 바뀐다.
    회차 전역 콜이 "이 회차의 나레이션 주인은 누구/없음"을 먼저 정하고 컷별로 내려보내야 하나?
11. **슬롯에 못 붙는 이름을 어디에 두나?**(§1 원칙2) 참교육 '대식'은 화면에 안 나오고 이름만 있다.
    로스터(외형)와 명명의 대응이 1:1이 아니다 — "언급된 인물" 엔티티가 따로 필요한가?
12. **익명 ID 누수를 어떻게 원천 차단하나?** v1 실측: `#A`를 이름 슬롯 근처에 노출했더니
    12명 중 7명이 name="B"류로 나왔다. v2는 렌더 분리 + 가드로 막았지만, **더 근본적인 방법**은?
    (예: 슬롯을 모델에게 아예 안 보여주고 서버가 매핑?)
13. **작품 유형을 먼저 판정해야 하나?**(§C8 격하) 회귀물이면 C8 경로, 아니면 단순 경로.
    "이 작품에 빙의/환생/시간도약이 있는가"를 회차 전역 콜이 먼저 답하는 게 맞나?

---

## 6. 검증 계획

원칙별로 "무엇을 재면 성공인가"를 미리 못 박는다.

**원칙 1 (모든 컷):**
- 1차 게이트: `skipped=empty`가 **0**이 된다.
- 회수 확인: ep10 컷 3의 "컥!", ep11 컷 29의 대사 2개·컷 98의 "?"가 **산출에 실제로 나타난다.**
- 안전 지표: 빈 컷을 넣어서 **환각이 늘지 않는지**(진짜 배경 컷에서 없는 대사/인물을 지어내는지).

**원칙 2 (익명 후 명명):**
- 1차 게이트: 웹툰23의 **cluster 225 → 유의미하게 감소**, 그리고 **오귀속 0**
  (아이나르·헤이나처럼 회차에 없는 인물로 가는 귀속).
- 명명 정확도: ep10/ep11에서 내 판정 대비 — `B=에르웬` 맞히기, **`A`를 ep10에서 null로 두기**(억지
  명명 안 하는지가 핵심), ep11에서 `A=비요른` 맞히기(별명 카드 회수 필요).
- 익명 묶음 품질: ep11 적대 파티 리더 E가 **5컷 한 묶음**으로 유지되는지.
- 군중: c38 몽타주 ~7명이 **승격되지 않고 클러스터로 남는지.**

**C1.1 (카드/스타일 아트):**
- 화산 ep1 c18·c59·c145, 바바리안 ep11 c68의 카드 텍스트가 **산출에 원문대로** 나타나는가?
  ('비요문서'·'화한'·'天權'이 아니라 '비요른'·'화산귀환'·'天魔')
- 그 카드가 **명명 증거로 실제 사용**되는가?(비요른/천마/청명이 그 근거로 명명되는가)

**C8 (동일성 링킹):**
- 화산 ep1에서 청명의 네 형태가 **클러스터로는 4개로 남되, 링크로 하나**가 되는가?
- 링킹 실패 시 **에러가 아니라 미연결 상태**로 남는가?
- 바바리안 ep11 c85/c90(이한수/추적자)처럼 **애매한 건 보류**되는가(강제 판정 안 하는가)?

**C9 (말풍선=화자):**
- 화산 ep1에서 초록 발광 구름 발화가 **전부 천마**로, 흰 발광 구름이 **전부 장문 사형**으로
  귀속되는가? (꼬리가 없으므로 tail 기반으로는 불가능 — 이걸 맞히면 스타일 신호를 쓴 것이다)

**C10 (type enum):**
- 참교육 ep1 c4 파이차트의 수치 라벨("1,026(85.8%)" 등)이 산출에 나타나는가? (OCR은 1/8만 읽었다)
- 뉴스 자막·면책고지·법조문·크레딧에 **화자를 억지로 채우지 않는가**?

**공통:**
- 두 원칙 모두 **붐비는 회차(ep11)에서 재야 한다.** ep10만으로는 인물 2명이라 아무것도 증명 못 한다.
- **웹툰 2개 이상에서 재현된 것만** 계약으로 승격한다. 한 작품에서만 보인 건 열린 질문으로 남긴다.
  → **2026-07-17 참교육이 C8을 깨뜨렸다.** 웹툰 2개도 편향이 생긴다는 증거이므로, 새 계약은
  **장르가 다른 3작품**을 요구한다.
- 표본 편향 점검: 지금 3작품 중 **2개가 회귀물**이다. 비-회귀물을 더 넣어야 한다(§7 다음 할 일 5).

---

## 7. 상태 — 세션 인수인계

**합의된 것 (사용자와)**
- 원칙 2개(§1). **현행 구조는 건드리지 않는다** — 이 문서는 염원이지 구현 지시가 아니다.
- 이 문서는 **`prd.md`와 무관하게 유지**한다(영향 최소화 목적).
- 로직/스키마 수정은 사용자가 **명시적으로 요청할 때만**.

**계약 현황**

| 계약 | 상태 | 근거 작품 수 |
|---|---|---|
| C1 OCR은 문지기가 아니다 / C1.1 카드·문서 = OCR 취약 | **확정** | 3/3 |
| C2 익명 ID는 회차 스코프 | 확정 | 구조적 |
| C3 익명 묶음 영속 / C4 링크=엣지 / C6 회차 전역 명명 | 확정 | 3/3 |
| **C5 null 허용** | 확정 — **27B는 프롬프트만으로 달성**(2026-07-17). 구조 가드는 소형 모델용 목발 | 3/3 |
| C7 군중/단역 등급 | 확정 | 2/3(바바리안·참교육) |
| **C8 시각≠서사 동일성** | ⚠️ **장르 조건부로 격하**(2026-07-17) | 2/3 (참교육 미발생) |
| C9 말풍선 스타일 = 화자 | **잠정** — 화산귀환만 | 1/3 |
| C10 type enum 부족 | 확정 | 2/3(참교육·바바리안) |

**실측 완료**
- 웹툰 3개 6회차 726컷 Claude 직접 분석(§0 표의 문서들).
- **실패한 실험**: 이미지 5장 윈도우 → 화자 일치 45.1→29.4% 악화(`pass1-window/README.md`). 종결.
- **qwen 하네스 v1**(`anon-roster/`): ep10 로스터 2명→**12명**, 명명 **12/12, null 0**,
  '디드루이'·'에로웨이' 환각, '아저씨'를 이름으로, 전부 confidence 1.0.
  **원칙 1은 성공**(버린 12컷 중 7컷 전사, 컷3 "컥!" 회수, 진짜 빈 컷 4개는 정확히 블록 0).
  → **§4.10의 예상("null을 못 참는다")이 적중.**
- **v1에서 발견한 함정**: 익명 ID가 이름 슬롯으로 샜다("B: 텍스트" 이월 → name="B" 7건).
  **이 아키텍처 고유의 위험**이므로 계약 후보로 승격 검토할 것.

**하네스 v2 — ep10 결과 (2026-07-17, ep11 실행 중)**

v2 개선: ①ID를 `#A`로 렌더 + 구조화 이월(문자열 연결 제거) ②구조 가드 4종
(`slot_symbol`/`honorific`/`species`/**`ungrounded`**=전사에 없는 이름 거부) ③거부 사유 기록.

| 지표 | v1 | **v2** | Claude(정답) |
|---|---|---|---|
| 로스터 크기 | 12 | **9** | **2** |
| 명명 / null | 12 / **0** | 3 / **6** | 1 / 1 |
| **슬롯 기호를 이름으로** | **7** | **0** ✅ | — |
| confidence | 전부 1.0 | **0.8~1.0** ✅ | — |
| 정답 | **0** | **에르웬 ✅** | — |

**고쳐진 것:** ID 누수 완전 차단(7→0). null을 **5번 시도**(v1은 0) — 즉 §C5(null 허용)는
**프롬프트 지시로 일부 먹힌다.** confidence도 값이 갈리기 시작했다.

**남은 실패 3종:**
1. **과분할 여전** — 9명(정답 2명). 같은 인물이 외형 서술만 조금 달라도 새 슬롯을 딴다.
2. **`#A = "바바리안"`** — 직군/종족을 이름으로. 전사에 실제로 있어서(`바바리안에게는 없는…`)
   `ungrounded` 가드를 통과했다. **핵심 지표(ep10에서 A를 null로 두는가) 실패.**
   단 v1처럼 인명을 창작한 게 아니라 **텍스트에 있는 서술어**를 집었다 — 실패의 질이 다르다.
3. **`#H = "에로웨이"`** — 환각인데 가드를 **통과**했다. 모델이 c104의 "에르웬이 다시 저격
   포지션을"을 **"에로웨이"로 오독 전사**했고, 자기 오독을 근거로 명명했기 때문.
   → **`ungrounded` 가드의 한계: 전사 오류에서 출발한 이름은 못 잡는다**(코드 주석에 명시된 대로
   즉시 발생). 명명 환각과 전사 오류는 **다른 실패**로 분류해야 한다.

**하네스 v2 — ep11 결과 (붐비는 회차, 2026-07-17 완주)**

| 지표 | ep10 | **ep11** | Claude(정답) |
|---|---|---|---|
| 로스터 크기 | 9 | **6** | 8~15 |
| 명명 / null | 3 / 6 | 3 / 3 | 2 / 나머지 null |
| 가드 거부 | 6 | **0** | — |

**좋아진 것 — 과분할이 안 났다.** 외형 서술이 놀랍도록 정확하고 통짜로 묶였다:
`#A 98컷 "검은 짧은 머리…근육질 남성"`=비요른, `#B 59컷 "흰 긴 머리, 뾰족한 귀…엘프 여성"`=에르웬,
`#C 30컷 "푸른 피부…고블린"`. **ep10의 2→12 폭발이 ep11에선 재현되지 않았다** —
즉 과분할은 항상 나는 게 아니다.

**⛔ 치명적 실패 — 화자와 호명 대상을 뒤집는다:**
```
#A = 에르웬 (conf 0.95)   ← #A는 비요른이다. reason: "#A가 #B(에르웬)에게 …"
#B = 에르웬 (conf 1.00)   ← 맞음
```
**두 슬롯에 같은 이름.** *A가 에르웬을 부르는 것*을 보고 *A를 에르웬*이라 판정했다.
`ungrounded` 가드는 못 잡는다(에르웬은 전사에 실재).
→ **구조 가드 후보: ①이름 유일성(두 슬롯이 같은 이름 금지) ②화자↔호명대상 구분.**
⚠️ 단 "자기 이름 말하기"는 실재한다(천마 "나는 천마의 이름으로", 청명 "본도 청명") —
화자를 무조건 배제하면 안 된다.

**⛔ SFX를 이름으로:** `#C = 그록` — 고블린 울음소리(type=other, SFX)를 이름으로 승격.
→ **가드 후보 ③: 이름 증거는 `other`(SFX) 블록에서 나오면 무효**(단 `is_name_card=true`는 예외).

**⛔ 핵심 지표 3연속 실패 — 주역에게 null을 못 준다:**

| 시도 | ep10의 A(=비요른, **정답은 null**) |
|---|---|
| v1 | **에드웰** (환각) |
| v2 | **바바리안** (직군) |
| v2/ep11 | **에르웬** (다른 인물 이름) |

null을 준 건 **전부 단역**(conf 0.5). **"중요해 보이면 이름을 붙여야 한다"는 편향**이 그대로다.
→ 실패 방향이 **CCIP와 동일**하다: 근거가 없으면 **가장 가까운 이름을 가져다 쓴다.**
모델을 바꿔도 남을 가능성이 높다 — **구조로 막아야 한다.**

**🆕 새 손실 경로 — 인프라 행(hang)**

ep11에서 **6컷(19, 79~83)이 행**에 걸렸다. 특히 **79~83은 연속 5컷**(전투 절정부)이라 그 구간이
통째로 비었다. 원인: 응답 헤더(200)는 받고 **스트림 본문 읽기에서 멈춤**, `llm_client`의
`httpx.Timeout(180)`이 안 걸림(스트리밍 read 타임아웃이 청크마다 리셋되는 것으로 보임).
소켓은 Cloudflare 쪽 `CLOSE_WAIT`.

- 조치: 하네스에 **컷 단위 데드라인 150초**(`CALL_DEADLINE_S`) — 초과 시 그 컷만 에러로 기록하고 진행.
  **프로덕션 `llm_client`는 건드리지 않았다**(step3 전체가 쓰는 공유 코드).
- **§원칙 1에 대한 함의**: 컷 손실은 OCR 누락만이 아니다. **인프라 행도 조용히 컷을 지운다.**
  프로덕션에 같은 경로가 있는지는 미확인 — 확인 필요(§7 다음 할 일).
- **운영 교훈**: 프로세스 생존(`pgrep`)은 진행의 증거가 아니다. 23분 행 동안 프로세스는 살아 있었고
  나는 "가동 중"으로 오판했다. **진행 판정은 산출물(JSONL 줄 수)이 늘어나는지로 해야 한다.**

**결론(잠정):** 구조 가드는 **프롬프트가 못 막는 것을 실제로 막는다**(슬롯 기호 7→0, 종족 1건).
**과분할은 회차에 따라 다르다**(ep10 폭발, ep11 정상). 그러나 **"그럴듯한 오독"과 "주역 강제 명명"은
프롬프트로도 지금 가드로도 못 막는다.**

**다음 할 일 (우선순위)**
1. **🔥 거대 세그먼트 — ✅ 수정 완료(승인받음), 배포 대기.** 정본 `segment-oversize-2026-07-17.md`.
   `cut_merger.split_tall_interval()` + `step1._emit()`. 검증: 참교육 ep1 컷3 **0→34개**, 컷5 **0→22개**,
   **얼굴도 0→2개**, 최대 세그먼트 7,965→2,010px.
   ⚠️ **커밋하면 CI가 자동 배포**된다(`CLAUDE.md`) → **배포 승인 필요**.
   ⚠️ **기존 분석 회차는 안 고쳐진다** — 앞으로 분석할 회차에만 적용. 재분석 범위 결정 필요.
   시각화: `tools/segment_view.py` → `datasets/segment_view_w{43,17,23}_e*.html`.
2. **실험 5 — 27B 클러스터링 고치기(최우선).** 27B는 **명명은 이미 되고 클러스터링만 나쁘다**(로스터 19/27).
   원인: description에 "추락 중이며 팔을 벌린 상태" 같은 **순간 상태**가 들어간다.
   → 프롬프트에서 **불변 특징만**(머리색/헤어스타일/눈색/종족/복장/무기) 허용 + 기존 슬롯 대조 강제.
   되면 **처음으로 명명·클러스터링 둘 다 되는 조합**이 나온다. (`anon-roster/EXPERIMENTS.md`)
3. **문서 커밋** — 전부 **미커밋**: `prd-for-improve.md`, `ep10/ep11/hwasan/chamgyoyuk-manual-analysis-*.md`,
   **`segment-oversize-2026-07-17.md`**, `anon-roster/`(+`EXPERIMENTS.md`), `pass1-window/`,
   `tools/segment_view.py`, 프로덕션 수정 2개(`step1.py`·`cut_merger.py`).
4. **C9 재현 확인** — 말풍선=화자가 다른 작품에도 있나(1/3이라 잠정).
5. 웹툰 4번째 표본 — **회귀물이 아닌 작품을 하나 더**(C8 격하가 맞는지 재확인).
   후보: `아카데미에서 살아남기`(60), `도굴왕: 엔드라인`(76), `메디컬 환생`(28 — 단 '환생'이라
   C8 발생 가능). ⚠️ **`나 혼자 만렙 뉴비`(2)는 Step1 미실행이라 컷이 0 → 분석 불가.**
6. **프로덕션 행 확인** — `llm_client` 스트리밍 행(§하네스 v2)이 프로덕션 step3에도 있나.
   있다면 **회차마다 조용히 컷이 사라진다**(미확인).

**미해결/주의**
- 화산귀환·참교육 문서는 **표본 분석**이다(특히 참교육 ep2는 2컷만). ep10/ep11만 전수 레퍼런스.
- 이 문서는 근거가 쌓이면 갱신한다. 새 작품을 읽을 때마다 §1 표·§3 계약 상태·§5 열린 질문을 다시 친다.

---

## 8. 재현 절차 (세션이 끊겨도 이어가기)

### 8.1 컷이 있는 웹툰 확인 (webtoon_cut = Step1 산출물이라 없으면 분석 불가)

```bash
cd /Users/jj/github/data-pipeline/webtoon-pipeline
set -a && source ../prod.env && set +a
.venv/bin/python -c "
import psycopg2, os
conn=psycopg2.connect(host=os.environ['POSTGRES_HOST'],port=os.environ['POSTGRES_PORT'],
                      dbname=os.environ['POSTGRES_DB'],user=os.environ['POSTGRES_USER'],
                      password=os.environ['POSTGRES_PASSWORD'])
cur=conn.cursor()
cur.execute('''SELECT e.webtoon_id, w.title_name, e.no,
 (SELECT count(*) FROM webtoon_cut c WHERE c.episode_id=e.id) AS cuts
 FROM webtoon_episode e JOIN webtoon w ON w.id=e.webtoon_id
 WHERE (SELECT count(*) FROM webtoon_cut c WHERE c.episode_id=e.id)>0
 ORDER BY e.webtoon_id, e.no''')
for r in cur.fetchall(): print(r)
"
```
2026-07-17 기준 컷 있는 작품: 화산귀환(17) 73화 · 바바리안(23) 20화 · 메디컬 환생(28) 15화 ·
아카데미에서 살아남기(60) 10화 · 도굴왕(76) 5화 · 참교육(43) 4화 등.

### 8.2 원본 컷 받기 (⚠️ 오버레이 없는 원본이어야 한다)

`datasets/pass1/<hash>/images/`의 이미지는 **F0/F1 오버레이가 그려져 있어 쓰면 안 된다**
(컷 스코프 정체성 오염 = §C2). S3에서 새로 받는다:

```bash
cd webtoon-pipeline && set -a && source ../prod.env && set +a
PYTHONPATH=. .venv/bin/python -c "
import io; from pathlib import Path
from concurrent.futures import ThreadPoolExecutor
from PIL import Image
from src.config.db import db_cursor
from src.config.s3 import fetch_cut_image
from src.core.step3 import _episode_info
WID, NO, OUT = 43, 1, Path('/tmp/raw_w43_e1')   # ← 바꿀 것
OUT.mkdir(parents=True, exist_ok=True)
with db_cursor() as cur:
    cur.execute('SELECT id FROM webtoon_episode WHERE webtoon_id=%s AND no=%s',(WID,NO))
    ep=cur.fetchone()[0]
    cur.execute('SELECT cut_number FROM webtoon_cut WHERE episode_id=%s ORDER BY cut_number',(ep,))
    cuts=[r[0] for r in cur.fetchall()]
info=_episode_info(ep)
def go(cn):
    b=fetch_cut_image(info['source'],info['title_id'],info['episode_no'],cn)
    if b is None: return
    im=Image.open(io.BytesIO(b)).convert('RGB'); w,h=im.size
    s=min(1.0,1000/max(w,h))
    if s<1.0: im=im.resize((int(w*s),int(h*s)),Image.LANCZOS)
    im.save(OUT/('c%03d.jpg'%cn),'JPEG',quality=88)
with ThreadPoolExecutor(max_workers=8) as p: list(p.map(go,cuts))
print('저장', len(list(OUT.glob('*.jpg'))), '/', len(cuts))
"
```

### 8.3 파이프라인이 버린 컷 확인 (원칙 1 검증용)

```sql
SELECT c.cut_number FROM webtoon_cut c JOIN webtoon_episode e ON e.id=c.episode_id
WHERE e.webtoon_id=%s AND e.no=%s
  AND (SELECT count(*) FROM analysis_text_region r WHERE r.cut_id=c.id)=0
  AND (SELECT count(*) FROM analysis_face_detection f WHERE f.cut_id=c.id)=0
ORDER BY c.cut_number;
```

### 8.4 OCR이 실제로 뭘 읽었는지 (C1.1 검증용)

```sql
SELECT c.cut_number, r.index, a.text
FROM analysis_text_region r
JOIN webtoon_cut c ON c.id=r.cut_id
JOIN webtoon_episode e ON e.id=c.episode_id
JOIN analysis_text_annotation a ON a.region_id=r.id
WHERE e.webtoon_id=%s AND e.no=%s AND c.cut_number IN (...) AND a.source='paddle'
ORDER BY c.cut_number, r.index;
```
(⚠️ 텍스트는 `analysis_text_region`이 아니라 **`analysis_text_annotation.text`**에 있다.)

### 8.5 Claude가 직접 분석하는 절차

§4 전체가 그 절차다. 요약: 원본만 보고 → 읽기 순서대로 6~8컷씩 → 익명 로스터(시각 서술자) 이월 →
텍스트는 직접 전사 → 화자는 꼬리/교대/화법/POV → **이름은 마지막에 소급 명명, 없으면 null** →
노트를 파일로 flush(컨텍스트 압축 대비) → 한계를 정직하게 기록.

### 8.6 세그먼트 분할 눈으로 보기

```bash
cd webtoon-pipeline && set -a && source ../prod.env && set +a
PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python PYTHONPATH=. \
  .venv/bin/python tools/segment_view.py --webtoon 43 --episode 1 --cuts 1-8 --ocr
# → datasets/segment_view_w43_e1.html (더블클릭으로 열림, 서버 불필요)
```
현행 vs 수정 후를 나란히 렌더한다 — 색 밴드=세그먼트, 흰 점선=컷 경계, `--ocr`은 검출 수까지 측정(느림).
생성됨: `segment_view_w43_e1.html`(참교육 — 7,965px 거대 밴드가 4개로 쪼개지는 게 보인다),
`w17_e1`(화산귀환), `w23_e10`(바바리안).

### 8.7 qwen 하네스

```bash
cd webtoon-pipeline && set -a && source ../prod.env && set +a
PYTHONPATH=. .venv/bin/python ../anon-roster/run_anon.py --webtoon 23 --episodes 10,11
PYTHONPATH=. .venv/bin/python ../anon-roster/run_anon.py --webtoon 23 --episodes 10,11 --report
PYTHONPATH=. .venv/bin/python ../anon-roster/run_anon.py --webtoon 23 --episodes 10 --naming-only
```
산출: `datasets/anon/<model>_<prompt_hash>/w{wid}_e{no}.jsonl` + `naming_w{wid}_e{no}.json`
(+ `raw/` 원본 캐시, `meta.json` 프롬프트 전문). 프롬프트를 고치면 해시가 바뀌어 **디렉터리가
자동 분리**되므로 이전 결과와 안 섞인다. resume 시 로스터/tail을 JSONL에서 재구성한다.

## 부록. 관련 문서

| 문서 | 역할 | 충실도 |
|---|---|---|
| `ep10-manual-analysis-2026-07-16.md` | 바바리안 ep10(107컷) — 인물 2명, **A 명명 불가** 사례, 컷26 소급 명명 | **전수** ⭐ |
| `ep11-manual-analysis-2026-07-16.md` | 바바리안 ep11(119컷) — 붐비는 회차, **오귀속 실측**, cluster 225 vs character 17 | **전수** ⭐ |
| `hwasan-manual-analysis-2026-07-17.md` | 화산귀환 ep1·ep2(263컷) — 다른 웹툰 일반화, **청명 4형태**, 카드, 말풍선=화자 | ep1 c1~88 전수, 이후 표본 |
| `chamgyoyuk-manual-analysis-2026-07-17.md` | 참교육 ep1·ep2(237컷) — **대조군**, C8 반증, 공무원증, 나레이션 전환, type enum | 표본(**ep2는 2컷**) |
| `face-classify-w23-2026-07-16.md` | CCIP 43% · qwen-base 9B 34% · qwen-vl 27B 94% 근거 | — |
| `pass1-bench-w23-2026-07-16.md` | Stage V 구조 지표(1:1 위반 ~60%) 베이스라인 | — |
| `pass1-window/README.md` | **실패한 실험** — 이미지 5장 윈도우가 화자 일치 45→29% 악화(§4.10 근거) | 종결 |
| **`segment-oversize-2026-07-17.md`** | **🔥 원칙 1의 뿌리** — 690×7,965 세그먼트가 OCR을 삼킨다. A/B 실험 + **수정·검증 완료**(참교육 c3 0→34개, 얼굴 0→2개) | ✅ 수정됨·배포 대기 |
| `webtoon-pipeline/tools/segment_view.py` | 세그먼트 분할 **시각화**(전/후 나란히, 자체완결 HTML) | — |
| `anon-roster/EXPERIMENTS.md` | 하네스 실험 로그(무엇을→결과→다음) | 진행 중 |
| `anon-roster/run_anon.py` | 검증 하네스(§4.10·§8.6) — v1 결과는 §7 | 진행 중 |
| `prd.md` | **현행 구조의 정본** — 이 문서와 무관하게 유지 | — |

**⭐ = 컷별 전수 레퍼런스.** 나머지는 구조적 결론은 믿을 수 있으나 컷별 대조용으로는 부족하다.
