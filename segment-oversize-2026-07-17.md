# 🔥 거대 세그먼트가 텍스트를 삼킨다 — 원인 규명 (2026-07-17)

`prd-for-improve.md` 원칙 1("모든 컷은 분석에 사용한다")을 조사하다가 **뿌리를 찾았다.**
지금까지 "OCR이 스타일 텍스트를 못 읽는다"로 이해했던 손실의 상당수가 사실은
**OCR에 너무 큰 이미지를 넣어서 생긴 것**이다.

## 결론 (한 줄)

**PaddleOCR은 잘못이 없다. 파이프라인이 690×7,965 짜리 이미지를 OCR에 넣고 있다.**
같은 픽셀을 컷 단위로 넣으면 33개를 읽고, 프로덕션 세그먼트로 넣으면 4개를 읽는다. **8배 차이.**

## 결정적 실험 (재현 가능)

대상: 참교육(웹툰 43) ep1 컷 3 — DB상 `regions=0, faces=0`(= Stage V 호출조차 안 됨).
육안 확인: 나레이션 박스 3개 + 각진 말풍선 대사 1개 + 법정 장면 얼굴 2명.

| 실험 | 입력 | 종횡비 | OCR 검출 |
|---|---|---|---|
| **A** | **컷 3 단독** (690×1600) | 2.3:1 | **33개** — '학생들이' '마음놓고' '교권을' '침해하는' '해도' '제제할' '수단이' … ✅ |
| **B** | **프로덕션 세그먼트 seg1351** (690×**7,965**) | **11.5:1** | **4개** — '여려워졌더' '누브니' '국회화고육부는' ':' ❌ |

**실험 B는 프로덕션 DB와 정확히 일치한다**(seg1351의 regions = 4, 그중 하나가 '여려워졌더').
즉 재현 성공 — 프로덕션의 손실을 그대로 만들어냈다.

컷 경계 실측: 스트립 12,800px, 컷마다 1,600px.
→ **컷 3 = y 3,200~4,800**, **컷 5 = y 6,400~8,000** — 둘 다 seg1351(1,963~9,928) 안에 있다. ✅ 인과 확정.

## 왜 세그먼트가 8,000px이 됐나

`src/operators/cut_merger.py::_content_intervals`:
- 행의 채널별 `(max-min) <= NEAR_UNIFORM_TOL(12)` 이면 **여백 행**
- 여백 행이 **`MIN_BAND_PX(10)` 이상 연속**이면 구분 밴드 → 세그먼트 종료

**참교육 1화 프롤로그는 검은 배경이 끊김 없이 이어진다** — 다큐멘터리풍 도입부라 패널 사이
흰 여백이 없다. 그래서 y 1,963부터 9,928까지 **8,000픽셀 동안 여백 행이 10줄도 안 나온다**
→ 세그먼트가 안 끊긴다.

상한(`src/core/step1.py`)은 `WINDOW_BUDGET_PX=10,000` / `MAX_BUFFER_PX=16,000`이라
**7,965px은 강제 방출에도 안 걸린다**(캡 미달). 그대로 OCR로 간다.

## 반증한 가설 (기록)

처음엔 `_is_near_uniform_block`(구간의 98% 이상이 대표색이면 '사실상 단색'으로 **버림**)을
의심했다 — "검은 배경이 대부분이라 버려진 것 아닌가?"
**측정해보니 틀렸다**: 컷 3의 단색 비율은 **0.55**로 임계 0.98에 한참 못 미친다(컷 1~8 전부 0.53~0.76).
→ 이 가설은 폐기. 원인은 **버림**이 아니라 **크기**였다.

## 얼마나 퍼져 있나

| 에피소드 | 세그먼트 수 | 최대 높이 | 평균 | 4,000px 초과 |
|---|---|---|---|---|
| 참교육 ep1 | 164 | **7,965px** | 918px | 1 |
| 바바리안 ep10 | 83 | **8,100px** | 1,626px | **5** |
| 화산귀환 ep1 | 237 | 4,499px | 653px | 1 |

**바바리안 ep10은 4,000px 초과 세그먼트가 5개**다. 세 작품 모두에 존재한다 —
참교육만의 문제가 아니다.

### ⚠️ 바바리안에는 적용 안 됨 — 내 중간 결론이 틀렸다 (2026-07-17 정정)

> **처음엔 "바바리안 컷 2·3도 같은 원인"이라고 결론냈다. 실측으로 반증됐다.**
> 컷 3은 **단독으로 넣어도 OCR 0개**이고, 세그먼트 분할 후에도 1개('S!')만 회수된다.
> 즉 바바리안 컷 3의 "컥!"은 **거대 세그먼트 때문이 아니라 스타일 효과음이라 못 읽는 것**이다.
> `ep10-manual-analysis-2026-07-16.md`의 원래 해석("OCR이 못 읽었다")이 **맞았다.**
>
> | 바바리안 ep10 | 컷 단독 OCR | 실제 내용 |
> |---|---|---|
> | 컷 2 | 1개 | "아아아아" SFX |
> | **컷 3** | **0개** | **"컥!" 말풍선 + "탓" SFX + 얼굴** |
> | 컷 4 | 3개 ('아저씨!' '괜찮으세요?!') | 평범한 말풍선 |
> | 컷 5 | 8개 | 나레이션 |
>
> **교훈: 거대 세그먼트 안에 있다는 것만으로 원인을 단정하면 안 된다.** 반드시 컷 단독 OCR로
> 대조할 것(참교육 실험 A). 아래 표는 그 구분이다.

### 두 실패는 다른 원인이다

| 실패 | 원인 | 대표 사례 | 세그먼트 분할로 고쳐지나 |
|---|---|---|---|
| **거대 세그먼트** | 입력이 뭉개져서 감 | **참교육 c3·c5**(나레이션 박스) | ✅ 0개 → **34개** |
| **스타일 아트** | 입력은 멀쩡한데 모델이 오독 | **바바리안 c3 "컥!"**, 별명 카드 '비요른'→'비요문서' | ❌ 0개 → 1개 |

후자는 **VLM 직접 전사로만** 회수된다(§C1 — 나는 OCR 없이 "컥!"을 읽었다).

### 참고: 바바리안의 거대 세그먼트 현황 (원인은 아니었으나 존재함)

바바리안 ep10의 거대 세그먼트:

| seg | y | 높이 | regions |
|---|---|---|---|
| 36646 | 880~6,255 | **5,375px** | **2** ← **컷 2·3이 여기 있다** |
| 36657 | 20,496~28,596 | 8,100px | 9 |
| 36716 | 143,793~148,972 | 5,179px | **0** |
| 36687 | 80,041~85,630 | 5,589px | 15 |
| 36723 | 165,392~169,553 | 4,161px | 2 |

컷 경계 실측: 컷2 = y1,600~3,200, 컷3 = y3,200~4,800 → **둘 다 seg36646(5,375px) 안**이지만,
위에서 반증했듯 **그게 원인은 아니었다**(컷 단독으로도 0개).

거대 세그먼트 자체는 세 작품 모두에 존재하므로, **다른 회차/작품에서는 실제 손실을 유발할 수 있다** —
참교육에서 실증됐다. 다만 **"거대 세그먼트 안에 있다 = 그것 때문이다"는 성립하지 않는다.**

세그먼트 높이별 텍스트 밀도(regions/1,000px):

| 높이 | 참교육 ep1 | 바바리안 ep10 | 화산귀환 ep1 |
|---|---|---|---|
| <500px | **9.75** | **9.91** | **5.66** |
| 500~1.5k | 3.51 | 2.87 | 2.15 |
| 1.5k~4k | 1.84 | 2.28 | 2.27 |
| **>4,000px** | **0.50** | **0.99** | 2.22 |

⚠️ **이 표만으로는 인과를 말할 수 없다** — 교란 요인이 있다. 텍스트가 많은 구간은 패널 사이
여백 때문에 잘게 쪼개지고, 그림 위주 구간은 통으로 붙는다. 즉 "큰 세그먼트엔 원래 글자가 적다"가
성립할 수 있다. **인과의 근거는 이 표가 아니라 위의 A/B 실험이다**(같은 픽셀 33개 vs 4개).

## 함의

1. **§C1(OCR은 앵커이지 문지기가 아니다)의 근거가 한 층 더 내려간다.**
   OCR을 더 좋은 모델로 바꿔도 소용없다 — **입력이 이미 망가져서 간다.**
   (Baidu OCR 철거 결정은 여전히 옳다. 문제가 모델이 아니었으니.)
2. **"스킵률이 낮으면 안전하다"는 틀렸다.** 참교육은 스킵률 2.2%로 세 작품 중 가장 낮은데,
   버린 것은 **1화 도입부의 핵심 설명 2컷**(체벌금지법 배경 + 교사 폭행치사 뉴스)이다.
3. **손실이 조용하다.** `skipped=empty`로 기록돼 지표에 안 잡히고, 벤치의 "유효 컷"에서도 빠진다.
4. **작화 스타일이 손실을 결정한다.** 검은 배경이 이어지는 연출(다큐풍 도입부, 야간 전투,
   암전 연출)일수록 세그먼트가 커지고 → 더 많이 잃는다. **하필 연출상 중요한 장면**이다.

## 고칠 방향 (제안 — 미구현, 논의 필요)

⚠️ 이건 프로덕션 Step1 변경이라 **사용자 승인 없이는 손대지 않는다.**

1. **세그먼트 높이 상한을 OCR 친화적으로** — 지금 `MAX_BUFFER_PX=16,000`은 **메모리 상한**이지
   OCR 입력 상한이 아니다. OCR로 보내기 전에 `~2,000px`(또는 종횡비 3:1) 단위로 **재분할**하면
   된다. 여백 밴드가 없어도 강제로 자르되, **겹침(overlap)을 줘서 경계에 걸친 글자를 보존**해야 한다.
2. **여백 판정 완화** — `NEAR_UNIFORM_TOL=12`는 검은 배경의 미세 그라데이션을 "콘텐츠"로 본다.
   다만 이건 부작용이 크니 1)이 더 안전하다.
3. **손실 가시화** — `regions=0`인 컷을 조용히 스킵하지 말고 **경고로 남긴다**.
   지금은 아무도 모른다.

### ✅ 제안 1을 검증했다 — 4개 → 117개 (29배)

**같은 픽셀**(seg1351, 690×7,965)을 **2,000px 타일 + 100px 겹침**으로 잘라 그대로 OCR에 재투입:

| 타일 | 높이 | OCR 검출 |
|---|---|---|
| 0 (y 0~2000) | 2,000 | 25개 |
| 1 (y 1900~3900) | 2,000 | 32개 |
| 2 (y 3800~5800) | 2,000 | **41개** |
| 3 (y 5700~7700) | 2,000 | 18개 |
| 4 (y 7600~7965) | 365 | 1개 |
| **합계** | | **117개** |

| 방식 | 검출 |
|---|---|
| **현행(통짜 7,965px)** | **4개** |
| **재분할(2,000px 타일)** | **117개** ← **29배** |
| (참고) 컷 3 단독 | 33개 |

**코드 수정 없이 입력 크기만 바꿔서 재현했다.** 즉 고치는 비용이 낮고 효과가 크다.
겹침 100px는 경계에 걸친 글자 보존용 — 중복 제거는 별도 필요(bbox IoU 등).

⚠️ 남은 검증: 117개 중 **중복/오탐이 얼마나인지** 세지 않았다. 겹침 구간의 같은 글자가 두 번
잡혔을 수 있다. 그래도 4 → 117은 중복을 감안해도 압도적이다.

## 재현 방법

```bash
cd webtoon-pipeline && set -a && source ../prod.env && set +a
PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python PYTHONPATH=. .venv/bin/python -c "
import io, numpy as np
from PIL import Image
from src.config.db import db_cursor
from src.config.s3 import fetch_cut_image
from src.core.step1 import _resize_cut_to_width
from src.operators.ocr_yolo_client import run_ocr
with db_cursor() as cur:
    cur.execute('SELECT source, title_id FROM webtoon WHERE id=43'); src,tid = cur.fetchone()
W=690; chunks=[]
for cn in range(1,9):
    b=fetch_cut_image(src,tid,1,cn)
    chunks.append(np.asarray(_resize_cut_to_width(Image.open(io.BytesIO(b)).convert('RGB'), W)))
strip=np.vstack(chunks)
for name, arr in (('컷3 단독', strip[3200:4800]), ('프로덕션 세그먼트', strip[1963:9928])):
    buf=io.BytesIO(); Image.fromarray(arr).save(buf,'JPEG',quality=92)
    print(name, arr.shape, '→', len(run_ocr(buf.getvalue())), '개')
"
```
⚠️ `PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python` 필수(chromadb/protobuf 충돌).

## ✅ 수정 완료 (2026-07-17, 사용자 승인)

### 무엇을 고쳤나

| 파일 | 변경 |
|---|---|
| `src/operators/cut_merger.py` | **`split_tall_interval()` 추가** + 상수 `MAX_SEGMENT_PX=2000`, `SPLIT_SEARCH_RATIO=0.15`, `SPLIT_SLACK=1.2` |
| `src/core/step1.py` | **`_emit()` 헬퍼 추가** — 방출 3경로(hard cap / final flush / terminated)를 한 군데로 모아 분할 적용 |

**분할 방식 — 무작정 2,000px마다 자르지 않는다.** 목표 경계 ±15%에서 **가장 평평한 행**
(`(max-min)`이 최소 = 글자가 없을 확률이 높은 행)을 찾아 자른다. 그래서 **겹침/중복 제거가 불필요**하다.
- `SPLIT_SLACK=1.2` 이내로만 넘는 구간은 안 쪼갠다(자투리 방지).
- 탐색창이 전부 글자면 목표 지점에서 그냥 자른다 — **통짜로 다 잃는 것보다 낫다.**

### 검증

**단위 테스트(불변식)**: 연속·비겹침·**합집합 보존**·최대 높이(≤ max_h×slack)·**절단 지점 ptp=0**
(= 글자 관통 안 함) 전부 통과. 빽빽한 텍스트(평평한 행 없음)·오프셋 구간 포함.

**실제 파이프라인 경로 (참교육 ep1, `_content_intervals` → `split_tall_interval`):**

| | 현행 | 수정 후 |
|---|---|---|
| 세그먼트 수 | 9 | **13** (+4) |
| 최대 높이 | **7,965px** | **2,010px** |
| 전체 텍스트 검출 | 78개 | **183개 (2.3배)** |
| **컷 3** | **0개** | **34개** ✅ '학생들이' '마음놓고' '교권을' '침해하는' … |
| **컷 5** | **0개** | **22개** ✅ '교권' '교실' '붕괴의' '지적되던' … |

**내가 육안으로 읽었던 나레이션 박스가 그대로 돌아왔다.**

**🆕 YOLO(얼굴)도 같이 회수된다** — 세그먼트를 OCR·YOLO가 공유하므로 예상대로였다:

| | 현행 | 수정 후 |
|---|---|---|
| 전체 얼굴 | 2개 | **5개** |
| **컷 3 얼굴** | **0개** | **2개** ✅ (육안으로 본 법정 장면 얼굴 2명과 일치) |

**즉 거대 세그먼트는 텍스트만이 아니라 얼굴까지 삼키고 있었다.** `regions=0 AND faces=0`으로
스킵된 컷들의 "얼굴도 없었다"는 것 역시 같은 원인이었다.

### 비용

세그먼트 9 → 13개(+44%)라 OCR 호출이 그만큼 는다. 다만 **각 호출이 훨씬 작아서**(7,965 → 2,010px)
총 픽셀은 같고, 오히려 큰 이미지 리사이즈 부담이 준다. 실측 필요.

### 남은 것

- ⚠️ **프로덕션 미배포.** 커밋하면 CI가 `pipeline_repo` 태그를 자동 커밋해 배포된다(`CLAUDE.md`).
  **배포 승인 필요.**
- ⚠️ **기존 회차 재분석 필요.** 이미 분석된 회차는 잘린 세그먼트 그대로다 —
  고친 코드는 **앞으로 분석할 회차에만** 적용된다.
- `MAX_SEGMENT_PX=2000`은 참교육 실측(2,000px 타일 → 117개)에서 고른 값이다. 최적값 탐색 안 함.
- ✅ YOLO 개선 확인됨(위). 다만 **컷 116**(얼굴만 있고 글자 없음, `faces=0`)은 미확인.

## 상태

- **원인 규명 완료 → 수정 완료 → 검증 완료.** 배포 대기.
- 관련: `prd-for-improve.md` §1 원칙 1·§7, `chamgyoyuk-manual-analysis-2026-07-17.md`,
  `ep10-manual-analysis-2026-07-16.md`(정정 주석)
