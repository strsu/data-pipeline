# 정리 단계 재설계 초안 — "슬롯 + 대사 흐름"을 척추로 (임시)

> ⚠️ **임시 저장.** 2026-07-22 세션 논의를 잃지 않으려고 만든 초안. 정본 아님.
> 설계 원칙의 정본은 `prd-for-improve.md`, 현행은 `prd.md`. 합의되면 그쪽으로 편입.
> 자족적으로 쓴다(세션 끊겨도 이어가게).

## 0. 한 줄

**요약·명명·플로우는 이미 된다. 깨지는 건 "어느 얼굴이 누구냐"(얼굴 전수 귀속)뿐이다.**
그러니 정체성의 척추를 **얼굴정체(CCIP 43%)에서 → 익명 슬롯 + 대사 흐름으로** 옮기고,
얼굴 귀속은 "교차회차 링킹 투표권 하나"로 강등한다.

## 1. 동기 — 무엇이 실제로 깨졌나 (웹툰23 '바바리안' 47화 실측, 2026-07-21)

- 죽은 **카락**(2화 사망) 클러스터가 자석이 되어 154장을 흡수 → **브라운 로트밀러** 얼굴이
  카락에 쌓이고 프로필이 로트밀러 역할(내비게이터)로 오염.
- 이름 `로트밀러`(3546)엔 **히쿠로드 무라드** 얼굴이, `나일 어반스`(3502)엔 미샤 오빠 **테일론**이 병합.
- "미궁 파티 6인" = 실제 5인 (죽은 카락이 유령 6번째 멤버로 부활 — 얼굴 오귀속의 직접 증상).
- 아루아 레이븐 → OCR '레이본'(이름 실수, 흐름엔 무해).

**공통 뿌리 = `face → named character` 하드 바인딩.** CCIP 43% 정확도가 이 척추를 떠받쳐서,
한 번 틀리면 프로필·화자·서사·인원수까지 오염된다.

## 2. 재보정 (2026-07-22 사용자 정정 — 앞선 전제 2개가 틀렸다)

- ❌ "OCR을 더 좋은 모델로" → **불필요.** PaddleOCR 성능 자체는 괜찮다. 문제는 **SFX(효과음)를
  자꾸 잡는 것.** 답은 "더 잘 읽기"가 아니라 **"LLM이 SFX를 버리기"**. (Qwen3-VL/CLOVA 조사는
  이름카드 한 군데 빼면 보류.)
- ❌ "명명이 망가졌다" → **과장.** 실험8은 *얼굴정체 주입을 뺀 채 glm 단발 cold 명명*을 잰 거라
  아무도 안 쓰는 경로였다. 실제 프로덕션 명명은 다회차 증거 누적 + ep1~10 수동라벨로 "나쁘지 않게"
  등록된다. **실제 병목은 명명이 아니라 얼굴 귀속.**
- ✅ 요약은 '바바리안'·'화산귀환' 모두 현행 하네스로 나름 잘 나온다 → **전체 플로우는 문제없다.**

## 3. 핵심 구분 — "대표 얼굴 1장" ≠ "얼굴 전수 귀속"

> 인물당 **대표 얼굴 crop 1장**(쉬움)과 **모든 얼굴 탐지를 named 인물에 배정**(어렵고 깨지는,
> 47화의 원흉)은 완전히 다른 일이다.

- 도감·갤러리엔 인물당 crop 1장이면 충분 → 슬롯에서 좋은 것 하나 고르면 끝.
  **154장을 카락에 다 배정할 필요가 없다.**
- 전수 귀속 단계를 없애도 요약·명명·화자 손실 거의 없음.

## 4. 제안 아키텍처

```
[Stage V]  컷별 익명 추출 (이름 미주입 — 실험8로 검증됨)
             각 컷: 익명 인물(슬롯) + 대사 블록 + 말풍선 스타일/꼬리
                ↓
[정리(개선 대상)]  회차 전역 1콜로 "슬롯 정리":
  슬롯별 = { 등장 컷들, 딸린 대사 라인, 이름 증거, 시각 서술자, 대표 crop 1장 }
    ① 화자 배정  = 흐름(꼬리+대사교대+화법 반말/존대·호칭+POV) → 슬롯   ← 얼굴/이름 불필요
    ② 명명       = 증거풀→자모정규화→argmax, 기본값 NIL(무명)
    ③ 교차회차 링크(선택) = 이름 우선 + 서사, CCIP는 투표권 하나
```

### 레이어 대조

| 레이어 | 지금 | 제안 |
|---|---|---|
| 척추 | 얼굴 정체(CCIP 43%) | **익명 슬롯 + 대사 흐름** |
| 화자 | 얼굴 기반 | **흐름 → 슬롯** |
| 이름 | 슬롯에 박음 | 슬롯에 붙는 **수정가능 엣지**, 기본 NIL |
| 얼굴 | 전수 귀속(깨짐) | **대표 crop 1장**, CCIP는 교차회차 투표권 |
| SFX | 텍스트로 등록 | **분류 후 배제** |
| 제안검토 | 노이즈 큐 | **제거**, 저신뢰 엣지만 human UI로 |

## 5. 외형↔인물 1:N 스키마 — **살린다 (새 설계의 척추)**

화산귀환 청명 4형태(전생전 성인/아기/유아/전생후 거지소년) 때문에 만든 `appearance(외형) N :
character 1` 스키마 = 지금 말하는 **"익명 슬롯 = appearance"**와 정확히 같은 것.

- 청명 = 4 appearance를 묶은 1 character. 비요른 = 이한수 = 2 appearance(빙의).
- 얼굴 귀속 강등이 이 스키마를 **죽이는 게 아니라 살린다** — 아기↔성인은 임베딩으로 절대 못 잇고
  (§C8.2) **서사(POV 연속·호명·말투·지식·자각)로만** 이어진다(§4.8의 5신호). 그러니 링크는
  임베딩 매칭이 아니라 **수정 가능한 서사 엣지**여야 하고, 그게 1:N 위에서 도는 링커다.
- **기본은 1:1**(한 슬롯 = 한 인물). 1:N은 빙의/환생/변장 같은 특수 케이스에서 링커가 엣지를 걸 때만.
- 이름도 1:N (§C8.1): 비요른(몸)/이한수(인격), 청명/초삼 → **alias 목록 + 정본(canonical) 지정** 필요.
- 기존 스키마 `analysis_character_appearance(character_id)`가 이미 그릇 → 유지·활성화.

## 6. SFX 처리

- LLM이 `type=sfx`로 분류 → **text_region에서 배제**(대사·이름 증거로 안 씀).
- 반대로 `is_name_card=true`는 **유지**(스타일 아트라도 값짐). SFX vs 이름카드는 둘 다 스타일
  텍스트지만 **의미로 구분** — 모델이 가능.
- 부수효과: 명명 유령(소금/소론 류)도 함께 소멸(발화행위 게이트 + SFX배제 이중).

## 7. 제안검토(adjudicator) 제거

- 노이즈 뿌리 = 바로 그 `face→named` 하드바인딩(오귀속이 name/merge/reassign 제안 대량 생성).
  실제 유효 제안 10~20%.
- 척추를 슬롯+이름으로 바꾸면 **제안 대부분이 애초에 안 생긴다.**
- 조치: adjudicator LLM 큐 **제거**. 대체 = 저신뢰 이름/링크 **엣지에 플래그**만 달아 human 라벨
  UI에 직접 노출(§C4 이름=수정가능 엣지). 심판 없이.

## 8. 명명 파이프라인 상세 (2026-07-22 리서치 종합 — EXPERIMENTS 실험8 + 엔티티해소 조사)

```
① 수집   각 이름-힌트를 (표면형, 발화행위종류, 수신자, confidence)로 슬롯에 부착
          게이트: 발화행위 있어야 통과 (호격 -야/-아/님·자칭 "내 이름은"·카드·소품)
          → 유령 이름 차단 (LLM NER null-과탐 방어)
② 정규화 슬롯별 후보를 Hangul-자모 편집거리로 blocking→병합
          → 에르완/에르렌/에르윈 = 1정본 + alias / 아루아 레이븐·레이본도 여기서
③ 배정   호격은 화자 아니라 수신자 슬롯에 (단일-mention 규칙 + 컷 공존)
          하드제약: cannot-link(한 컷 두 슬롯 ≠ 같은 이름) + must-link(한 슬롯 전 컷 = 같은 이름)  ← Magiv2
④ 결정   회차 전역 1콜 argmax + self-verify 2차콜. 기본값 NIL.
⑤ 승격   prominence 임계 넘는 슬롯만 명명 시도 (1컷 크라우드 제외)
⑥ 산출   이름 = confidence+source 엣지(추출 재실행 없이 교정). 슬롯없는 이름(죽은 대식)도 텍스트-only 등록
```
형식 근거: "익명 먼저·이름 마지막·소급"은 비단조추론/belief revision. 만화 SOTA(Magiv2)조차 이름을
대사에서 안 뽑음(name bank 요구) → **희소 화면내 증거에서 이름 뽑기는 우리 몫(미개척).**

## 9. 다음 실측 / 열린 질문

1. ⭐ **[먼저] "대사 흐름만으로 화자배정이 얼마나 되나"** — ep10·ep11 정답지로 소실측.
   사용자 핵심 가설("흐름으로 화자 추출 가능?")을 먼저 검증해야 설계 확정.
2. 정리 단계를 "슬롯 정리 + 흐름 화자배정 + 지연 명명"으로 프로토타입(run_anon 확장).
3. 교차회차 링크에서 CCIP를 투표권 하나로 두는 실측(47화 카락 자석·6인=5인 잡히나).
4. 이름카드 vs SFX 구분 정확도(모델이 의미로 가르나).
5. 열린: 대표 crop 1장 선택 기준. 교차회차 이름-매칭의 동명이인 처리.

## 10. 실측 로그 — 2026-07-22 야간 자율세션

> 사용자 위임: 실험·측정만(프로덕션 무수정), 게이트웨이 통일, ep10/11 정답지 + 1~50화 실데이터 적극 활용.
> 게이트웨이 모델 상태(probe): ✅ glm-5.2·glm-4.6v·qwen-base(9B) / ❌ qwen-vl(27B, HTTP500)·qwen3.5-122b(미라우팅).

### M — 프로덕션 규모 측정 (웹툰23, 1~50화+ 분석분)

**M4 제안(suggestion) 노이즈 — adjudicator 제거 강력 지지**
- 총 1,801건. 해소된 208건 중 **accepted 15 = 7.2%**(사용자 추정 10~20%보다 낮음).
- **merge가 71%(1,278건)** 차지, accept율 6.6%(13/197). face_reassign 150·label_conflict 199 대부분 pending.
- → 제안 큐는 순수 노이즈 생성기. 뿌리는 face→named 오귀속(§7). **제거 결정 데이터로 확증.**

**M1 화자 커버리지 — narration이 완전 공백**
| type | n | speaker있음 |
|---|---|---|
| speech | 13,607 | 64% |
| monologue | 1,033 | 59% |
| **narration** | 3,095 | **0%** (7건뿐) |
| system/other | 2,920 | 0% |
- narration은 POV 독백(ep10 전수 A)인데 **화자 0%** — 흐름 접근이 채울 명백한 빈자리.
- speech도 36%가 화자 없음.

**M3 SFX 비중** — other(효과음) 1,324건 = 전체 텍스트 주석의 6.4%. (+system 7.7%)

**M2 얼굴 오귀속 규모 — 자석·과분할 확증**
- **M2a: cluster 753 vs character 36 (21배).** ep11 시점 225 vs 17에서 폭발.
- **M2b: face_identity source = human 669 / step2 5,832** (ep1~10 수동라벨 669).
- **M2c 죽은/부재 정체성 자석(확증):**
  - **카락**: 162얼굴 · ep2→88 · **45개 화**. (2화 사망인데 45개 화에 퍼짐)
  - **이한수**: 533얼굴 · ep1→87 · **67개 화**. (빙의 전 현실 자아 — 회상에만 나와야 함)
  - avg score 전반 0.05~0.09(매우 낮음) — 저신뢰 매칭이 그대로 커밋됨.
  - → §1의 카락 사례가 개별이 아니라 **구조적**. 이한수(빙의 원자아)가 두 번째 대형 자석 = §C8(시각≠서사) 직격.

### E1 — 흐름-only 화자배정 (ep10, 정답지=수동분석 컷별 A/B, 102 gold)

입력: 중립 시각 로스터(A/B, register/POV 힌트 없음) + 순서대로 블록(type 포함, 화자 제거).
이미지 없음. 스크립트 `anon-roster/exp_speaker_flow.py`(스트리밍 — 524 회피).

| 모델 | 정확도 | 비고 |
|---|---|---|
| qwen-base(9B) | **0.44** (45/102) | ❌ 90블록(61%)에 슬롯 대신 **type("other")을 화자로** 출력 = 태스크 이해 실패. narration POV 전부 놓침. 9B 한계 재확인 |
| **glm-5.2** | **0.951** (97/102) | ⭐ **wrong=0**, null 5(전부 SFX=올바른 거절). speech 59/59·**narration 24/24**·monologue 12/12. acc_excl_null=**1.0** |

**⭐ 핵심 결과 — 사용자 가설 확증: "대사 흐름만으로 화자 추출"이 capable 모델에서 사실상 완벽.**
- 이미지·얼굴·이름 0 주입, **오직 흐름**(대사교대·반말/존대·호칭·POV)으로 배정.
- **narration 24/24 전부 POV 인물(A)에 귀속** — 프로덕션 narration 화자 0%(M1) 대비 극적. 흐름 접근이
  정확히 그 빈자리를 100% 채운다.
- 배정한 것 중 **틀린 게 0개**, SFX 5개는 null로 정직하게 거절 = §0 염원(모르면 null) 그대로.
- ⚠️ **경계조건**: (a) 정답 2인 로스터를 *제공*함(정리단계 클러스터링은 분리 검증 필요=E1b),
  (b) 입력은 *깨끗한 수동 전사*(노이즈 파이프라인 전사 아님=E1c), (c) 정답지는 강한 참조(prd §4.9).
  그래도 "흐름→화자"라는 핵심 메커니즘은 결정적으로 성립.
- 관찰: 9B는 포맷 자체를 못 따름(type을 화자로 출력) → **정리·화자 콜엔 glm-5.2급 필요**(전사=glm-4.6v와 분리, §4).

### E5 — 자모 정규화 프로토타입 (결정론, `anon-roster/exp_jamo_norm.py`)

exp8 실제 오독 변종으로 검증. 음절 편집거리는 전부 1(구분 불가)인데 **자모 유사도**가 갈라준다:

| 쌍 | 음절거리 | 자모유사 |
|---|---|---|
| 에르웬~에르완 | 1 | 0.86 |
| 에르웬~에르렌 | 1 | 0.71 |
| 에르웬~에르윈 | 1 | 0.86 |
| 레이븐~레이본 | 1 | 0.86 |

- **thresh 0.65 클러스터링**: {에르웬·에르완·에르렌·에르윈} 한 그룹 + {레이븐·레이본} 병합, **오병합 0**
  (시아·소금·소론·제베·투리엔·미샤 전부 분리 유지). 0.55로 낮추면 비요른/비요문서(깨진 카드)까지.
- → §8 ② 검증됨. 외부 라이브러리 없이 Unicode 산술로 충분.

#### ⚠️ 자모의 가중치 — 문지기가 아니라 정규화 도구 (라락 사례로 정정, 2026-07-22)

사용자 지적: **"라락"은 타탁류 SFX의 OCR 오독인데(ep43 나레이션으로 오분류된 고립 토큰), 자모
정규화를 쓰면 라락~카락(0.8)으로 죽은 카락이 부활할 위험이 있다.** 정당한 우려. 순서를 못박는다:

```
Gate 1 발화행위(PRIMARY): "이 토큰이 이름으로 쓰였나?"
   자칭("나는 X"/"자칭 X") · 호격조사("X야/아/님") · 이름카드 · 나레이션이 X를 주어로 서술.
   → 라락(명명 구문 없는 고립 토큰) = 여기서 탈락. 자모 볼 것도 없음.
Gate 2 자모 정규화(SECONDARY): Gate1 통과분끼리만 표면형 병합(에르완/에르렌/에르윈→에르웬).
Gate 3 죽은이름 대조(SAFETY): 이름 통과분이 죽은인물과 자모-근접 → **merge 아니라 veto(거부)**.
```

**자모의 가중치 규칙:**
- **"이름이냐" 판정 = 0** (자모는 절대 명명 승격 근거가 아니다).
- **"기존 확정 이름의 오독 변종이냐" 정규화 = 높음**.
- **죽은 이름 방향 = merge 금지, veto만** (라락→카락 부활 원천 차단).

라락이 v2에서 거부된 건 옳지만 **이유가 틀렸다**(SFX/비명명이 아니라 "죽은이름 근처"라 운으로 막힘).
만약 라락이 살아있는 이름과 자모-근접했다면 잘못 흡수됐을 것. **문지기는 발화행위 게이트고, 자모는
그 뒤에서 확정된 이름들의 표면형만 통일한다.** = §8 순서 확정.

#### ⚠️⚠️ dead-name veto는 폐기 — 죽음은 파생 상태, 죽어도 텍스트엔 나온다 (사용자 지적 2026-07-22)

**하드코딩 `DEAD=["카락"]`은 원칙 없는 반칙이고, 회상·3인칭 언급을 깨뜨린다.** 두 가지가 근본:

**(1) 죽음은 서사에서 파생되는 상태다.** 요약 레이어가 이미 안다("ep1~2 카락 사망"). 인물마다
`state(alive/dead/absent) + last_seen_ep`를 요약에서 파생·저장하고 링커가 참조 — 하드코딩 리스트 금지.

**(2) 죽어도 텍스트엔 정당하게 나온다:**
- 회상(flashback): 죽은 카락이 회상 장면에 얼굴·이름으로(§C8 주마등 프레임과 동일).
- 3인칭 언급: "예전에 카락이 그렇게 했었지…" — 살아있는 인물이 과거형으로 언급.
- → veto는 이 둘을 잘못 차단한다. **틀렸다.**

**핵심 = 이름 증거의 3역할 구분** ("이름이 나오냐"가 아니라 "현재 슬롯에 묶느냐"):

| 역할 | 예 | 슬롯 결합 |
|---|---|---|
| 자칭 | "나는 카락" | **화자 슬롯** |
| 호격 | "카락아!" | **수신자 슬롯** |
| **3인칭 언급** 🆕 | "예전에 카락이 했었지" | **어느 현재 슬롯에도 결합 안 함** (언급된 엔티티일 뿐) |

3인칭 언급은 *말한 사람*이 화자고, 카락은 **인물 대장엔 남되(존재) 현재 파티 슬롯엔 안 붙는다.**
회상이면 별도 회상-프레임 슬롯에 붙는다.

**그리고 흐름 접근은 죽음을 이미 자연스럽게 처리한다**: 죽은 사람은 현재 회차에서 말을 안 하니
현재 슬롯이 안 생긴다 — E2E에서 카락이 7회차 내내 안 나온 이유가 이것. face-attribution은 죽음을
몰라 자석이 됐지만, 흐름은 "지금 말하나?"를 물으니 원천 미발생. **→ dead-veto 불필요·유해.**

**교정된 가드(최종):** ①발화행위 게이트(라락=SFX/고립토큰 차단, 죽은이름과 무관) ②자칭/호격/3인칭-언급
구분(언급은 슬롯 미결합) ③서사 상태(파생)+회상 프레임(죽은 인물의 현재-활동 결합은 회상 프레임 있을 때만).
라락은 ①이, 카락 언급/회상은 ②③이 처리 — **자모·dead-list가 아니라.**

### M5 — 최종 등록 이름은 이미 깨끗함 (자모유사 스캔, 1~50화)

36개 named character 쌍 스캔 → 유사도 ≥0.6은 **1쌍뿐**(드자르위 클랜 대변인~마법사 0.68 = 실제
다른 인물, 올바른 비병합). **레이븐/레이본 류 중복 없음** — 명명은 다회차 누적으로 이미 수렴.
- **함의(정직):** E5 자모정규화는 *현재 버그 수정*이 아니라 **회차별 명명(신설계)의 방어장치**다.
  현행은 다회차 누적이 변종을 흡수하지만, per-episode 명명으로 가면 이 안전망이 필요해진다.
- **재확인**: 문제는 이름이 아니라 얼굴(M2). 사용자 진단과 일치.

### E1b — 로스터 미제공, 모델이 통합+화자배정 (ep10, glm-5.2) ⭐

로스터를 안 주고 "대사만으로 인물을 갈라내고 화자 배정하라". 결과:
- **model_roster_size = 2** (정답 2). exp8의 glm-4.6v **컷별 비전이 17로 과분할**한 것과 정반대.
  → **과분할의 원인은 "컷별"이었고, 회차전역 텍스트 콜이 고친다.**
- 화자 정확도 **0.951, wrong=0** (E1a와 동일). SFX 5개 null.
- 모델이 스스로 뽑은 로스터 서술: `A: 반말·해라체·POV·B에게 '아저씨'로 불림 / B: 에르웬·존댓말·
  '아저씨'라 부름·저격`. **호격에서 이름(에르웬)까지 포착.**
- ⚠️ **경계**: ep10은 반말/존댓말로 화자가 명확히 2인. **대사 없는 침묵 인물(ep11 군중)은 텍스트
  통합으로 못 잡는다** → 설계 정련: **흐름=화자/명명 통합, 시각클러스터=외형(침묵 포함)**, 상보.

### E4 — SFX 분류는 이미 충분, 문제는 "배제 안 함" (anon glm-4.6v type vs 정답, ep10)

99개 정렬쌍: speech 46/46, monologue 8/9, narration 23/24, **other(SFX) recall 87%(13/15)·
precision 81%(13/16)**. 혼동은 system↔other↔narration(타이틀/상태창) 소수.
- → **"SFX 버리기"는 분류 개선이 아니라 type=other를 이름·화자·대사에서 배제하는 필터링**(§6 = 값싼 변경).
  모델은 이미 SFX를 태깅한다.

### M2 심화 — 자석은 100% CCIP(step2) 산물

| 인물 | human | step2 | 비고 |
|---|---|---|---|
| 카락 | **0** | 175 | 사용자 미개입 구간, 순수 CCIP 자석 |
| 이한수 | 25 | **510** | CCIP가 510 밀어넣음(빙의 원자아) |
| 비요른 | **322** | 363 | 수동라벨 322는 깨끗 |
- ep15~18에서 **이한수 얼굴(33·23·21·18) > 비요른(2·3·12·15)** — 주인공 얼굴을 이한수가 던전화에서 훔침.
- **핵심: 사용자가 수동라벨한 얼굴은 자석 안 됨, CCIP 자율구간만 자석.** → CCIP 강등의 직접 근거.

### E1c — 현실성: 노이즈 파이프라인 전사로도 견고 (ep10, glm-5.2)

깨끗한 수동전사가 아니라 **실제 anon glm-4.6v 전사(오독 포함, 204블록)**로 흐름 화자배정.
자동정렬된 79 gold 채점:
- **정확도 0.911** (72/79), null 제외 **0.96**, wrong 3(전부 speech, 오독으로 인한 애매).
  **narration 22/22·monologue 8/8** 여전히 완벽.
- → 경계조건(b) 닫힘: **깨끗전사 0.95 → 노이즈 0.91.** 흐름 화자배정은 파이프라인 노이즈에 견고.

### 세 실험 종합 — 흐름 화자배정 결정적 확증

| 실험 | 조건 | 정확도 | 로스터 |
|---|---|---|---|
| E1a | 깨끗 전사 + 정답 로스터 제공 | **0.951** | 제공됨(2) |
| E1b | 깨끗 전사 + **자가 통합** | **0.951** | **모델이 2로 통합**(exp8 비전 17↔) |
| E1c | **노이즈 파이프라인 전사** + 로스터 | **0.911** | 제공됨(2) |

**결론: "대사 흐름만으로 화자 추출"(사용자 가설)은 capable 텍스트 모델(glm-5.2)에서 성립하며,
노이즈·자가통합 조건에서도 견고하다.** 현행 face 기반(narration 0%, speech 64%)보다 압도적.

### E1b-ep11 — 붐비는 회차에서도 흐름-통합이 폭발하지 않음

ep11(시각 인물 8~15명) anon 전사 231블록으로 흐름-통합:
- **roster_size = 3** (폭발 X): A=비요른(99블록)·B=**에르윈**(에르웬 오독, 25)·C=적대 탐험가 리더(11).
  None=96(SFX+미배정).
- **해석**: 말하는 3파티만 잡음. 침묵 적대파티원·몽타주 군중은 대사가 없어 안 잡히는 게 **정답**.
  → E1b 상보성 실증: **흐름=화자/명명(말하는 인물), 시각클러스터=외형(침묵 포함).**
- B가 "에르윈"으로 잡힘 = **E5 자모정규화 필요성 실증**(에르윈→에르웬 canonical).

---

## 11. 최종 종합 (2026-07-22 야간 세션)

### 무엇을 왜 했나
사용자 가설 "대사 흐름으로 화자 추출이 가능한가"를 검증하고, 흐름-first 재설계(§4)의 정량 근거를
ep10/11 정답지 + 1~50화 프로덕션 실데이터로 모으기 위함. 범위=실험·측정만(프로덕션 무수정).

### 결과 한눈에
| 축 | 측정 | 결론 |
|---|---|---|
| **흐름 화자배정**(핵심 가설) | E1a 0.95 / E1b 0.95(자가통합) / E1c 0.91(노이즈) | ✅ **성립·견고** |
| narration 귀속 | 프로덕션 0% → 흐름 22~24/24 | ✅ 흐름이 빈자리 100% 채움 |
| 과분할 원인 | 컷별 비전 17 → 회차전역 텍스트 2 | ✅ **"컷별"이 원인, 통합이 해결** |
| 얼굴 자석 | 카락 step2 175/human 0, 이한수 step2 510 | CCIP 자율구간만 자석, 수동라벨은 깨끗 → **CCIP 강등** |
| 과분할 규모 | cluster 753 vs character 36 (21배) | 얼굴 전수귀속 폐기 근거 |
| 제안 큐 | accept 7.2%(merge 71%) | **제거** |
| SFX | 분류 recall 87% but 화자·이름에 배제 안 됨 | **배제 필터**(값싼 변경) |
| 이름 정규화 | 자모 thresh 0.65로 변종 병합, 오병합 0 | per-episode 명명 안전장치 |
| 등록 이름 현황 | 유사쌍 1개(비병합 정답) | 이름은 이미 깨끗 = 문제는 얼굴 |

### 설계에 미치는 최종 함의
1. **흐름-first 척추는 실증됐다.** 화자·명명을 익명 슬롯 위 흐름으로 옮기는 게 face 기반보다 압도적.
2. **정리 단계 = 이중 구조**: (a) 시각클러스터로 외형 로스터(침묵 포함) + (b) 흐름-통합으로 말하는
   인물 화자/명명. 두 로스터를 링크. E1b가 (b)는 회차전역 텍스트 1콜로 됨을 보임.
3. **모델 분업 확정**: 전사·시각클러스터=glm-4.6v(SFX분류 OK·빠름), 화자·통합·명명=glm-5.2급
   (9B는 태스크 못 따름). 이미지 없이 텍스트로.
4. **얼굴 전수귀속 폐기 + 대표 crop 1장**: 자석이 전부 CCIP 산물이므로 근거 확실.
5. **제안 큐 제거·SFX 배제·자모 정규화**: 전부 값싼 변경, 데이터로 뒷받침.

## 12. E2E — 흐름-통합이 카락/로트밀러 오귀속을 교정하는가 (ep37·41)

프로덕션 DB의 ep37~44 대사(이미 전사됨)를 순서대로 뽑아, 정체성 주입 0으로 흐름-통합(glm-5.2)만
적용. 스크립트 `anon-roster/exp_e2e_correction.py`.

**결정적 사실(대사 실측)**: ep37~44 대사에서 **'카락' 호명 = 0회**. 반면 로트밀러 21·히쿠로드 17·
무라드 13·드왈키 26·미샤 9·엘리사 11. → 대사는 카락을 한 번도 안 부른다. 프로덕션의 "카락 파티원"은
순수 face-attribution 산물(CCIP 주입 이름이 서사 오염).

### ep37 — 완벽 교정 🎯
| 프로덕션 얼굴귀속(before) | 흐름-통합(after) |
|---|---|
| **이한수 22**(1위)·드왈키 7·타리칸 5·무라드 5·**카락 4**·미샤 4 / **비요른 없음** | A=**비요른**·B=드왈키·C=히쿠로드 무라드·D=**브라운 로트밀러**·E=타리칸·F=미샤 칼스타인 |
- **유령 카락·이한수 완전 소멸.** 주인공 비요른·내비게이터 로트밀러 정확 복원(프로덕션은 비요른→이한수,
  로트밀러→카락으로 뭉갬).
- 실제 파티 5인 + 손님 타리칸 = **"6인=5인, 카락은 유령" 확증.**
- 됨의 이유: 파티 결성화라 **자칭**("자칭 비요른/드왈키/히쿠로드 무라드")이 많아 이름↔화자 결합 확실.

### ep41 — 분리 성공, 명명 배정 실패 ⚠️
- ✅ **카락·이한수 여전히 0.** 약탈자(엘리사·한스)를 파티와 정확 분리. 무라드·드왈키·엘리사·한스 정확.
- ❌ **주인공 슬롯(197블록=비요른)에 '로트밀러' 이름 오결합.** 비요른을 무라드 alias로도 오결합.
- 원인: ep41은 **호격**("로트밀러!" 길안내 21회) 밀집 → 모델이 "가장 많이 불린 이름"을 "가장 많이
  말한 슬롯"에 결합. = §8 ③(호격=화자 오결합) + Magiv2 cannot-link이 고치려는 바로 그 실패.

### E2E 결론
- **구조적 오염(죽은 카락 유령·빙의 이한수 자석)은 흐름-통합이 확실히 제거** — 두 회차 모두 카락·이한수 0.
  face-attribution 재앙이 흐름 접근에선 원천 미발생.
- 남은 것은 **밀집 호격 회차의 "이름→슬롯 배정" 오류**(재앙 아님, 교정 가능한 명명층 문제).
  → §8 ③(호격=수신자, 단일-mention 규칙) + cannot-link/must-link이 다음 실측 대상. 자칭>호격 신뢰 가중도.

### ep41 v2 — 호격규칙만으론 부족, 교차회차 전파가 진짜 해법 (실측)

ep41 재실행 시 프롬프트에 "호명된 이름은 상대일 수 있다 — **자칭/나레이션 명명일 때만** 슬롯 이름
확정 + cannot-link" 추가:
- 결과: 오류를 **고치지 못하고 뒤섞음.** A(비요른=엄격한 바바리안 리더)는 여전히 '로트밀러',
  B(미샤=`~냥`·`~당` 말투)가 '비요른'으로. v1의 "비요른=무라드 alias" 오류는 사라졌지만 새 오류 발생.
  드왈키·무라드(자칭)·엘리사는 정확.
- **진짜 원인**: ep41엔 비요른·로트밀러의 **회차 내 앵커(자칭/나레이션 명명)가 없다** — 둘 다 호명만
  됨. 그래서 "어느 슬롯이 로트밀러인지" 텍스트만으론 확정 불가.
- **함의**: 클러스터링(6명 분리)은 견고, **이름→슬롯 결합만 앵커 부재 시 실패.** 호격 프롬프트 규칙은
  불충분. 진짜 해법 = **교차회차 이름 전파**(ep37에서 자칭으로 확정된 비요른·로트밀러 앵커를 ep41
  슬롯에 링크) = 설계 §4 ③·C6. **값싼 패치가 아니라 링커가 필요함을 실측 확인.**
  → 자칭 앵커가 있는 회차(ep37)에서 이름을 확정하고, 그 앵커를 시각/서사 링크로 후속 회차에 전파하는
  것이 명명층의 핵심 남은 작업.

## 13. 교차회차 링커 프로토타입 — 앵커 없는 회차의 이름을 persona로 전파 ⭐

E2E가 남긴 문제(ep41은 자칭 앵커 없어 이름→슬롯 결합 실패)를 링커로 해결. 스크립트
`anon-roster/exp_linker.py`. 파이프라인: ①ep41을 **이름 없이 persona만** 재클러스터링 →
②prior 로스터(ep37 자칭 확정 name+persona)에 **persona(화법·역할·관계)로만** 매칭 → 이름 전파.
**얼굴·CCIP 0 사용.**

| ep41 익명 슬롯(persona) | 회차내 명명(실패) | **링커 전파(교정)** |
|---|---|---|
| A: 바바리안 탱커·게임 플레이어 시점·반말 | ❌ 로트밀러 | ✅ **비요른 (0.95)** |
| C: 네비게이터·베테랑·하오체 | 놓침 | ✅ **브라운 로트밀러 (0.75)** |
| D: 드워프·신관에 호감·친근 | 놓침 | ✅ **히쿠로드 무라드 (0.9)** |
| B: `~냥`·`~당` 애교 | ❌ 비요른 | ✅ **미샤 칼스타인 (0.45, 정직한 저확신)** |
| E: 엘리사(신관 사칭) | — | ✅ **null=신규** (ep37에 없음) |
| F·G: 조난자 | — | ✅ **null=신규** (타리칸 0.2 부분매칭도 올바로 기각) |

**결과:**
- ep41 이름 오배정(비요른↔로트밀러)이 **정확히 교정.** 회차내 명명이 못 한 것을 교차회차 전파가 해결.
- **신규 인물(엘리사·조난자)은 억지 매칭 없이 null=신규**로 정직 판정(§C5). prior에 없는 persona는 안 붙임.
- confidence 캘리브레이션 양호(비요른 0.95·무라드 0.9·로트밀러 0.75·미샤 0.45·신규 0.0).
- **핵심 신호 = persona(화법 지문·역할·관계)** — 얼굴 임베딩도 CCIP도 안 씀. 화법은 회차 넘어 안정적
  (비요른 반말·미샤 ~냥·로트밀러 하오체·무라드 드워프)이라 링킹 키로 강력. §C8(빙의)도 이 방식이면
  이한수↔비요른을 '말투 연속'으로 이을 수 있다(§4.8 ④와 동일 신호).

### 전체 아키텍처 E2E 검증 완료
```
Stage V(익명, 이름 미주입) → 흐름-통합(회차전역): 슬롯 클러스터+화자배정(0.95)+회차내 명명(자칭시)
   → 교차회차 링커(persona 매칭): 앵커 없는 회차에 이름 전파 + 신규=null
```
- E1: 흐름 화자배정 0.95(견고) / E1b: 통합이 과분할(17→2) 해결 / E2E: 구조오염(카락·이한수) 제거 /
  링커: 앵커 없는 명명 교정. **흐름-first 척추가 처음부터 끝까지 실측으로 성립.**
- 얼굴은 이 전 과정에 **불필요**(대표 crop 1장·교차 투표 외). face-attribution 재앙이 원천 미발생.

## 14. 연쇄 링커 스트레스 테스트 — ep37 시드 → ep38~44 순차 전파

`anon-roster/exp_linker_chain.py`. ep37 자칭 로스터를 시드로, 38→44를 순차로 흘리며 매 회차 슬롯을
누적 prior에 persona 링크 + 신규 회차내 앵커로 추가. 정답 대조(대사 이름 호명): 카락 **0/전회차**,
핵심5(비요른·로트밀러·무라드·드왈키·미샤) 전회차 등장, 엘리사 ep41부터.

**✅ 성공(코어 견고):**
- **카락 7/7 회차 전부 미출현** — 죽은 유령이 연쇄 내내 안 생김(최대 목표).
- **드왈키·미샤 = 록솔리드**(특유 어미 ~시오/~냥·~당으로 7회차 안정, conf 0.9+).
- **신규 정상 추가**: 엘리사(ep41)·한스 아르·이리타 테이슨 자칭으로 올바로 신규(이리타 테이슨은
  프로덕션 ep41 얼굴목록 실인물).

**⚠️ 실패(스트레스가 드러낸 2결함):**
1. **역할 기반 드리프트** — 비요른/무라드/로트밀러는 전부 "리더/POV/베테랑"이라 화법만으론 특정 회차에서
   안 갈림. ep39 비요른 POV슬롯→'무라드', ep42 리더→'미샤' 스왑. (어미 독특한 드왈키·미샤는 안 흔들림
   → **차이는 persona 특이성**.)
2. **오류 compounding** — "라락"(ep43 대사에 1회 실재, 카락 오독 추정·자모유사 0.8)이 로스터 진입 →
   ep44 전파. new-name 게이트 약함.

**정확도**: 핵심5 중 회차당 ~4/5, ep39 최악(2/5), 라락 phantom이 ep43~44 오염.

**설계 함의(스트레스가 가드 필요성을 실증):**
- 드리프트 → §8② **persona를 안정 지문으로 영속·매칭**(매 회차 느슨 재도출 금지, 앵커 지문 고정 키) +
  cannot-link. 화법이 겹치는 인물은 **역할어(리더/POV)로 매칭하면 안 되고 고유 어미·관계로**.
- compounding → §8①⑤ **발화행위 게이트 + 다중근거 + 자모 dedup**(라락 단일언급·라락~카락 0.8 → 차단)
  + self-verify 2차콜. **신규 추가는 자칭 1회로 부족, 다중 확증 요구**.
- → 링커 코어는 성립하나 **가드 없이는 장기 연쇄에서 드리프트·phantom 축적.** 재설계 가드가 실증 정당화됨.

## 15. 무오염 신규 에이전트 — ep1 독립 분석 (방법론 삼각검증, 2026-07-22)

하네스·우리 분석파일 일절 미제공, 웹툰 제목도 미공개, **원본 이미지 142컷만** 준 신규 에이전트에게
"인물·요약·네러티브 추출 + 어떻게/왜 분석했는지 기록"만 지시. 산출:
`scratchpad/ep1_fresh_agent_analysis.md`.

**독립적으로 우리 핵심 원칙을 재발견(삼각검증):**
- 142컷 **순차 8컷씩·무스킵**(나레이션이 검은 gutter에 있어 못 버림) → 원칙1 + §4.2 재발견.
- **3개 텍스트 레지스터**(회고 나레이션/생대사/게임 시스템텍스트) 추적 → §4.5 재발견.
- **지연 명명**: 주인공을 끝까지 무명으로 두다 c134~142 "이름 도박"에서 비요른 확정 → §4.7·§C5 재발견.
- **소급 재라벨**: 초반엔 "주인공=카락"으로 가정했다가 **끝에서 뒤집음**("무명이라 명시 + 이름 도박이
  성립하려면 주인공≠카락") → §4.8②·§C6(소급 재라벨 1급 연산) 재발견. **오염 없이 수렴.**

**→ 우리 설계가 특이한 게 아니라, 강한 VLM이 신중히 분석하면 자연히 도달하는 구조임을 독립 확인.**
그리고 에이전트가 성공한 건 **끝에서 가정을 뒤집었기 때문** — 약한 모델이면 "주인공=카락"을 커밋했을
것(=프로덕션 오염의 씨앗). 소급 재라벨이 왜 1급이어야 하는지 실증.

**⚠️ '카락 죽음' 전제 미세 교정 (독립 근거):** ep1에서 **참수당하는 건 오름(오름, 카두아의 아들)** —
게임을 언급한 다른 전입 플레이어다. **카락은 도끼를 고르는 부족 성인식 청년(단역)**이고 주인공도
아니다. 즉:
- 비요른(주인공, 응답 없는 소환에서 이름을 **도둑질**)= 이한수(전입 플레이어) → §C8 빙의 확인.
- **카락 ≠ 비요른, 카락 = 배경 단역** → 프로덕션의 카락(162얼굴·45개화) 자석이 **더욱 명백히 오귀속**.
- 사용자의 "카락 1~2화 죽음"은 ep2 미확인이거나 오름과의 혼동 가능 — 어느 쪽이든 **카락=단역**이라
  우리 결론(프로덕션 카락 자석=순수 face 산물) 불변. 오히려 강화.

## 16. 연쇄 링커 v2 — 가드 추가 결과 (ep38~44)

`exp_linker_chain_v2.py`(고유어미 강조 + dead-check + 자모 dedup). v1 대비:

| 항목 | v1 | v2 |
|---|---|---|
| 라락 phantom | ep43~44 오염 | ✅ **완전 소멸** |
| 어미 지문 | 역할어 위주 | ✅ 고유 어미 명시(~구려·~당·~소) — 변별적 |
| 미샤·드왈키 | 안정 | 안정(0.95+) |
| 비요른 드리프트 | ep39→무라드·ep42→미샤 | ⚠️ 감소했으나 잔존(ep38 None·ep44 누락) |
| 로트밀러/무라드 | 산발 | 산발(~3/7) |
| 새 결함 | — | ⚠️ **"엘리사 베헨크"**(ep42) = 엘리사 중복, 자모 dedup 미포착(이름+성 vs 이름) |

**🔑 핵심: dead-check가 7회차 내내 한 번도 발화 안 함.** 라락을 막은 건 dead-list가 아니라
**어미지문 강조 + 앵커 게이트**. → 사용자 지적("문지기=발화행위 게이트, dead-veto 불필요")이
v2에서 이미 실증. v3는 dead-veto를 완전히 빼도 됨.

**남은 진짜 난제 2개(프롬프트로 안 풀림):**
1. **역할 겹치는 인물 드리프트**(비요른·로트밀러·무라드는 다 "리더/POV/베테랑"). 어미 특이한 미샤·드왈키는
   안 흔들림 → 해법 = **지문 누적·영속**(회차마다 재도출 말고 증거로 갱신 + 관계그래프 앵커).
2. **이름+성 중복**(엘리사 베헨크 vs 엘리사) → 자모 밖의 **포함/부분문자열 dedup** 별도 필요.

## 17. 연쇄 링커 v3 + v1/v2/v3 비교 — 게이트로 되는 것 vs 구조가 필요한 것

`exp_linker_chain_v3.py`(발화행위 게이트 self/narration_subject만 + 3인칭 언급 분리 + dead-veto 제거).

| 차원 | v1(가드X) | v2(어미지문+dead-veto) | **v3(발화행위+3인칭, dead-veto 제거)** |
|---|---|---|---|
| 라락 phantom | ❌ ep43~44 | ✅ 소멸 | ✅ 소멸 |
| dead-veto | — | 넣었으나 **0회 발화** | **제거**(라락 여전히 안 나옴) |
| 카락 등장 | 0/7 | 0/7 | 0/7 |
| 3인칭 언급 분리 | ❌ | ❌ | ✅ **mentioned 분리**(에르웬·테일론·카락·칼스타인경) |
| 미샤·드왈키 | 안정 | 안정 | 안정 |
| 역할 드리프트(비요른/로트밀러/무라드) | 큼 | 감소·잔존 | **잔존**(ep38 비요른 누락→A=무라드, ep41 비요른 slot=타리칸) |
| 부분이름 중복 | 라락(별개) | 엘리사 베헨크 | ❌ **악화: 로트밀러/브라운 로트밀러 + 엘리사 베헨크** |

**✅ 게이트가 해결(사용자 지적 실증):**
- 라락은 dead-list 아니라 **발화행위 게이트**로 막힌다 — v3는 dead-veto를 뺐는데도 안 나옴.
- **3인칭 언급 분리 작동**: 카락 언급·에르웬(부재인데 언급)·테일론(미샤 오빠)이 mentioned로 빠지고
  어느 슬롯에도 안 붙음. = §8③이 요구한 구조.

**❌ 게이트로 안 풀리는 구조적 문제 2개(v3가 오히려 드러냄):**
1. **역할 드리프트** — 비요른/로트밀러/무라드는 어미가 겹쳐 회차별 재클러스터 시 스왑. 미샤(~냥)·
   드왈키(~소)는 어미 특이해 불변. **프롬프트로 안 풀림.**
2. **부분이름 중복** — v3가 발화행위 게이트를 엄격히 하니 자칭에서 신규를 더 잘 만들어, "로트밀러"(자칭)와
   "브라운 로트밀러"(시드)가 별개 등록(자모 0.6<0.70). 엘리사/엘리사 베헨크도.

**결론 — 구현 전환점:** 3판 스트레스가 계속 같은 곳을 가리킨다. 게이트/프롬프트로 되는 건 다 됐고
(라락·3인칭·카락 제거·어미지문 클러스터링), 남은 둘은 **실제 상태·스키마**가 필요:
- 드리프트 → **persona 지문 영속·누적**(재도출 금지, 증거로 갱신) + 관계그래프 앵커.
- 부분이름 중복 → **alias/canonical 구조 + 포함(substring) dedup**("브라운 로트밀러" ⊇ "로트밀러" →
  같은 엔티티, 정본=긴 쪽). = §C8.1이 이미 요구.
- → **다음은 프로토타입이 아니라 스키마/파이프라인 구현.** 이 두 구조가 링커의 마지막 남은 조각.

## 남은 검증(다음 세션)
- 시각클러스터 회차전역 dedup(17→외형 정답개수) 실측 — 흐름 밖 침묵 인물 로스터.
- 흐름-통합을 실제 오귀속 회차(ep37~44 카락/로트밀러)에 적용해 교정되는지 E2E.
- 실험 산출: `anon-roster/exp_speaker_flow.py`(E1a/E1b), `exp_speaker_realism.py`(E1c),
  `exp_jamo_norm.py`(E5). 데이터 `datasets/speaker_flow/`.

## 18. 구현 상태 (2026-07-22 세션 — 계획서 `~/.claude/plans/fancy-sniffing-stroustrup.md` 승인 후)

### ✅ Phase 1 — 스키마(service) 완료·검증(미적용)
- `service/backend/apps/api/toon/models.py`: CharacterAppearance→회차슬롯(episode·local_id·persona·
  prominence·timeline_context·representative_detection·link_method/confidence + 부분 unique),
  **NameEdge 신규**(surface·canonical_name·is_canonical·role 7종·confidence·source·status), Character
  (persona·narrative_state·last_seen_ep·is_adopted_name), TextBlockType +5종·speaker_applicable·is_diegetic,
  WebtoonPipelineState.flow_first_enabled.
- `migrations/0037_flow_first_identity.py` 생성, 전부 **db_default**, `manage.py check` 통과.
- ⚠️ **migrate 미적용** — 가산적·flag off라 Temporal 무중단 적용 가능(`SET lock_timeout='3s'` 권장).

### ✅ Phase 2 — StageV 익명화(pipeline) 완료·검증
- `src/core/step3.py`: `_flow_first_enabled(webtoon_id)` 헬퍼, `build_pass1_input(anonymize=)`(ON이면
  identified_faces=id만), extract_cut/extract_episode 배선. py_compile 통과.

### ✅ Phase 3 코어(pipeline) 확정 — 배선만 migrate 후
`src/core/step3.py`(extract_roster 뒤): `consolidate_episode`(익명 슬롯+흐름 화자+이름 3역할, exp_speaker_flow
이식), `_build_consolidate_payload`(익명 트랜스크립트, 정체성 0), `_sanitize_consolidate`, `_commit_slots`
(회차슬롯 appearance+persona), `_commit_name_edges`(name_edge 역할별), `_project_names`(projector D1a:
엣지→name/aliases, 역할 우선순위 human>self>card>narration_subject>vocative), `_commit_flow_speakers`
(흐름→speaker_id, human 불가침), `consolidate_and_commit_episode`(오케스트레이터). py_compile 통과.

### ⏭️ 배선(migrate 후, 옵션 B) — 남은 것
1. **resolve_and_narrate(step3b) 게이트 브랜치**: flow_first_enabled면 extract_roster+R-화자 대신
   `consolidate_and_commit_episode` 실행, N(서사)은 유지. ResolveResult.characters/speaker_resolution 비워
   apply가 재커밋 안 하게. **flag-off 경로 무영향이 핵심** — end-to-end 테스트하며 붙인다.
2. **재분석 멱등**: `_commit_slots`가 슬롯마다 새 cluster 생성 → 회차 flow appearance **선정리(prepare)**
   또는 ON CONFLICT 필요(현재 재실행 시 부분 unique 위반). 배선과 함께.
3. **temporal**: EpisodeChainWorkflow는 무변경(게이트가 step3b 내부라). 필요 시 `arc`/`consolidate` run kind로
   Phase 4 링커 분리.

### 배포/검증 순서 (계획서 P8)
service migrate(0037, lock_timeout) → pipeline 배포(P2+P3코어) → **웹툰23 `flow_first_enabled=true`** →
배선 브랜치 붙여 재분석 → 수용기준 검증(카락 자석 0·파티 5인·narration 화자↑).

## 관련 문서
- `prd-for-improve.md` §2(의존 역전)·§C1.1·§C5·§C6·§C7·§C8/8.1/8.2·§4.6·§4.8
- `anon-roster/EXPERIMENTS.md` 실험 8(glm-4.6v 실측)
- 메모리: suggestion-adjudicator-dryrun-2026-07-12, anon-glm46v-experiment8-2026-07-22
