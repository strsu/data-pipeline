# 인물 정체성 & 서사 로스터 — prd.md 보강 제안 (핀트 어긋남 해결)

> **상태**: v0.1 조사·검증 완료, 구현 전 · 갱신일: 2026-07-06
> **성격**: `prd.md`(§9/§17 Step3)를 **대체하지 않는 보강 문서**. Step3 LLM 스테이지(resolve/narrate)의 정체성·화자·서사 품질 결함 원인규명 + 개선안.
> **이어가기(세션 리셋 후)**: 이 문서 + 메모리 `identity-roster-experiment-2026-07-06`, `feedback-llm-model-selection` + 실험 스크립트 `webtoon-pipeline/_ident_experiment.py` / `_faith.py`~`_faith4.py` / `_gen.py` / `_glm52_retest.py`(uncommitted, throwaway). 실험 원본 응답은 세션 scratchpad에 저장했으나 세션 종속 — **핵심 수치는 본 문서에 인라인**함.
> **코드/DB 변경 없음**: 전 과정 읽기 전용 실험. 파이프라인/스키마 미수정.

---

## 0. 한 줄 요약

Step3의 "핀트 어긋남"(예: 죽은 천마를 산 것으로, 놀란 주체·정보 전달자 뒤바뀜, 인물 오귀속)의 뿌리는 **얼굴 임베딩도, 이미지 부재도 아니라 ①추론 스테이지에 권위 있는 '서사 로스터'가 없음 + ②텍스트 스테이지에 비전 모델(glm-4.6v)을 오용**한 것이다. 개선: **텍스트 스테이지를 glm-5.2로 교체 + 에피소드 로스터 추출·주입 스테이지 신설.**

---

## 1. 문제 정의 (실측 사례)

대상(검증에 사용한 회차): **화산귀환(naver/769209, 무협)** ep1(id=1255)·ep2(1334)·ep3(1333) + **아카데미에서 살아남기(820097, 게임 빙의물)** ep2(11757)·ep3(11758) + **참교육(758037, 학원 액션)** ep1(7729). = **3웹툰·3장르**.

핀트 어긋남 실측(ep1, 프로덕션 DB 산출):
- `episode_report.summary`: "천마가 **살아있다는** 사실" ← 원문(컷123 "백년 전에 죽은 대마두잖아?")은 **사망**.
- `character_profile`: 놀란 주체·정보 전달자가 뒤바뀜(거지가 놀랐다고, 실제론 청명이 놀람).
- 회차를 넘어 **오류가 프로필에 박제·전파**: ep2/ep3에서도 char1(청명) 프로필에 "천마 살아있다" 잔존, char4가 "화산 장문인"인데 천마(마교 교주) facts를 그대로 보유.

---

## 2. 정본 인물 로스터 (사용자 확인 — 테스트 오라클) ⭐

이 로스터는 향후 정체성/화자/서사 품질의 **정답 기준**으로 쓴다. (2026-07-06 사용자 확정)

**화산귀환 (초반부):**
- **청명** — 주인공. 대화산파 13대 제자·매화검존. **천마를 죽이고 100년 후 환생**. 환생한 **거지 소년 몸의 원래 이름이 '초삼'**(거지판에서 청명은 초삼으로 불림). 즉 **청명 = 초삼**(동일 인물, 몸의 이름).
- **구칠** — 거지. 청명에게 **그 몸의 원래 이름(초삼)과 천마의 죽음**을 알려준 **정보 전달자**.
- **왕초, 종팔** — 거지 무리 인물(구칠과 같은 걸개 패거리).
- **운암** — 화산파 **제자**(장문인에게 보고, 처소/입관식 준비). 장문인 아님.
- **장문인** — 화산파의 **수장**. ep2 말~ep3에서 청명의 입문을 허락.
- **천마** — 마교 교주. **100년 전 청명에게 죽음**(회상 속 인물, 현재 시점 미등장). ep2/ep3에는 등장·언급조차 없음.
- 청진 — 전쟁 중 실종된 화산 무인. ep3에서 청명이 "청진 후손" 거짓 각본에 이용(미등장).

✅ **환생 트로프 처리(검증됨, 정정 반영)**: 청명이 거지 소년(초삼)의 몸으로 환생 → **초삼 = 청명의 환생 몸 이름**(별개 인물 아님), 정보 전달자는 **구칠**. glm-5.2 로스터가 이를 정확히 인지함("청명… 거지들 사이 초삼이라 불림", 주요사실 "구칠이 왕초=종팔을 알려줌"). *(2026-07-06 세션 중 사용자가 한때 "청명≠초삼"이라 정정했다가 재정정 — 정본은 청명=초삼. 이에 따라 이전에 '청명/초삼 병합 = 잔여 오류'로 적었던 것은 취소함.)*

---

## 3. 조사 요약 (가설이 여러 번 뒤집힘)

전 과정 화산귀환 ep1 컷116–144(청명·거지 대화) + 전체 트랜스크립트 기반, LLM 콜 30+회.

| 가설 | 검증 | 결과 |
|---|---|---|
| (원안) step2 임베딩 빼고 VLM이 얼굴 crop으로 정체성 결정 | 112px crop 27개 몽타주 → glm-4.6v | **반증** — 24/27 과병합, 청명·거지 시각 구분 실패 |
| 추론 스테이지에 이미지 넣으면 교정 | 컷 이미지+텍스트 | 부분 — 관계 파악은 도움, but 천마 생사는 여전히 오답 |
| strict "사실 인용 강제" 프롬프트가 천마 생사 교정 | 재채점 | **반증** — 천마 생사는 텍스트만으로 11/12 이미 정답(문제 아님). strict는 who-is-who **악화** |
| **who-is-who 혼동의 뿌리 = 권위 로스터 부재** | 손-로스터 주입 | **확증** — 혼동 **0/6**, 사실 전부 교정 |
| "qwen ≫ glm(과추론)" | glm-5.2로 재검증 | **반증/정정** — glm-4.6v(**비전**)를 텍스트에 오용한 탓. **glm-5.2(텍스트)는 정답+빠름** |

핵심 수치:
- **천마 생사(사실)**: 로스터 없이도 텍스트만으로 11/12 정답 → **문제의 핵심 아님**(사용자 지적이 옳았음).
- **who-is-who 혼동**(죽은/부재 인물을 화면 인물로 오인): 로스터 없음 **7/12**(strict 프롬프트가 최악, "거지=천마" 확신+자기검증 "모순없음"). **손-로스터 주입 시 0/6.**
- **로스터 자동추출 present_now 정답**(천마=미등장): glm-4.6v ~1/7, qwen 2/2, **glm-5.2 5/5**(회상/현재 분리·환생 인지까지 정확).
- **속도**: glm-5.2 로스터추출 ~2.5–4.5분/회차, 분석 ~0.5–1.5분. qwen(~15분)보다 몇 배 빠름.

### 3.1 일반화 결과 (glm-5.2 로스터접근, max_tokens 100k · 세션-리셋 대비 인라인)

| 회차(장르) | 프로덕션(glm-4.6v)의 대표 오류 | 로스터접근(glm-5.2) 결과 |
|---|---|---|
| 화산 ep2 (무협) | "운암을 만나 입문"(운암=장문인 병합), char 프로필 천마 오류 잔존 | 운암=제자/장문인 분리 ✅, 종팔·사형 present=false ✅, 천마 없음 ✅, **초삼=청명 환생몸·구칠=정보전달자 정확** ✅ |
| 화산 ep3 (무협) | "청명과 초삼이 별개 동행"(초삼=청명 몸인데 별개로), 대인/장문인 혼용 | 회상 cut11–72 / 현재 정확, 청진 present=false ✅, 운암≠장문인 ✅, 각본·객청 엔딩 정확 |
| 아카데미 ep2 (게임빙의) | (요약은 대체로 정확) | 에드=주인공, 페니아, 가주 present=false ✅ (혼동 트리거 없어 loose도 깨끗) |
| 아카데미 ep3 (게임빙의) | — | **원작 주인공 테일리·원작 수석 루시 등 메타 참조 인물 present=false ✅**, 회상/현재 정확 |
| 참교육 ep1 (학원 액션) | — | 회상(cut1–62)/현재(63–137) 분리, **박대석 present=false·사망**(회상 속 자살 피해자) ✅, 우현식·담임 present=false ✅, 나화진·교감 present=true ✅ |

→ **로스터접근이 3웹툰·3장르 전부에서 회상/현재 분리 + 참조-부재 인물 present_now=false + 인물 역할 분리를 정확 처리**, 프로덕션 who-is-who 오류를 제거. 환생 정체(초삼=청명 몸)·정보전달자(구칠)도 정확. **알려진 잔여 오류 없음**(초삼 관련은 오해였고 정정됨). 상세 3열 비교는 세션 scratchpad `GENERALIZATION_3col.md`(세션 종속).

---

## 4. 근본 원인 (2가지)

1. **서사 로스터 부재.** 현재 파이프라인은 회차 간 확정 이름만 `confirmed_roster_prior`로 나른다(`narrative_context.load_prior`, `kind='character' AND name<>'' AND is_confirmed`). 첫 회차·미확정 클러스터뿐인 초반엔 **로스터가 비어**, 모델이 애매한 in-scene 언급("천마! 너 천마 아냐?!", 욕설 "얼어 죽을 천마")으로 정체를 **자유연상** → 현재 미등장인 천마를 화면 인물에 결부.
2. **텍스트 스테이지에 비전 모델 오용.** Stage R(resolve)/N(narrate)은 이미지 없는 텍스트 추론인데 **glm-4.6v(비전)**를 씀. glm-4.6v는 텍스트 추론을 과하게 해 반어·욕설·장르 트로프를 사실로 오독. **glm-5.2(텍스트)로 바꾸면 상당 부분 해소.**

> **별개 문제(로스터로 안 고쳐짐)**: 얼굴 클러스터/프로필 bleed — char4가 ep1의 천마 클러스터에 ep2의 장문인 얼굴이 병합되어 "화산 장문인"에 천마 facts가 박제됨. 이는 step2 임베딩(앵커 무제한 누적) + 프로필 누적 로직 문제(§5.4).

---

## 5. 개선 제안 (구체화)

### 5.1 즉효·저비용: Stage R/N 텍스트 모델 glm-4.6v → glm-5.2
- 대상: `step3.py`의 Stage R(`_RESOLVE_SYSTEM_PROMPT` 경로)·Stage N(`_NARRATIVE_SYSTEM_PROMPT`, `narrate_episode`) — **이미지 없는 텍스트 콜**. Pass-1(컷 비전, `extract_cut`)만 glm-4.6v 유지.
- 배선: `resolve_llm_model()`(`llm_resolver.py`)이 **스테이지별 모델**을 해석하도록 확장(현재 웹툰 단위 단일 모델). 또는 `config_llm_model`에 텍스트 기본을 glm-5.2로 두고 Pass-1만 override. (모델명 하드코딩 금지 — Req 7.1)
- 근거: glm-5.2가 천마 생사·회상/현재·환생 인지 정확(5/5), glm-4.6v는 오판(~1/7).
- **주의(운영) — max_tokens를 모델 용량대로 보낼 것**: 컨텍스트는 **glm-4.6v=32768, glm-5.2=131072, qwen-vl=131072**(게이트웨이 실측). glm-5.2는 추론형이라 reasoning에 토큰을 쓰므로 `max_tokens`를 넉넉히 줘야 JSON 절단이 안 남(실험 초기 16384로 두어 ep3 로스터추출이 48727자 뱉고 파싱 실패한 사례 = **설정 실수**). 권장: 텍스트 콜 `max_tokens≈100000`(프롬프트 ~20-25k 여유 남김; qwen은 prompt+max_tokens≤131072라 100000이 안전, glm-5.2는 131072도 수용). **파이프라인 반영 시 `step3.py`의 `_PASS1_MIN_MAX_TOKENS`/`_PASS2_MIN_MAX_TOKENS`(현재 16384)를 glm-5.2용으로 상향**하고, 모델별 max_tokens를 params로 해석(`llm_client.py`는 이미 `params.max_tokens`를 그대로 전달).

### 5.2 로스터 추출 스테이지 신설 + 주입
- **신규 스테이지(텍스트 전용, 에피소드 1콜)**: 전체 트랜스크립트를 읽고 인물 로스터 산출.
  - 필드: `name` / `naming_evidence`(컷+원문) / **`present_now`**(현재 시점 컷에서 실제 행동·발화해야 true; 언급·호명·회상만이면 false) / `role` / `status`(생존·사망·불명+근거) / `segments`(회상/현재 컷 범위).
  - 규칙: "언급≠등장", 반어·욕설("얼어 죽을 X", "너 X 아냐?!")을 정체/생사로 오독 금지.
  - **환생/빙의 처리(검증됨)**: 주인공이 다른 몸으로 환생/빙의하는 경우(청명→거지 소년 초삼, 에드→게임 악역)를 glm-5.2가 정확히 처리함(환생 몸 이름·원세계 기억 인물·원작 메타 인물 구분). 프롬프트에 "환생/빙의 시 주인공의 원래/현재 정체를 한 인물로 묶고, 정보 전달자 등 조연과 구분"을 명시해 유지할 것.
- **주입**: 이 로스터를 Stage R·N 프롬프트에 "확정 로스터 — present_now=false 인물을 화면 인물로 지목 금지"로 삽입. (현재 비어있는 `confirmed_roster_prior` 자리를 회차 내 로스터로 보강.)
- **효과(실증)**: 손-로스터 주입 who-is-who 혼동 0/6, 자동-로스터(glm-5.2)도 ep2 운암≠장문인 등 교정(로스터 없는 loose는 여전히 오병합).
- **영속/배선 옵션**(설계 논의 필요): (a) 순수 인메모리(회차 처리 중만), (b) 회차 로스터를 저장해 다음 회차 prior에 합류. 정본 인물은 여전히 `analysis_character`; 로스터는 회차 내 해소용 캐시로 시작 권장.

### 5.3 (선택) 사실 표현 — 생사류 평탄화 방지
- 천마 생사 자체는 glm-5.2로 대체로 잡히나, 잔여 방지책: `analysis_character_claim`(현재 미사용) 채워 "누가·언제 그렇게 주장" 형태로 근거 인용과 함께 커밋. 장르 트로프가 명시 사실을 덮지 않도록.

### 5.4 별개 트랙: 얼굴 클러스터/프로필 bleed (임베딩)
- 서사 로스터로 **안** 고쳐짐. step2 임베딩 앵커 무제한 누적(캡·프루닝·삭제 없음) → 후반 과병합, 초반 과분할. threshold 0.16이 배치 avg-linkage용인데 greedy 1-NN에 오용.
- 프로필 누적 로직이 클러스터 오병합 시 이질 facts를 한 인물에 박제(char4=장문인+천마 facts).
- 후속 과제(본 문서 범위 밖): 앵커 프루닝/프로토타입, threshold 재보정, 프로필 출처·정합성.

### 5.5 Pass-1 JSON 파싱 실패로 인한 컷 드롭 (로버스트니스)
- 증상(실측 로그): `naver/758037 ep1 cut28 — 비전 콜 실패(스킵): ... finish_reason='stop', 응답 935자: Expecting ',' delimiter`. **절단이 아니라 모델(glm-4.6v)이 살짝 깨진 JSON을 뱉은 것**인데 `_PASS1_RETRIES`(2회) 후 **컷을 통째 스킵** → 그 컷의 llm 텍스트·scene_meta가 없어 트랜스크립트에 구멍.
- 실태(완료 회차, scene_meta 없는 컷 기준): 화산 1–6%, 아카데미 ep2 **16%(17/109)**·ep3 7%, 참교육 ep1 4%. **단 대부분은 무텍스트 액션 컷**(아카데미 ep2는 17컷 중 16컷이 대사 없음 → 손실은 action_summary뿐). **실제 대사 있는 컷 드롭은 드묾**(아카ep2 cut104 1건, 참교육 cut28 등).
- 리스크: 빈도는 낮아도 **핵심 컷(인물 명명·사건 전환)이 걸리면 서사/화자 흐름을 왜곡**할 수 있음(사용자 제기).
- 수정(저비용): Pass-1 파서(`llm_client.py:_parse_json_content`)에 **json-repair 폴백**(콤마/괄호 보정) 추가 → `finish=stop`인데 파싱 실패 시 복구 시도 후 스킵. 대부분의 "Expecting ',' delimiter"류를 살림. (별도로 스킵 시 최소한 step1 paddle 텍스트라도 Pass-2 트랜스크립트에 포함할지 검토.)

### 5.6 불량 OCR / 오탐 얼굴 soft-delete (초기 논의, 별도 트랙)
세션 초반에 논의한 데이터 위생 이슈 — 정체성/로스터와 독립. 요지 기록:
- **깨진 효과음 OCR**(예: '하아…'→'Sror', 'Sof', '스록'): LLM이 `type='other'`로는 분류하나 파이프라인이 **exclude를 커밋하지 않음**(1화 `other` 44개 전부 `is_excluded=false`). **"말이 안 되는 텍스트"만** 지우려는 것이므로 `type='other'` 일괄 제외는 과삭제 — 별도 판정 신호(자모 깨짐/라틴 노이즈 비율 또는 LLM garbage 플래그) 필요.
- **오탐 얼굴**(예: 컷6 conf=0.435 비얼굴): model-api `FACE_CONF_THRESHOLD=0.3`뿐(낮고 모양 인식 없음), 파이프라인 추가 하한 없음 → `is_used=false`로 안 걸러짐.
- **삭제 방식**: 두 테이블(`analysis_text_annotation`·`analysis_face_detection`)에 `deleted_at`(Django soft-delete) + 도메인 플래그(`is_excluded`/`is_used`) 둘 다 존재. **권장: 도메인 플래그**(HITL UI에 계속 보이며 토글 가능) > `deleted_at`(전역 숨김이라 라벨링서 복구 어려움). 텍스트=`is_excluded`, 얼굴=`is_used=false`. 얼굴 오탐은 conf 자동하한보다 **HITL 위주**(경계값 애매) 권장.

---

## 6. 검증 현황 / 한계 / 다음 단계

**검증됨**: 화산귀환 ep1(강한 ground truth)·ep2·ep3 + 아카데미 ep2·ep3 + 참교육 ep1 = **3웹툰·3장르**에서 로스터접근(glm-5.2)이 프로덕션(glm-4.6v)의 who-is-who 오류를 제거(§3.1). glm-5.2 ≫ glm-4.6v(텍스트). **qwen-vl 비권장(판정 확정)**: 회차 따라 **불안정** — 화산 ep3·아카 ep3는 정확했으나 화산 ep2(운암=장문인 병합)·참교육(우현식·담임을 회상전용인데 present=true)에서 오판. **glm-5.2는 5회차 전부 일관 정확** + qwen이 glm의 2.6~10배 느림. → 안정성·속도로 glm-5.2 채택. 3웹툰·3장르 전부 glm-5.2 로스터 정확.

**한계**:
- 소수 회차. 대규모 일반화 미검증(장르는 3종 커버).
- glm-5.2 reasoning overflow는 max_tokens 상향으로 해소(§5.1).
- Pass-1 컷 드롭(§5.5), 얼굴 클러스터/프로필 bleed(§5.4)는 미해결.

**다음 단계 선택지**:
- **B(구현 설계)**: §5.1(모델 교체)·§5.2(로스터 스테이지)·§5.5(json-repair)를 실제 스키마·배선·프롬프트·토큰예산 스펙으로. (구현은 명시 요청 후)
- **C(임베딩 트랙)**: §5.4 얼굴 클러스터 위생.
- **D(추가 검증)**: 더 많은 회차/웹툰, qwen 최종 판정.

---

## 7. 부록 — 재현 방법 & 산출물

- 실험 스크립트(webtoon-pipeline/, uncommitted throwaway):
  - `_ident_experiment.py` — crop 몽타주(Test A)/컷이미지(Test B)/전체맥락(Test C).
  - `_faith.py`~`_faith4.py` — 충실도·로스터 주입·자동추출·추출robust 매트릭스.
  - `_gen.py <episode_id> [qwen]` — 임의 회차에 로스터추출→분석 + loose baseline(glm-5.2 기본).
  - `_glm52_retest.py` — ep1 로스터추출 모델 비교(glm-5.2/5/4.7).
  - `_rescore.py` — 원본 JSON 정정 재채점(자동 스코어러 버그 주의: 근거 문자열 키워드 오탐).
  - `_ident_gather.py` — 입력(crop/이미지) 수집·점검.
- 실행: `cd webtoon-pipeline && set -a && source ../prod.env && set +a && .venv/bin/python _gen.py <eid>` (읽기 전용, DB write 없음).
- **분석 뷰어**: `roster-viewer.html`(레포 루트, 자족형 HTML) — 5회차 glm-5.2 vs qwen-vl 로스터·분석 비교. 브라우저로 열람. webtoonmoa `src/routes/manage` 개선 시 참고용(인물 로스터·등장/미등장·근거 표시 UI 레퍼런스).
- 모델 선택 규칙: 이미지 필요 → `glm-4.6v`, 텍스트 전용 → `glm-5.2`(게이트웨이 vllm.prup.xyz, model_id만 교체).
- 프로덕션 오류 확인 쿼리: `analysis_episode_report`(summary) + `analysis_character_profile`(role/key_facts) — CLAUDE.md의 psycopg2 패턴.
