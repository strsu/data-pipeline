# 남은 해결과제 (remain-trouble) — 2026-07-24

세그먼트-단위 분석 + flow_first 작업 중 드러난 미해결 과제 종합. 관련 정본:
`docs/segment-unit-plan.md`(세그 구현), `chamgyoyuk-manual-analysis-2026-07-17.md`(참교육 수동분석),
`redesign-flow-first-2026-07-22.md`(흐름-first 원설계).

---

## ★ 결정된 방향 (2026-07-24 논의)

### 대조결과: flow_first true vs false (화산귀환 ep1)
| | flow_first=TRUE | flow_first=FALSE |
|---|---|---|
| **얼굴 매칭** | **0**(step2 purge) | **89 정체 + 76 클러스터** |
| 화자 | 대사명명 클러스터 | (CCIP 정체 주입 + 표준 resolve) |
- **결론**: flow_first=true는 얼굴(인물)을 **아예 버림** → 틀림. **인물을 버리면 안 됨**(사용자). 얼굴 매칭 차이 하나로 방향 확정. (step3 화자/서사 full 대조는 429-drop으로 불발, 얼굴차이로 충분.)

> **⭐ D1 상세 설계·경위·현황·계획 = `prd-for-character-linking.md`(정본, 자족 문서). 다음 세션은 그거부터 읽을 것.**

### D1. 정체성 = **a,b,c 슬롯 + 얼굴 클러스터 유지 + reconcile 명명** (채택)
flow_first의 **purge를 제거한 하이브리드**. reconcile은 참교육(박대석)으로 증명됨.
- **Stage V 익명**(CCIP 이름 주입 X — 오염 방지), **but step2 얼굴 클러스터링 유지**(purge X) → 얼굴=시각 슬롯 a,b,c(인물 안 버림).
- **reconcile + 대사로 이름 부여** → 시각 슬롯에 이름 얹음, 대사 증거로 소급 교정.
- = 얼굴(시각정체) + 대사/reconcile(명명) 이중구조. flow_first를 "purge"→"익명분석+얼굴클러스터유지+reconcile명명"으로 재설계.
- 배선 미정: reconcile을 회차별/전역 언제 돌려 이름 얹을지.

### D2. **세그-네이티브 저장** (채택 — 프론트 대공사 수용)
- region/face를 **세그 좌표로 저장**(cut_id→provenance 강등), 세그→컷 매핑 제거.
- **컷툰(육아일기 등)=1컷 1세그**로 취급 → 세그먼트가 **유니버설 단위**(컷/세그 이분법 소멸). 컷-단위 분석도 세그(=그 컷)에 저장.
- 프론트(webtoonmoa)·human 라벨링을 세그 기준으로 재작업.
- 이미지는 저장 안 함(공간). step3는 재조립(strip vstack+크롭) + strip_y로 bbox 배치.

### D3. **429-drop 버그** — ✅ **수정·배포됨**(`e2c0cbc`, 2026-07-24)
- ~~`call_llm_json`이 429(4xx) 재시도 안 함 → 세그 드롭~~ → **429만 백오프 재시도**하게 수정. 나머지 4xx 즉시실패 유지. (동시성은 여전히 낮게 권장.)

### D4. LLM 주도 lookback — **프로덕션 미적용**(정정)
- 사용자 요청("LLM이 필요시 이전 컷 다시 보기")을 "적용했다" 했으나 **프로덕션 Stage V엔 없음**(유닛당 이미지 1장, 이전컷 재조회 X). `reconcile`(텍스트 소급)과 혼동. 원하면 구현(직전N 롤링 컨텍스트 or 툴형 요청).

### D1 후속. Path A ✅ 구현·배포·검증완료 → **명명 재현율(recall) 갭 발견**(2026-07-24, 최우선 과제)
D1(Path A)은 구현·배포(이미지 `0545bd5`)·검증 완료. **정본 = `prd-for-character-linking.md`**(설계·구현·검증 전부). 요지: step2 익명 클러스터링(이름붙은 것 `name<>''`은 매칭 제외=자석 차단), Stage V 익명 slot 주입, consolidate 삭제→표준 resolve, 명명=대사. **검증(화산귀환 전체리셋 후 ep1·ep2 재분석)**:
- **ep1 통과**: 89얼굴→29익명클러스터·명명0(자석소멸), 대사명명 청명·동료걸인소년·천마(0얼굴=외형없는이름) 3명, misid=0.
- **ep2 성공+약점**: 교차회차 이름링킹 **저절로 작동**(구칠 ep1:2+ep2:1, 운암 ep1:1+ep2:1 = ep2 얼굴이 ep1 *익명* 클러스터에 CCIP매칭 → ep2 대사가 공유클러스터 명명. Q3 미명명-CCIP보조 실현).

**⭐ D5. 명명 recall 갭 — ✅ 해결·배포·검증 완료 (2026-07-24, naming 패스 + 자동귀속)**
- **구현·배포**(이미지 `a47259d`): step3.py `name_clusters_by_dialogue`(전-회차 트랜스크립트+화자 클러스터 라벨 → glm-5.2 → 자칭/호격/3인칭 이름 바인딩) + resolve_and_narrate union + `_project_characters` **자동귀속**(고신뢰≥0.85 대사명명 익명 클러스터가 동명 기존인물과 겹치면 `_attach_cluster_to_character`로 흡수: appearance/화자 재배정+소프트삭제). stage='naming' usage 등록. fresh/reresolve 양쪽 작동(speaker.character_id 폴백).
- **검증(화산귀환 ep2 reresolve)**: naming이 청명 5조각 잡음(과분할) → 고신뢰 3조각(4596·4600·4591) 자동귀속 → **청명 ep2 얼굴 0→34·대사 0→92**, ep1도 11→27(조각흡수), 교차회차 통합(4579=ep1:27+ep2:34). 저신뢰 2조각(0.60·0.75)은 suggestion 유지(정밀도 안전장치 정상). 4596/4600/4591 소프트삭제 확인.
- **결론**: 주인공 재현율 완전복구(청명 0→34). ~~정밀도 유지~~ → **정밀도 오염 발생(D6 참조)** — 자동귀속이 오명명 클러스터까지 흡수해 다른 인물 얼굴이 청명에 섞임. **재현율↑ but 정밀도↓ 트레이드오프가 뒤집힘.**
- 남은 후속: **D6 오염 수정 최우선**, 저신뢰(호격/3인칭) 바인딩 개선, 전회차 재분석 반영.

**⭐ D6. 자동귀속이 오명명 클러스터를 흡수해 얼굴 오염 (2026-07-24 발견, 최우선 — 원인규명 완료, 수정 미착수)**
- **증상(사용자)**: 청명 얼굴에 다른 인물(구칠/스승)이, 구칠 얼굴에 청명이 섞임.
- **원인규명(화산귀환 ep1/ep2, 크롭 몽타주+대사 대조)**:
  1. **CCIP는 얼굴 레벨에선 잘 분리**했다 — appearance 4605(상투 노인=스승)는 청명(젊은 거지)과 **별도 클러스터**로 유지(각 appearance 내부 일관). 즉 CCIP 얼굴혼합 아님.
  2. 4605 얼굴 컷(ep1 c68/69)의 대사 = "너는 무인이기 이전에 도인이다!"(스승이 청명 **훈계**) → 4605=스승 확정. 그런데 청명(4579)에 병합됨.
  3. reresolve 로그: naming이 **5개 클러스터를 "청명"으로 과다명명**(청명이 시각 5개일 리 없음). 청명을 **부르거나(호격 "청명아") 같은 화면서 상호작용하는 인물(스승)**의 클러스터에 청명 이름이 샘 — 대사흐름 명명이 "누구 얼굴이 말하나"vs"누가 언급/호명되나"를 못 가름.
  4. **자동귀속(_attach_cluster_to_character)이 이 오명명 클러스터들을 청명에 흡수** → 스승 얼굴이 청명으로 라벨.
- **결론**: 오염 벡터 = **CCIP 아니라 naming 과다부여 + 자동귀속**(내가 D5로 만든 기능). recall 고치려던 게 정밀도 오염 증폭기가 됨.
- ⚠️ **정정(사용자)**: 위 예시의 4605는 실은 **청명**(훈계받는 얼굴)이고, 실제 오염원은 **4614=구칠**(청명에 오병합). 스승/구칠 어느 쪽이든 **메커니즘(대사흐름 명명이 화자-얼굴 결속 실패 → 자동귀속이 오명명 클러스터 흡수)은 동일**. VLM 얼굴게이트 실험(glm-4.6v vs gemma4)도 노이즈·정답오류로 결론 유보 → **얼굴-ID 노선 폐기, 대사흐름 슬롯으로 선회(D7)**.

**⭐ D7. 정체 척추를 CCIP→대사흐름 슬롯으로 (2026-07-24, ✅설계·검증 완료, 파이프라인 적용 중)**
- **근본 전환(사용자 통찰)**: "얼굴인식 틀려도 화자 매핑이 정체를 실어나른다." 정체 = **대사흐름 익명 슬롯**(교대·화법·호칭·존대·POV), 얼굴은 **말풍선 꼬리로 슬롯에 종속 부착**. CCIP는 러프 prior만. 얼굴 외형매칭 안 함(각도·표정 변이에 안 무너짐).
- **왜**: 실측 — ①per-세그 Stage V는 명확한 꼬리는 잘 읽으나(c68 스승 오프패널 정확) **꼬리없는 뾰족풍선·교차컷 맥락을 구조적으로 못 봄**. ②교차세그 "seg1F0=seg2F1"을 **CCIP(약함)가 유일하게 매칭** → 오염 뿌리. ③VLM 얼굴매칭도 같은 컷 두 패널의 같은 청명(c100 F0·F1)조차 못 이음(외형 변이).
- **검증(3-테스트, glm-4.6v 윈도우 콜)**:
  - **v1 얼굴매칭 윈도우**: 청명을 여러 P로 쪼갬(c100·c104 붕괴) — ❌.
  - **v2 대사흐름 슬롯**: 7창(두 회차+장르)서 슬롯이 대사역할로 안정, **명명인물 격리**(구칠=B, 천마=A), POV독백 처리. 단일창 granularity 오류(W2 2명→4슬롯 과분할, W6 회상 삽입대사 과소분할).
  - **T2 carry-forward**: 이전 슬롯 로스터를 다음 창에 넘김 → **교차창 슬롯 안정(A=A) + 과분할 교정(c7-12 4→2슬롯)** 동시 해결. ⭐핵심.
  - **T3 타장르(웹툰43 학교코미디)**: 현대 반말·상황역할로도 코히런트 → **장르 불문**.
- **최종 설계**: **단일창 대사흐름 슬롯(정체 스파인) + carry-forward 로스터 앵커(교차창 링킹·granularity) + 얼굴은 꼬리로 슬롯 부착.** 하네스 `$CLAUDE_JOB_DIR/tmp/exp_window_flow.py`·`exp_window_carry.py`.
- **파이프라인 적용(목표)**: Stage V(병렬 지각) → ⭐**VLM 윈도우 reconcile**(슬롯·화자·얼굴부착, carry-forward) → R → apply. 슬롯=명명·화자 정체단위, CCIP=prior.
- **모듈 구현**: `src/core/step3_reconcile.py`(`reconcile_episode` 윈도우+carry-forward, cut_numbers override). **⚠️ gated·미배선(프로덕션 미커밋)** — 아래 스케일 문제로 배선 보류.
- **⚠️ 스케일 검증 실패 → 커밋 보류(2026-07-24)**: 전체 ep2(117컷~20창) 돌리니 **carry-forward 과앵커** — 회차 전체 캐스트(청명·구칠·왕초…)를 **2 역할슬롯(질문/반응, 설명/정정)으로 붕괴**. 원인=슬롯설명이 역할기반이라 재발역할이 뭉침. 페르소나기반 프롬프트로 재시도(c1-36) → 4슬롯(붕괴는 면함, D=구칠 자기소개 포착) but 설명이 순간행동("놀란 인물")이라 **정체 안정 미보장**. **이 상태로 배선하면 정체가 CCIP보다 악화(캐스트 붕괴) — 배선 금지.**
- **남은 크럭스 = 로스터 정체 안정성**(선결). 후보: ①**이름 앵커링**(슬롯이 대사로 명명되면 그 이름으로 고정·링크) ②**윈도우 겹침**(인접창 1-2컷 공유로 실연속성 앵커) ③**2패스**(1패스 캐스트확정→2패스 배정) ④페르소나 설명 말버릇/관계 강제. + 얼굴→슬롯 침묵/오프패널 미부착, 침묵얼굴은 CCIP prior 유지.
- 검증 하네스: `$CLAUDE_JOB_DIR/tmp/`의 exp_window_flow·exp_window_carry·exp_persona_test·run_reconcile_ep2.
- **채택 수정 = VLM 얼굴 검증 게이트(신규 단계)**. CCIP는 약해서(43%) 게이트에 못 씀 → **강한 VLM으로 자동귀속 전 "클러스터 얼굴 = 이 인물?" 판정**. CCIP=러프 클러스터, 대사=명명, **VLM=정체 거부권**(Path A 정합: VLM이 이름 안 몰고 오명명만 veto).
  - **실험(glm-4.6v vs gemma4, `$CLAUDE_JOB_DIR/tmp/exp_face_verify.py`, 청명 갤러리 vs 쿼리 클러스터)**:
    - **단일크롭 이진판정은 두 모델 다 실패**(과수용 — 스승·구칠도 청명이라 함). **멀티-얼굴 몽타주 대조가 필수.**
    - 몽타주 대조: **glm-4.6v = 게이트로 작동** — 오염원 스승(4605)·구칠(4618) **정확히 거부**(아니오 0.6~0.7), 청명 원본·조각 수용. clean 청명(4614)만 오탐 거부(갤러리가 피투성이만이라) — **오류가 안전방향(과소병합→suggestion, 오염 아님)**. **gemma4 = 부적합**(판별력 0, 전부 "같다" 0.95 — 오염 못 막음).
  - **게이트 설계(확정)**: 자동귀속 전 **glm-4.6v(등록 default)로 흡수 후보 클러스터 몽타주 ↔ target 인물 몽타주 대조** → 동일인 고신뢰면 흡수, 아니면 suggestion(사람 검토). 조건: 멀티-얼굴 몽타주 + 갤러리가 인물 외형범위 커버(clean/bloody 등). = 정밀도 방어(스승 차단) + recall 보존(오탐은 suggestion).
  - 보조 수정(선택): naming 자동귀속을 자칭만 허용, 이름당 primary 1개 상한, 호격은 응답슬롯에.

<details><summary>(구) D5 발견 경위 — 접기</summary>

**명명 recall 갭 — 큰 시각 클러스터가 이름 대사 있어도 익명으로 남음**
- **증상(ep2 실측)**: 최대 클러스터 app=4610(22얼굴)·4603(16)·4614(11)이 **명백한 주요인물인데 익명**. "청명"이 ep2 대사에 **8번** 나오는데 청명 명명 ep2 얼굴 **0**. 정밀도는 완벽(오명명0)이나 **재현율이 샘** — 대사에 이름이 있고 큰 시각 클러스터가 있는데도 안 붙임.
- **원인 가설(미검증)**: (a) resolve의 name_evidence→시각클러스터 **바인딩 약함**(이름은 잡되 어느 F/슬롯인지 결속 실패, 특히 자칭 아닌 호격/3인칭 언급); (b) 익명 slot 주입이 유닛-로컬이라 resolve가 큰 클러스터를 이름과 못 이음; (c) 명명 임계/증거요건이 과보수.
- **명명-제외 트레이드오프**: 청명은 ep1 명명됨→ep2 step2 매칭 제외→ep2 청명 얼굴이 새 익명클러스터→재명명도 안 돼 ep1 청명과 **단절**(ep2:0). *이미 명명된* 인물이 후속 회차서 대사 재명명에 의존하는데 그게 이 recall 갭에 걸림.
- **탐색 방향(실험 예정)**: ① resolve 이름→클러스터 바인딩 강화(대사 이름 + 최다등장 시각슬롯 매칭, 호격/3인칭도 활용) ② **Q3 웹툰-전역 명명 패스**(명명된 인물의 후속회차 익명 클러스터를 이름/시각으로 재링크 — 명명-제외 트레이드오프 해소) ③ Stage V slot을 resolve 페이로드에 노출해 큰 클러스터 가시화 ④ 명명 증거요건 완화(임계/역할). anon-roster 하네스로 대조 실험 후 최적 채택.

- **⭐ 실험 결과(2026-07-24, `$CLAUDE_JOB_DIR/tmp/exp_naming_bind.py`) — 해법 검증됨**: ep2 트랜스크립트(400대사)를 **화자 클러스터 라벨(S1..)과 함께** glm-5.2에 통으로 주고 naming만 시킴 → **지배슬롯 S1(char4596,58대사)을 자칭 "본도 청명"으로 청명 바인딩 성공** + S2(4600)도 청명(자칭)·S8 구칠·S19 운암. **naming패스+같은이름병합 결과 = 청명 90대사(2클러스터병합)·구칠8·운암3 vs 프로덕션 = 청명 0(누락)·시장거지대장15·구칠3·운암3.** 주인공을 통째 복구.
  - **결론**: recall 갭은 근본문제 아님. 프로덕션은 per-세그 name_evidence(face_label결속)라 자칭 "본도 청명"을 놓침 → **전-회차 naming 패스(클러스터 라벨과 함께 통으로)로 복구**. **화자배정 노이즈(c69 오배정)에도 자칭은 강건** → 화자재도출은 recall 선결 아님(별도 품질축).
  - **최적 해법(윤곽)**: resolve에 **전-회차 naming 패스 추가**(consolidate 명명 강점을 시각 클러스터에 바인딩) + **같은-이름 클러스터 병합**(이름=병합신호) + 기존 역할명명과 **합집합**(시장거지대장 유지). 후속: 호격 바인딩(never-자칭 인물)·조건B 화자재도출은 추가 품질축.

</details>

**우선순위**: ~~D3·D1~~ ✅ + **D5(recall) ✅** → **D8(cluster-first 정체추적 재설계, 현재 R&D)** → D2(세그-네이티브) → D4(lookback).

**⭐ D8. cluster-first 정체추적 재설계 (2026-07-24 R&D, 사용자 설계리뷰) — 정체=글로벌ID문제, 얼굴은 신호 하나**

> ### ✅✅ 2026-07-25 A+B+C 구현·배포·검증 완료 (아래 R&D를 프로덕션으로)
> 커밋: data-pipeline `b0bf6cf`(A+B+C) + `2da618b`(별칭수정) **push·배포됨**, service `c55491e`(마이그 0041) 적용됨. **cluster-first 전역 ON**(config `cluster_first_enabled` default true, 게이트 no-row→True).
> - **B**: `name_clusters_by_dialogue` = conversant-제약 **slot-coref 2콜**(`_extract_conversant_pairs`+`_alias_coref`). **slot 단위**(라인단위 O(라인수)는 긴회차 180s타임아웃 — glm-5.2 콜당 ~270s, litellm p50 268s=정상). 별호=aliases.
> - **C**: `resolve_global_identities(webtoon_id)` = pending name suggestion→클러스터별 정본→**name-link union-find**→대표 승격+나머지 `_attach_cluster_to_character` 흡수. `step3d_global_identities` activity(apply 후, 비차단·멱등)로 배선(workflows.py·worker.py). cluster-first OFF면 no-op.
> - **A**: 게이트 전역 ON(step3.py) + service 마이그 0041. **배포순서**: data-pipeline 먼저→service(구코드는 컬럼 안 읽어 순서 무관도 안전).
> - **리뷰 3회**: M1(클러스터별 자기신뢰 게이트) M2(`_GENERIC_NAME_STOPLIST` 호칭 오병합 차단) M3(흡수 전 소스 재검사=HITL 클로버 방지) m5(그룹당 단일 트랜잭션) + **별칭오염 수정**(정본과 함께 투표된 별호만 — flip 클러스터의 천마/운암 오유입·라우팅버그 차단, best_conf→canon_conf).
> - **검증(웹툰17 naver 769209 리셋→재분석)**: ep2 실측 **청명/구칠 별도 인물·얼굴데이터 분리·오염0**(D6 벡터 차단 실증), 청명 aliases=[매화검존] clean. **회차간 정체 flip 클러스터**(cid4647 천마↔청명, cid4631 청명↔운암) 존재 = CCIP/coref 고유 모호성, 다수결 처리.
> - **남은(E/follow-up)**: 폐기 D7 `step3_reconcile.py`(67bb5b0) 삭제·flow_first/consolidate dead정리 / **regen(reresolve) 경로 step3d 미배선**(정규 체인만 배선) / 무명 얼굴-link 병합(v1은 name-link만, Hungarian 미구현) / skip된 pending suggestion 잔류 / flip 클러스터 정밀화.


- **재프레임(사용자)**: 청명↔구칠 뒤집힘·오염은 얼굴정확도가 아니라 **Global Identity Resolution** 문제. 웹툰 얼굴은 약함(작화변이·측면·SD·가림·옷). → **멀티모달 추적**: `대사패턴·공기(cooccurrence)·얼굴·헤어·위치` 가중합 + Hungarian/그래프 전역클러스터링 + **이름은 최후에만**. 방법4(클러스터-우선, 명명-최후)가 핵심 — flip을 없앰. 리뷰: 동의, 단 **웹툰은 대사를 앵커모달로**(얼굴 가중 낮춤), Hungarian 불균형·로컬품질·가중치검증 함정 대비.
- **①D5 명명중단 = cluster-first**: auto-attach·eager promotion OFF, 이름은 커밋 아니라 **name_candidate**. 클러스터 익명 추적, 전역 최후 명명. → flip/오염 원천차단.
- **로컬 실측(하네스 `$CLAUDE_JOB_DIR/tmp/` — cluster_first·cluster_split·exp_multimodal·exp_novel_read·exp_face_attach)**:
  - **소설식 Pass1(전체 트랜스크립트 단일 텍스트콜, glm-5.2)**: 윈도우 carry-forward 붕괴를 우회 — 5장르(무협·학교·바바리안·무속·회귀)서 캐스트 5~12명 **정체기반 설명(백스토리·관계)+명명**. 윈도우식보다 압도적. **⭐ 이미지 불필요, 대사만으로 캐스트 추출**(사용자 "소설처럼" 직관).
  - **Pass2 얼굴부착(꼬리)**: 대사 캐스트에 얼굴을 꼬리로 부착 — c25 청명/구칠 분리 등 되나, **비슷한 인물(청명↔구칠) 매핑 스왑** 잔존(사용자 실측 정정).
  - **불안정성**: Pass1이 **비결정적** — 환생설정(청명=거지몸, 구칠도 거지)이 경계선 → 때로 청명+구칠 한 슬롯 병합, 때로 분리. **문제는 병합만이 아니라 경계선 인물쌍 불안정.**
  - **분할 신호**: 병합 슬롯은 **충돌 자칭**(청명+구칠 self-ID 둘) + **얼굴클러스터 2개**(4610=청명, 4614=구칠)로 flag → 분할 대상까지 지목.
  - **멀티모달 tiebreak 실측**: 대사슬롯 × CCIP appearance 집계 → 병합슬롯=복수강한클러스터(분할), 깨끗슬롯=단일(확증, 천마→4594). **단 co-presence 노이즈**(옆 얼굴도 크레딧, 4605가 사형슬롯에 오크레딧) → **깨끗한 얼굴→슬롯엔 꼬리 필요.**
- **수렴 설계**: **대사슬롯(정체 주) + 얼굴클러스터(tiebreak·확증) + 충돌자칭/복수클러스터→자동분할 + 이름 최후.** 다음: 꼬리기반 화자-얼굴로 멀티모달 노이즈 감소, 자동분할 로직 통합, Hungarian 전역링킹.
- **통합 파이프라인 실측(3회차, exp_pipeline.py)**: ⭐**ep2 S1(청명+구칠 병합) 자동분할 성공** — 충돌 self-ID 트리거→분할→**구칠대사→얼굴4614·청명대사→얼굴4610**(각자 얼굴클러스터로 확증). 대사가 쪼개고 얼굴이 확증 = 멀티모달 실전 작동. 이슈: ①ep1 S1 청명/매화검존 **별칭 과분할**(같은 인물 전생별호), ②얼굴클러스터"2+"플래그는 co-presence 노이즈(self-ID 충돌이 신뢰 트리거).
- **⭐ 3층 모델(사용자 통찰, 스키마 이미 지원)**: **Character(정체) —1:n— Appearance(외형: 젊/늙/옷/SD, 환생) —1:n— Name(별호: 청명/매화검존)**. `analysis_character` 1:n `analysis_character_appearance`가 이미 1:n. → 청명/매화검존 "과분할"은 버그 아니라 **한 캐릭터의 2외형·2이름**. 분할이 이름 2개 찾은 건 맞고, 2캐릭터로 취급이 오류.
- **⭐ 별칭 vs 다른사람 판별 = 공기(cooccurrence)**: **두 이름의 화자가 서로 대화하면 다른 사람**(청명↔구칠 = 청명이 구칠에게 물음 = 공존 = 분할O), **절대 공존 안 하면 같은 사람/별호**(청명/매화검존 = 매화검존은 청명 회상뿐, 비공존 = 병합). 얼굴이 젊/늙 달라도(1:n 외형) 공기가 정체 유지. → **분할 후 공기 판별로 별칭 병합** 넣으면 이슈① 해소.
- **⚠️ 프로덕션**: D5(a47259d) 자동귀속 라이브 → 신규분석 오염 지속. cluster-first 확정 후 prod에서 auto-attach OFF 필요.

### ⭐⭐ 야간 R&D 결론 (2026-07-25) — 아키텍처 확정. **다음 세션 여기부터**
**정본 설계 = `prd-for-character-linking.md` 최상단 "최종 설계 v2".** 하네스·연구로그 **영속 위치 = `anon-roster/identity-redesign/`**(exp_*.py + RESEARCH_LOG.md, gitignore지만 로컬 유지 — job tmp 아님). 실행: `cd webtoon-pipeline && set -a && source ../prod.env && set +a && VLLM_API_HOST=http://192.168.1.15:4000 .venv/bin/python ../anon-roster/identity-redesign/exp_coref.py`.

**확정 아키텍처:**
- **Pass1 = conversant-제약 별칭-coref (2콜, 텍스트, glm-5.2)** — `exp_coref.py`(무제약) / `exp_constrained.py`·`exp_best.py`(제약):
  1. **conversant쌍 추출**: 서로 대화(호명·질문답·교대)하는 인물쌍 = 확실히 다른 사람.
  2. **별칭-인식 coref + "conversant=다른캐릭터" 하드제약**: 회차 전체 트랜스크립트를 소설처럼 읽고 캐릭터 파악. 한 캐릭터=여러 이름(별호·직함·전생명·OCR변이·관계명) 네이티브 그룹핑.
- **경계선 안정화 = conversant 하드제약**(E8: 청명/구칠 병합 **0/3**, 제약없이 40%). 앙상블(E9 3/5)은 폴백.
- **얼굴 = 종속·확증·교차링크**(정체 주 아님): 꼬리로 얼굴→캐릭터. 얼굴클러스터 tiebreak(청명 appearance **4610** ≠ 구칠 **4614**). co-presence 노이즈 → 순수꼬리 필요(E11 진행).
- **교차회차 글로벌정체 = 이름링크 ∪ 얼굴클러스터링크**: 양쪽 명명(청명·매화검존, flip없음) + 무명(구칠 ep1"거지소년"/ep2명명, but appearance 4614가 회차span → 얼굴로 링크). E3·E6.
- **cluster-first**: 이름 최후, D5 자동귀속 정지. 코드 준비 = `_cluster_first_enabled` 게이트(step3.py, config `cluster_first_enabled`, 기본OFF·안전폴백). **로컬 커밋 `62e3bc7`, 미배포**(랩 outage 시 작성). ON이면 apply eager명명·승격·자동귀속 OFF, 이름 suggestion만.

**야간 실험 요약(E1~E11, RESEARCH_LOG.md 상세):**
- E1/E5/E10 별칭-coref 5장르 검증(별호·직함·전생명·OCR변이·언급만인물 그룹핑). E2 무제약 40%불안정. E7 conversant검출 신뢰. **E8 제약=3/3 결정론분리** ⭐. E9 앙상블 폴백. E3/E6 교차회차=이름∪얼굴. exp_best 통합 성공.
- 폐기: 윈도우 carry-forward(스케일 과앵커 붕괴, `step3_reconcile.py` gated·미배선). VLM 얼굴게이트(노이즈). 기계적 "같은컷 공기"(자기다중명명 거짓공기).

**다음 세션 재개 체크리스트:**
1. `prd-for-character-linking.md` 설계v2 + 이 D8 읽기. 하네스=`anon-roster/identity-redesign/`.
2. **미완**: ①E11 얼굴부착 정밀도(순수꼬리로 청명/구칠 스왑 개선) 결과 확인 ②전역 명명 최후 패스(coref+얼굴/공기 링크→이름) 구현 ③Hungarian/그래프 전역클러스터링(미검증) ④프로덕션 배선(conversant-제약 coref를 R단계로, cluster-first ON).
3. **시급**: cluster-first ON 배포로 D5 자동귀속 오염 정지(다른웹툰 재분석 전). 코드 준비됨(62e3bc7) — service에 config `cluster_first_enabled` 컬럼 마이그레이션 필요.
4. 인프라: 랩(spark1 .15 litellm/LLM) 야간 ~42분 다운 후 복구. LLM 대량은 직결+저병렬(429·과부하 회피).

---

## A. 세그+flow_first 조합 (둘 다 ON) — 미완성·논의 필요

화산귀환(17)을 segment_unit=true + flow_first=true로 돌리며 발견. 이 조합은 **E2E 미검증이었음**
(내 세그 E2E는 flow_first OFF였음).

### A1. ⭐ 얼굴↔인물 매칭 소멸 (flow_first purge) — **논의 대기(D)**
- `step2.py:714` — flow_first ON이면 step2가 **CCIP 정체결합 전면 스킵 + 기존 정체 purge**(`flow_first_purge`).
  → ep1 face_embedding=0·face_identity=0. 인물=대사흐름 명명 클러스터만(kind=character 승격 0, 얼굴 매칭 0).
- 사용자 지적: "분석(a,b,c) 다 하고 **나중에 얼굴 매칭**을 해야 하는 것 아닌가" — 지금은 아예 버림.
- **결정 필요 방향**: (a)회차별 step3 후 얼굴매칭 후속단계 / (b)전회차 후 웹툰전역 1회 매칭 /
  (c)flow_first를 얼굴매칭과 공존 재설계(익명분석+사후 얼굴 클러스터링·persona 링커로 이름전파).
  추천=(c) — 원설계의 persona 링커(대사·화법 회차간 연결) + 얼굴 클러스터 사후병합.

### A2. consolidate 경로 미검증
- 세그+flow_first는 flow-first **consolidate** 단계를 탐(내 Phase C resolve/apply 아님). 화자배정을
  consolidate가 함(슬롯5·배정361·speakers180) → **apply.speakers_resolved=0**(내 세그 region_map은 이 조합서 미사용).
- 즉 내 Phase C(세그 region_map)는 flow_first OFF일 때만 활성. flow_first ON+세그 조합의 정합성은 별도 검증 필요.

### A3. 화산귀환 재분석 검증 진행 중
- A/B/C fix(아래) 후 step3 재실행(`naver_769209_s3`) 중. ep1 서사 빵꾸 해소 검증 대기.

---

## B. 참교육 수동분석(chamgyoyuk-2026-07-17)의 미해결 발견

`chamgyoyuk-manual-analysis-2026-07-17.md` 참조. **스킵 컷 문제(세그 커버리지)는 07-22 재분석+strip_y로
해결됨**(ep1 c3→13리전·c5→10리전 검증). 아래는 아직 유효:

### B1. 나레이션 주인이 회차 안에서 바뀐다 (§4.8① 깨짐)
- 참교육 ep1: 다큐3인칭(c1~11) → 학생1인칭(c17~52) → 냉소적 성인화자(c60~). 회차 내 2번 전환.
- **`basis=pov`("나레이션=주인공")를 기본값으로 두면 안 됨.** 회차 내 판정 변수. → 화산귀환 서사 품질에도 영향 가능.

### B2. type enum이 못 담는 텍스트 (신규계약 C10 후보)
- 면책고지·법조문·인포그래픽/차트·뉴스자막·시간경과자막·문서프롭·스태프크레딧·편집자주.
- 전부 **"화자 없는 텍스트"** — 화자 슬롯 무의미. 현행 `speech|monologue|narration|system|other` 5종 부족.

### B3. 언급만 되고 화면에 없는 인물 (로스터↔이름 1:1 아님)
- 참교육 '대식'(자살, 화면 미등장, 이름만). 익명 로스터(외형)엔 안 잡히고 텍스트에만 존재.
- **외형 없는 이름 / 이름 없는 외형 둘 다 정상** — 로스터·명명 대응을 1:1로 강제하면 안 됨.

### B4. 명명 경로 ③ 문서/소품(prop)
- 나화진 이름=화면 속 공무원증 인쇄(ep1 c124). diegetic screen/document text(뉴스자막·상태창도 동류).
- OCR 취약(기울어진 원근·작은 글씨). 이름은 살고 직함은 깨짐(교권보호국→'교권보호구').

---

## C. 세그먼트-단위 구현 잔여 (기능은 배포됨, 개선 여지)

### C1. apply/reresolve가 region_map 위해 strip 재조립 (비효율)
- 세그 모드 apply가 `episode_region_map_and_ranges`로 컷 전량 재페치+vstack. fresh flow서 extract가 이미 함.
- **최적화**: ExtractResult에 region_map 부착해 apply가 재사용(재조립 회피). reapply(ep=int)만 재조립.

### C2. 뷰어(webtoonmoa) 세그모드 미대응
- 세그모드는 scene을 `segment_scene_meta`에 씀, `cut_scene_meta`는 빔. 뷰어가 옛 cut_scene_meta를
  읽으면 컷별 scene 안 보임. → 프론트가 segment_scene_meta 읽도록 수정 필요(webtoonmoa).

### C3. 재분석 필수
- strip_y·세그모드는 step1부터 재적재해야 반영. 기존 회차는 구 데이터. 대상 웹툰 재분석 체인 필요.

---

## D. 인프라·기타

- **YOLO 서버(gpgpu.prup.xyz) 간헐 502** — step1 YOLO 콜, 재시도로 self-heal(무해).
- **flow_first persona 링커 미배포** — 교차회차 정체(대사·화법)를 얼굴 없이 잇는 실험코드
  (`anon-roster/exp_linker*.py`). A1(c) 되살릴 때 활용.
- **litellm 인프라**: 로컬 대량 실험은 직결 `http://192.168.1.15:4000`(터널 502 회피)+동시성 낮게(429 회피).
  pod는 클러스터 내부라 무관.

---

## 우선순위 제안
1. **A1(D) 얼굴 매칭 후속화 설계** — 사용자와 방향 결정 후 구현 (가장 큰 미결).
2. **A3 화산귀환 재분석 검증** — 서사 빵꾸 해소 확인(진행 중).
3. **B1 나레이션 화자 전환** — 서사 품질 직결, 비교적 국소 수정.
4. **C1 apply 재조립 최적화** / **C2 뷰어 세그 대응**.
5. **B2 type enum 확장(C10)** / **B3·B4** — 계약·설계 논의.
