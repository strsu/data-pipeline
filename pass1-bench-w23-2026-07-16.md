# Pass-1 teacher/student 벤치마크 + 학습쌍 수집 — 바바리안 1~10화 (2026-07-16)

Phase 0 파일럿(lora.md §10) 실측 결과. 하니스: `webtoon-pipeline/tools/pass1_bench.py`.

## 설정

- **대상**: 웹툰 23(게임 속 바바리안으로 살아남기, naver/808482) 1~10화, 컷 1,287개(유효 1,175 — 빈컷/이미지없음 112 스킵, 프로덕션 Req 1.10 파리티)
- **입력**: 프로덕션 `build_pass1_input()`(step3와 동일 함수 공유) — human 얼굴 라벨 포함(`identity_source=human_first`, human>step2 투영)
- **모델**: teacher=glm-4.6v, student=qwen-base(Qwen3.5 9B Q4_K_M GGUF) — 둘 다 greedy(t=0.0), fallback 없음
- **동시성**: teacher 2 / student 1 (운영 Temporal과 게이트웨이 공유 — 양보)
- **산출물**: `datasets/pass1/78c51d18a1d9/` (프롬프트 해시 디렉터리) — `w23_e{1..10}.jsonl` + `images/*.jpg`(모델이 본 오버레이 원본) + `meta.json`

## 결과 — 구조 지표 (1,175 유효 컷)

| 지표 | teacher (glm-4.6v) | student (qwen-base) |
|---|---|---|
| 콜 성공 | 1,152 (에러 23 = 전부 429 rate-limit) | 1,174 (에러 1 = 연결 끊김) |
| json-repair 개입 | 32 (2.8%) | 33 (2.8%) |
| finish≠stop | 2 | 18 |
| **병합 위반 컷** | **8 (0.7%)** | **419 (35.7%)** — 병합쌍 821건 |
| **index 누락 컷** | **3 (0.3%)** | **501 (42.7%)** |
| index 여분 컷 | 1 | 129 (11.0%) |
| type 부여율(블록) | 99.7% | 80.8% |
| 평균 레이턴시 | 55.9s | 17.5s (**3.2배 빠름**) |
| 평균 completion 토큰 | 1,913 | 548 |

### student 병합은 블록 수에 비례해 폭발

| 컷의 OCR 블록 수 | 병합 위반률 |
|---|---|
| 1~4개 | 25% (192/774) |
| **5개 이상** | **69% (227/329)** |

에피소드별 병합률은 30~40%로 균일 — 특정 회차 문제가 아니라 모델의 일반 행동. 2026-07-10 6컷 실측(3/6)이 1,175컷에서 그대로 확정됐고, **누락(42.7%)이 병합보다 더 크다**는 것이 새 발견(6컷 실측에서는 표본이 작아 안 보였음). 병합·누락·여분을 합치면 student 컷의 **약 60%가 1:1 바인딩 위반** — LoRA 교정의 1차 타겟 지표.

## 결과 — teacher 대비 내용 일치 (1,151컷 비교)

| 지표 | 값 |
|---|---|
| 블록 type 일치(둘 다 type 부여한 블록) | 66.7% |
| 화자 일치(speech/monologue 전체, null 포함) | 68.5% |
| 화자 일치(한쪽이라도 배정한 블록만) | **42.4%** |
| 등장인물 face_label jaccard | 0.68 |

주의: 이건 "teacher=정답" 가정의 일치율이 아니라 **분포 차이**다 — 2026-07-10 실측에서 qwen이 옳고 glm이 틀린 사례(cut84/93)가 확인된 바 있음. 다만 SFT의 목표가 teacher 행동 복제이므로, 학습 후 이 수치가 얼마나 오르는지가 수렴 지표가 된다.

## 학습쌍 수율

- **클린 teacher 쌍: 1,110 / 1,175 (94.5%)** — 에러·repair·finish≠stop·구조위반 전부 없는 것만
- 그중 **human 라벨 얼굴 포함 컷: 452** (프리미엄 등급 후보)
- teacher 429 에러 23컷은 재시도하면 회수 가능(현 resume은 에러 기록도 완료로 봄 — 회수하려면 해당 라인 삭제 후 재실행, 또는 `--retry-errors` 추가 구현)

## 관찰/함의

1. **teacher도 완전 무결은 아님**: 병합 8컷·repair 32건 — 클린 필터(§5.2)가 실제로 걸러낸다. 429는 동시성 2에서도 발생(운영 step3와 겹칠 때) — 게이트웨이 부하 신호.
2. **student의 결함은 구조에 집중**: 콜 성공률·JSON 유효성(repair율 teacher와 동일)은 문제없고, 오직 1:1 바인딩(병합/누락)이 문제. "이해력은 되는데 출력 구조 습관이 나쁨" — SFT로 고치기 가장 좋은 형태.
3. **블록 수 비례 폭발(5+에서 69%)** → 학습 데이터에서 블록 많은 컷의 비중/가중을 높이는 게 효율적일 수 있음.
4. 속도 3.2배(대화 컷 기준, 동시성 배제 순수 레이턴시)·토큰 1/3.5 — 교정만 되면 Stage V 전환 가치 충분.

## 다운스트림 검증 — qwen 비전으로 step3b 끝까지 (2026-07-16)

Pass-1(비전) 구조 결함이 **최종 서사 산출까지 전파되는가**를 확인. qwen student의 Stage-V 출력을
입력으로 프로덕션 step3b 함수(로스터→Stage R→Stage N)를 그대로 재실행 — **텍스트 모델은 운영과
동일 glm-5.2(131K)**, 차이는 비전 소스뿐. DB 커밋 없음(`persist_usage=False`, apply 미호출, 읽기 전용).

- 하니스: `webtoon-pipeline/tools/pass2_run.py` (qwen V → glm-5.2 로스터/R/N 오프라인) — `extract_roster`
  → `resolve_episode_windowed` → `narrate_episode` 재사용. 산출 `datasets/pass2·pass3/<hash>/w23_e{no}.json`(+ pass2 `.md` 읽기용).
- 운영 정답 덤프: `tools/pass_prod_export.py` → `datasets/prod/`(episode_report/beat/thread/character).
- 비교 뷰어: `tools/pass_compare_view.py` → `datasets/compare_w23.html`(회차별 qwen vs 운영 나란히, 서버 없이 열림).
- Pass-1 뷰어(비전 3열: 이미지·glm·qwen): `tools/pass1_view.py` → `datasets/pass1/<hash>/view.html`.

### 1화 실측 (qwen V → glm-5.2)

| 산출 | qwen 재실행 | 운영(glm-4.6v→glm-5.2, DB) |
|---|---|---|
| 로스터 / 명명 인물 | 7명 / R chars 13 | 누적 인물(전역), 1화 명명 6 |
| 화자 해소 | 87 블록 | — |
| 서사 비트 | 9 | 8 |
| threads(1화 planted) | 7 | 8 |
| 회차 요약/티저 | 생성됨(요약 329자) | 생성됨 |
| 토큰(로스터+R+N) | 212,670 (3콜) | — |
| 소요 | 615s | — |

**요약/티저/비트/떡밥 내용이 운영과 동등 수준** — 이한수↔비요른 빙의, 오름 '악령' 낙인, 성인식
통과 도박 등 핵심 서사를 양쪽 모두 동일하게 포착. qwen의 1:1 병합·누락(35~43%)이 **서사 산출로
전파되지 않았다** — 텍스트 단계(glm-5.2)가 트랜스크립트 수준에서 복원. 즉 qwen의 결함은 Stage-V
구조 지표에 갇히고 최종 품질을 무너뜨리지 않는다 → **비전 슬롯 전환(Phase 2) 리스크 완화, Pass1-first
베팅 강화.**

### prior 2모드 — 운영 prior vs 순수 qwen 체인 (2026-07-16 추가)

step3b는 회차를 가로지르는 서사 기억(prior = 확정 로스터/미회수 떡밥/최근 요약)을 R·N에 주입한다.
prior는 **비전(pass1)이 아니라 다운스트림(pass2/pass3) 산출**이라 pass1만으론 만들 수 없다. `pass2_run.py`에
`--prior` 2모드 구현:
- `prod`(기본): `narrative_context.load_prior`가 **운영 DB(glm 기반 확정 로스터/요약)**를 읽음 — 프로덕션 동일 경로.
- `local`: **우리 qwen 이전화 pass2/pass3만으로 prior 조립**(`build_local_prior`) — 운영 DB 미조회, 순수 qwen 회차 체인.
  character_id 연속성은 faces가 DB step2에서 오므로 유지. 한계: human 확정·누적 도감·떡밥 회차간 정교한 해소는 없음.

출력은 모드별로 분리: `datasets/pass2·pass3/<hash>/{prod,local}/w23_e{no}.json`. 2~10화를 **local**로 실행 중
(백그라운드, ep2 로컬 prior roster=6/threads=6/summaries=1 — prod 모드와 동등 규모). prod 변형은 ep1~3까지
남아있음(`prod/`).

**실시간 뷰어**: `tools/pass_live_view.py` → `datasets/live_w23.html`(런타임 fetch+5초 폴링, 회차칩 진행바).
정적 대조는 `tools/pass_compare_view.py` → `datasets/compare_w23.html`. 둘 다 http 서빙 필요(fetch/정적 무관하게 datasets 루트에서).

### Stage V 프롬프트 플레이그라운드 (2026-07-16)

`tools/pass1_playground.py`(로컬 HTTP 서버 + UI, `--port 8790`) — 웹툰/에피소드/컷(step1·step2 분석된 것만
DB 로드)을 골라 프로덕션 동일 입력(`build_pass1_input`)을 조립하고, **임의 모델·thinking on/off·temperature/
top_p/top_k/max_tokens**로 Stage V 1콜을 날려 system prompt·입력·출력(reasoning/content/파싱 JSON)을 대조.
thinking은 `chat_template_kwargs.enable_thinking`으로 전송(응답 reasoning 유무로 반영 확인). 읽기전용(DB SELECT + 단발 콜).

- **qwen-vl(thinking=false) 2컷 스팟체크(바바리안 1화)**: 컷1(2블록)·컷2(7블록) **둘 다 1:1 완전 보존**(누락·병합 0),
  reasoning 0자(사고 OFF 확인), finish=stop, 78s·160s. 같은 컷2에서 **qwen-base(9B)는 병합2/누락4**였음 —
  qwen-vl(131k)은 Stage V 구조 습관이 훨씬 안정적(단, 모델 급이 달라 student 후보로는 별도 비용/속도 평가 필요).

### 11~15화 확장 (2026-07-16)

1~10화 순수 qwen 로컬 체인 검증 후 11~15화로 연장. 11~15 전부 step1·step2 분석 완료(분석 컷
108/92/100/103/105), 운영 glm 최종본은 **11~14 존재, 15는 진행 중**(비교 시 15 운영 칼럼은 glm 완료 후
`pass_prod_export --episodes 15` 재실행하면 채워짐).

- 11~15는 qwen Stage-V(pass1)가 없어 먼저 생성 필요 → `pass1_bench.py`에 **`--student-only`** 추가
  (glm 티처 콜 생략 — prod에 이미 있어 불필요, 비용/시간 절반). teacher 엔트리는 `error="student_only(skip)"`로 마킹.
- 체인 실행(백그라운드): `pass1_bench --episodes 11-15 --student-only` → `pass2_run --episodes 11-15 --prior local`.
  로컬 prior가 11화부터 1~10화 산출 위에 계속 누적(순수 qwen 체인 연장).
- 뷰어: `pass_live_view.py --max-ep 15`로 회차칩 1~15 확장. prod 익스포트 `pass_prod_export --episodes 1-15`.

## 다음 단계 (lora.md §10 Phase 1)

1. 429 에러 23컷 회수(재실행)
2. QLoRA 학습: Qwen3.5 9B **HF 원본 가중치**(GGUF 아님) + NF4, 클린 쌍 1,110건(train/val은 웹툰이 하나뿐이므로 에피소드 단위 분리 — 예: 1~8 train / 9~10 val)
3. 학습 후 같은 하니스로 재벤치: 병합/누락률(1차 게이트), teacher 일치율(수렴), human 라벨 대조
4. 서빙 형태(GGUF 변환 or 어댑터)로 최종 평가 — 학습/서빙 양자화 drift 포함 검증
5. 데이터가 웹툰 1개뿐이라 **일반화는 미검증** — 다른 웹툰 재분석분이 쌓이면 val을 웹툰 단위 분리로 전환(§5.3)
